# STATE.md

A living "where am I" snapshot. Update at the end of each work session — replace the relevant sections, don't append. Keep it under ~150 lines: move finished-work narratives to [docs/HISTORY.md](docs/HISTORY.md), durable reasoning to [docs/DECISIONS.md](docs/DECISIONS.md). Project rules live in [CLAUDE.md](CLAUDE.md).

## Pending author actions

Things only the author does (pushes, deploys, migrations), queued and waiting.

**⚠ PRIVACY — teacher `display_name` holds an EMAIL, and the pre-auth screen is anonymous.** Found live 2026-07-31 building the viewer route: `get_activity_public_meta` returned `teacher_name = "zanreed@gmail.com"`, because `handle_new_auth_user` stores whatever Google supplied. Ruling 3.2A's "teacher display name" meant *"Mrs. Jafari"* — publishing an email address to an **unauthenticated** page that anyone with the link can open is a different thing. **Mitigated client-side already** (the route suppresses an email-shaped name and shows a neutral attribution; title still renders), so nothing leaks today — but the DATA is still wrong and any other consumer of that RPC would print it. **Server-side fix to decide:** either backfill/normalize `users.display_name` to a real name, or have `get_activity_public_meta` derive a display form (e.g. first name, or "your teacher") and never return the raw column. Worth pairing with a teacher-facing "how your name appears to students" control.

**✅ COMPLETE 2026-07-31 — direct-to-Storage image upload: applied, deployed, function deleted, VERIFIED END TO END AT BOTH LAYERS. Nothing queued.** Eng-reviewed same day (0 unresolved; DECISIONS.md → "Direct-to-Storage image upload").

**Migration.** `db push` applied 0019 after a one-line history repair — the phantom `20260729131811` row (the 0018 MCP-apply duplicate, exactly as the 0018 wrinkle note predicted) was `migration repair --status reverted`'d. Remote history is a clean 0001–0019.

**Layer 1 — predicate (SQL, 8/8).** 0019's 5 footer checks (bucket public/10MB/5 types; EXACTLY one policy = the INSERT gate; predicate calls the helper + is CASE-guarded; RLS on; `can_edit_activity` grants untouched) plus the impersonated matrix: owner ALLOW; non-owner/anon DENY 42501; **non-uuid key = clean 42501, the cast never ran**; root-level + nested keys DENY; soft-deleted activity DENY; overwrite sees 0 rows. Zero probe residue; ACL completeness still exactly the documented anon exception; **advisor 18 lints — unchanged from the post-0018 baseline, so 0019 added none.**

**Layer 2 — live API (browser console, 8/8).** `scripts/verify-image-storage-e2e.js`, run signed-in against the deployed app. **T1 owner upload HTTP 200 — storage-js forwards the session JWT** (the assumption class that failed once on `functions.invoke` is now proven, not assumed). T2 tokenless public read 200 `image/png`. T3 duplicate → `409 KeyAlreadyExists`. **T4 upsert/overwrite → `403` RLS — the no-UPDATE-policy design confirmed at the API layer.** T5 `text/plain` → `415 invalid_mime_type` and T6 11 MB → `413 EntityTooLarge` (**the bucket limits are real enforcement, fired on bodies that bypassed the client guard entirely**). T7 non-owned activity → `403`. T8 malformed key → `403`, **not a 5xx** — the D1 CASE pin holds above the DB too.

**Deploy integrity verified:** live bundle `index-BCenC_5H.js` (hash-identical to a local build) carries `activity-images` and zero `upload-image`; function list is down to `publish-activity` / `ingest-submission` / `get-feedback` / `get-activity`; app boots clean (sign-in gate, 0 console errors, 0 failed requests). **Operational note for next time:** the function was deleted before the Pages build finished, so uploads were briefly broken (old bundle → deleted function). Harmless here, but the ordering exists for a reason — wait for the deploy, then delete.

**`cacheControl` RESOLVED 2026-07-31 — our code is correct; the free tier does not pass it through.** A real editor upload (156 KB webp) stored `metadata.cacheControl = "max-age=31536000"`, proving `uploadImage.ts` sends it and supabase-js's multipart branch honors it (the Blob-vs-bytes worry from the Edge-Function era was unfounded — no code change needed). **But the public endpoint serves `cache-control: no-cache`** (`sb-gateway-mode: direct`, `cf-cache-status: REVALIDATED`): Supabase's Smart CDN, which is what passes the stored value to browsers, is **Pro-plan-and-above**. Nothing in our code can change the served header — the stored value is already right. **Measured impact is small and was verified, not assumed:** the response carries a strong `etag`, and a conditional request returns **`304` with 0 bytes**, so a revisit costs one round-trip per image, never a re-download. Accepted as-is; it becomes a free win if the project ever moves to Pro. Also worth knowing the R2 era did NOT have this limitation (raw PUT set `public, max-age=31536000, immutable` directly), so this is a real if minor regression from the migration — logged here so a future perf pass doesn't rediscover it as a mystery.

**No residue.** The E2E probe object was deleted via the dashboard 2026-07-31 and verified gone (`NoSuchKey`) with the real editor upload intact (200, 156 KB, `image/webp`); the bucket holds exactly that one object. **Operational note:** deleting a bucket object requires the dashboard or service role — there is no DELETE policy, by design (it's the same absence that makes overwrites impossible), and a raw SQL delete of the `storage.objects` row is the WRONG tool: it hides the file from the dashboard without reclaiming the backing blob, foreclosing a proper cleanup. Until the orphan-GC lands (TODOS.md), dashboard deletion is the only complete path.

**~~QUEUED~~ → STRUCK 2026-07-31, provably moot — graph-kit upload + publish-activity redeploy. Do NOT run these.** Verified in code, not assumed: the viewer already loads the kit as an **app-bundled lazy Vite chunk** — `await import('@activity/graph-kit')` at four sites in `packages/viewer/src/blocks/kitSurfaces.ts` — and the editor imports it statically (`ActivityConfigDrawer.tsx`). Both build from the workspace package, so they already carry ungraded mode. Ungraded mode exists **only** for the server-authoritative viewer; published pages always supply an `answerKey` and are unaffected by an additive kit change, and they stay pinned to their existing hash regardless. So no consumer anywhere needs the new kit ON R2. (`graph-kit-manifest.ts` was committed 2026-07-31 in `6b1f37f`, kit `ZM7PARJ5` → `R5LUPQJS`, so a clean-checkout deploy is unambiguous either way.)

**The R2 kit now has exactly ONE consumer left: the published-page path** (`renderer/src/document.ts` + `render.ts` inject the hashed URL, `publish-activity` reads the manifest, `scripts/build-graph-kit.mjs` + `deploy-train.mjs` maintain it). It therefore dies with publishing at **S9** — and cannot be removed earlier without deleting the only student path that can currently be graded (S4's grading RPC does not exist yet; the viewer's check service still runs against a mock). Do not "finish the Cloudflare exit" by ripping the kit out early.

```bash
pnpm upload:graph-kit      # then COMMIT the regenerated _shared/graph-kit-manifest.ts
pnpm deploy:publish        # only after the upload prints its Uploaded: lines
```

`packages/graph-kit` gained ungraded input mode (author ruling A, 2026-07-31) — `answerKey` is now optional so the server-authoritative viewer can mount a graph question as a pure input surface. **Nothing is broken meanwhile:** the change is additive, graded behavior is unchanged (half the new tests exist to prove that), already-published pages keep pointing at their existing kit hash, and no shipped code uses ungraded mode yet. The upload matters when the viewer's graph components land. Order is the standing CLAUDE.md rule — **kit upload FIRST, then the function deploy** (deploying first points at a hash that isn't on R2 yet and 404s the summon button), and the regenerated manifest must be committed or a future deploy from a clean checkout silently reverts to the stale hash.

**✅ QUEUE EMPTY — VERIFIED AGAINST THE LIVE PROJECT 2026-08-02.** The two blocks that stood here (`supabase db push` for `0020_section_checks`, and `pnpm deploy:get-activity`) were **stale, and a stale queue is worse than an empty one** — re-running an applied migration is exactly the kind of thing this file exists to prevent. Checked, not assumed: `list_migrations` ends at **0020 `section_checks`**, and `list_edge_functions` reports **get-activity v6** (the version the queued redeploy was for), alongside `check-activity` v3, `publish-activity` v122, `ingest-submission` v42, `get-feedback` v4.

**Deploy flags re-verified in the same call** (the standing CLAUDE.md rule): `ingest-submission`, `get-feedback`, `get-activity` all `verify_jwt:false`; `check-activity` and `publish-activity` `verify_jwt:true`. All five ACTIVE. Prior narratives for these deploys are archived in [HISTORY.md](docs/HISTORY.md).

_Queue cleared:_ all deploy trains through **2026-07-27** are author-run + done ([HISTORY.md](docs/HISTORY.md)). The 07-27 train was a single `publish-activity` redeploy carrying both the reference-panel `graph_figure` block (07-26) and block-level definitions + the print glossary appendix (07-27); `main` pushed and in sync at `b91185e`. **Live state (verified 2026-07-27 via `list_edge_functions`):** `publish-activity` **v120** (`verify_jwt:true`), `ingest-submission` **v41** wire **v9** (`verify_jwt:false`), `get-feedback` **v4** (`verify_jwt:false`), `upload-image` v3, storage **v12**, kit `graph-kit-XB5CUURV.js` (source of truth `supabase/functions/_shared/graph-kit-manifest.ts`). Standing reminder: any `ingest-submission` / `get-feedback` redeploy needs `--no-verify-jwt` ([CLAUDE.md](CLAUDE.md)).

## Next major arc — STRICT-GRID: COMPLETE (T1–T8) + known-gaps TRIAGED 2026-07-21 (nothing queued)

**Strict-grid editor migration ([docs/design/strict-grid-editor.md](docs/design/strict-grid-editor.md)).** The editor's ProseMirror tree now IS the stored rows-of-columns model (doc `(row|sectionBreak)+`, every block inside a `column`); serialize's collapse/unwrap is deleted. **App-only — NO renderer/publish/wire/storage change** (stored shape unchanged; bundle regen = zero drift). Killed the editor-vs-storage tech debt.

**SLICES 1 + 2 + known-gaps triage — DONE + on `main` (not pushed).** Slice 1 (T1–T6 structural, commits `3d91692`→`bcffebb`): `editor/extensions/Doc.ts` (strict top node, row-first fill), `StrictGridNormalize.ts` (empty-state/trailing-para/re-coalesce, undo-safe), `editor/strictGrid.ts` helpers, `lib/serializeTestBridge.ts`; StarterKit TrailingNode disabled; 1-col rows render FLAT (grip only on multi-col). Slice 2 (T7 seam affordances + T8 import): `RowSeamCaret.ts` keymap (boundary Backspace selects the seam, Arrow crosses it — Playwright-verified), `Columns.ts` `dissolveRow` + `addRowBelow`, toolbar column cluster gated to multi-col + Merge/Row-below escape hatches, and the ` ```columns ``` ` Markdown fence (`parseColumnsFence`, columns split by `---`, 2–6 cols → strict `row`). Known gaps TRIAGED 2026-07-21 (`/plan-eng-review` + `/plan-design-review`, both CLEARED): **B1** grip click-menu BUILT (`gridRowMenu.tsx`/`GridRowMenuHost.tsx`, reuses `dissolveRow`/`addRowBelow`); **B2** build-nothing (dragHandleNested deliberate; whole-stack/cross-row reorder paradigm-deferred); **B3** `caretBlockPos` folded onto `blockAncestor`; **B4** no action. Full record in the design doc "Known gaps" + [HISTORY.md](docs/HISTORY.md). Suite: **145 e2e + 721 app unit** green.

## Current focus

**⏭ NEXT SESSION STARTS HERE. S5.5 (teacher-print migration) is ENG-REVIEWED + RULED 2026-08-03 (23 decisions, 0 unresolved) — BUILD-READY.** Full rulings + task list (T1–T11): gstack design doc → "S5.5 eng review" section. Headlines: answer channel = real sanitizer client-side + AnswerKey context only the teacher route mounts (D3A); in-page print on the certified S5 path, worksheet iframe deleted (D4A); **base print ALWAYS shuffles ordering + matching via a NEW registry `printShuffled` field OUTSIDE the sanitize spec** — the outside voice caught that the ruled "skip shuffles" default would have printed answer keys (D15A, its strongest finding); randomized print VERSIONS feature added by author (arrangement-only; parameterization → TODOS), resequenced as a post-eviction coda so eviction is never hostage (D16B); foldable re-points via offscreen render → capture → existing measure iframe, fontsource fonts (spike first), bounded waits (D6A/D7A/D11A); cross-surface ANSWER-KEY parity gate must run green BEFORE the eviction commit — impossible after (D12A, semantic assertions never positions); **S5's two pending legs (Linux baselines — regenerate POST-S5.5-DOM per D19B — and contact-sheet read) gate the EVICTION task, not the build** (D9A); eslint no-restricted-imports lands with eviction (D10A); font registry → @activity/schema (D18A); two in-slice bug fixes ruled: savePrint stale-overwrite (D20A) and student matching-dropdown authored-order (D21C). Exit criterion: zero app/viewer imports of @activity/renderer (package survives for publishing until S9). TODOS gained 3 entries (parameterization, batch print, editor upgrade-seam note).

**Prior current-focus entry (S4, still accurate):** S4 (grading) is DONE — migration applied, function deployed, E2E 13/13 live, finding R2 closed. Nothing in S4 is outstanding. Details in the S4 block below; the short version is that the student check path now works end to end against real infrastructure.

**The remaining lanes, in the arc's own order:**
- **S5 print mode + per-block print-parity snapshots** (lane B, needs S3 — which is done). **ENG-REVIEWED + RULED 2026-08-01 (17 decisions, 0 unresolved) — BUILD-READY; R4 is CLOSED** (the artifact is now defined; see the "S5 eng review" section of the gstack design doc + the Last-updated entry below). Headline rulings: S5 = STUDENT print + the gate, **including the document-level print layer** (print header, reference print box, glossary appendix, meta.print vars, @page size — student surface, per the outside voice's strongest finding); **S5.5 = committed follow-on** migrating teacher print (ActivityPrint route, answer-key variant, foldable) onto the viewer tree, gated on S5 green — the renderer fully deletes only after S5.5. The parity artifact is **rules-parity, not cross-surface pixel diff**: `printExpectations(type)` derived from the registry PrintSpec asserted on BOTH surfaces under Playwright print emulation, viewer-only screenshot baselines (linux-CI authoritative), a `page.pdf()` pagination pass (print emulation never paginates), and ONE recorded human contact-sheet sign-off. Fixture rosters (per-block + structural + document) are guard-enforced so nothing can skip the gate; the harness runs network-blocked with local KaTeX assets so it survives R2's death.
- **✅ S6 local-first — BUILT AND BROWSER-VERIFIED 2026-08-02 (V1–V10 complete; nothing to deploy, no author action).** Student work now survives a dead network, a closed lid, a shared Chromebook, and a teacher republishing mid-activity. Build narrative archived in [HISTORY.md](docs/HISTORY.md); durable reasoning in [DECISIONS.md](docs/DECISIONS.md) (six S6 entries); rulings in the gstack design doc's "S6 eng review". **What shipped:** local-first buffer (debounce + flush-on-hide, quota degradation, boot sweep, orphan GC with the unsent-work exception) · queued checks that fire on reconnect/visibility/boot and grade CURRENT answers with a drift notice · single PERSISTED idempotency key (S3's no-persist rule overturned in place) · one editable tab via Web Locks with an `ifAvailable` probe and a `disabled` fieldset · sign-out hardened against the verified auth-js offline hole, plus idle escalation · offline boot, republish-pinned boot, inline re-auth, banner dedup · a service worker precaching exactly one entry and never caching the API · three e2e lanes (editor / signed-in student / preview-build worker). **Suite: 3437 unit + 227 e2e, 2 parked.**
  - **⚠ ONE PROMISE UNPROVEN, deliberately visible:** offline reopen against the built worker is `test.fixme` in `e2e/sw/service-worker.e2e.ts` with the full evidence inline and in [TODOS.md](TODOS.md). Online the worker demonstrably serves both navigation and subresources; under emulated offline, parse-time subresources fail while a page-script `fetch()` for the same URL succeeds. **Should close before S9 cutover**, which is when students meet that path.
  - **Known limitation (stated, not hidden):** offline boot needs a token that has not expired. Expired token + no network ⇒ the pre-auth gate, because refreshing needs the network and reading supabase's session storage directly in shipped code is a dependency worth refusing.

**⏸ PARKED, with the author's reason (2026-08-01): the teacher-grading slice.** Free text is captured by every check today but nothing can grade it yet, so "Recorded ✓ — your teacher will review" is a promise without a due date. **The author's call: not pressing — there are no teachers using the system right now.** It becomes urgent the moment a real teacher does. Full context, including the attempts-vs-latest-response question it exists to answer, is in [TODOS.md](TODOS.md) → "Teacher grading bound to `section_checks`".


**NEW ACTIVE ARC — components-as-data re-architecture (RULED 2026-07-28; eng + design reviews CLEARED, 0 unresolved).** The author ruled a full re-architecture of the student path: live-API viewer SPA + **student accounts** (district Google SSO, 13+ via teacher class assertion) + React component per block (single block registry) + server-authoritative batched grading (answer-key sanitizer, answers never reach clients) + upgrade-on-read + hard cutover with R2 redirect afterlife. Renderer + inline runtime retire only behind two parity gates (golden grading corpus; per-block print snapshots); old pages' anonymous wire regression-pinned forever. **Sequencing (author's explicit call): rewrite starts NOW; the August Algebra I release is deliberately delayed** ("good architecture is worth the wait") — Drop 0 hosting work stands, Drop 1′ slips behind the rewrite. All rulings, wireframe, tasks, and test plan live OUTSIDE the repo: `~/.gstack/projects/ZanReed-activity-platform/user-main-design-20260728-components-as-data.md` (+ `tasks-eng-review-20260728-015250.jsonl`, `tasks-design-review-*.jsonl`, wireframe in `designs/student-viewer-20260728/`). Build order: S0 (registry contract + design-token extraction) first; the checked-state family spec gates component work. **S0 LANDED 2026-07-28** — new `packages/viewer` (`@activity/viewer`): the single block registry (22 entries, guard-enforced vs the schema union + block-predicates), the design-token layer (`tokens.css` + `DESIGN.md`, light/dark/print, AA-guarded), and the 7.2A family spec ([docs/design/checked-state-families.md](docs/design/checked-state-families.md)). Lanes A (S2→S4 server) + B (S3 viewer) + C (S1 identity) can now start in parallel; registry changes go through `packages/viewer/src/registry/` only, never forked per-lane. **S1 (lane C) LANDED 2026-07-28** (narrative in [HISTORY.md](docs/HISTORY.md)). **S2 (lane A, read API) LANDED 2026-07-28** — see Last updated; migration 0017 + first `get-activity` deploy queued above. Lane A continues with S4 (grading RPC + parity corpus, depends on S2 — now unblocked once 0017 is live); lane B (S3 viewer) unblocked since S0. **SCOPE AMENDMENT (author, 2026-07-28): "there are no old pages to maintain"** — the live R2 pages are the author's own tests, so TV2's redirect afterlife is dead scope, T1A's anonymous-wire-forever pin drops (remaining parity gates are correctness gates: golden grading corpus + print snapshots), and S9 cutover DELETES the anonymous identity/wire machinery instead of preserving it. Full reasoning in DECISIONS.md → "Student identity S1".

**RULED 2026-07-31 — Cloudflare exit: Supabase-only backend, Pages stays (for now) as the static host.** The author ruled the refactor's hosting posture: everything moves off Cloudflare **except** static hosting of the SPA — Supabase cannot serve HTML (the `text/html`→`text/plain` anti-abuse rewrite, CLAUDE.md), so the app keeps living on Cloudflare Pages for now; the host is deliberately swappable (vanilla Vite static build, no Workers/KV/`_redirects` — moving is a build-config + Auth-URL + CORS change, nothing more; don't adopt Pages-specific features without a ruling). Concretely: (1) **the viewer's graph blocks use the app-bundled graph-kit as a lazy Vite chunk — NEVER the R2 summon path.** Lane B must not reproduce the fetch-from-R2 pattern; the hash-manifest machinery (`upload:graph-kit`, `graph-kit-manifest.ts`, `.env.r2`, the upload-first-then-deploy ordering rule) dies at S9. (2) **Fonts ship in the app bundle via the pinned `@fontsource` packages** (exactly what the editor already does); the R2 WOFF2 hosting + `build:fonts`/`build:mathlive-fonts` die with published pages. (3) **Image uploads go direct-to-Storage — BUILT + ENG-REVIEWED 2026-07-31, the `upload-image` function is DELETED** (author ruling superseding the same-day service-role retarget; full record in DECISIONS.md → "Direct-to-Storage image upload"). Migration `0019_image_storage.sql` carries the bucket AND the RLS INSERT policy that is now the gate (CASE-guarded activity-id parse → `can_edit_activity` as the caller — the helper rule held); `uploadImage.ts` writes the bucket directly (statusCode-keyed error mapping, 9 unit tests); `scripts/verify-image-storage.sql` is the behavioral proof. Apply + deploy + function deletion queued above. Existing image `src`es stay absolute R2 URLs on test activities only; they keep resolving until the bucket dies at S9, no rewrite needed. (4) **S9 cutover cleanup:** delete publish-activity's R2 upload path, the R2 secrets on the functions, `VITE_PUBLISHED_URL_BASE`, the R2 origin in `ALLOWED_ORIGINS`, then the bucket itself. The "custom domain for R2" backlog item is dead scope.

**PRIOR (superseded timing, work stands): Activity Bank arc.** Drop 0 hosting prep remains valid and deployed pieces stay live; the August scope moved behind the rewrite per the ruling above.

**JUST SHIPPED (2026-07-27) — block-level definitions**, all 5 slices on `main`, **pushed + deployed** (publish-activity v120). See the status-table row + [docs/design/definition-rich-content.md](docs/design/definition-rich-content.md) (§9 records where the build corrected the design). Notable side-effect: `packages/schema/src/graph-primitives.ts` is a NEW leaf module — `AxisConfig`/`Drawable`/`FunctionModel` moved out of `blocks/interactive-graph.ts` (which re-exports them all, so every existing import path is unchanged) because reaching them through a file that imports `inline.ts` would have closed a fatal module cycle. Behaviour-preserving; good hygiene independent of definitions.

**ACTIVE ARC — Activity Bank (Drop 0 hosting first).** Design + all rulings: `~/.gstack/projects/ZanReed-activity-platform/user-main-design-20260724-010349.md` (eng-cleared; D8 Cloudflare Pages, D15 hosting standalone-first). August scope = Drop 0 (hosting) + Drop 1′ (scoped catalog); branch cut to fall, co-ownership = fall arc. **Drop 0 prep DONE this session** (build/SPA-fallback/CORS verified, runbook written) — see Pending author actions + [docs/drop0-hosting-runbook.md](docs/drop0-hosting-runbook.md); awaiting author dashboard steps. Tasks T2–T11 (migrations/runtime/app/Bank UI) are the subsequent drops, not started.

**PRIOR: strict-grid (above).** Prior active arc: **editor-refinement-pass** ([docs/design/editor-refinement-pass.md](docs/design/editor-refinement-pass.md) — cross-block feedback list, rulings, 4-group sequencing) building on the slice-6 Notion-hybrid arc ([notion-hybrid-editor.md](docs/design/notion-hybrid-editor.md) — stages 0–7 shipped). App/editor-only. All committed on `main`, **NOT pushed** (author pushes). Harness: **145 e2e + 859 app unit** green (`pnpm --filter @activity/app test:e2e`); lint 0 errors, 12 accepted warnings (`react-refresh` + `react-hooks/exhaustive-deps`).

**Open remainders (deferred), roughly in priority order:**
1. **Focus mode** (slice-6 stage 7 split it out) — the dim-the-rest toggle needs a caret-tracking ProseMirror plugin (CSS can't identify the caret block under one contenteditable — the same wall that deferred stage-1 gutter focus-parity); off-by-default, wants its own design+eng pass.
2. **Input-parity / a11y touch pass** — deferred from slice-6 stage 1 (touch needs a real device; `/` covers the keyboard floor).
3. **Slice 6.5 smart-defaults** — net-new unvalidated heuristics; own spike, gates nothing.
4. **⌘⇧↑/↓ keyboard-reorder settle** — snap-motion follow-up (debounce design).
5. **Chip open:** the slash menu dies under synthetic keyboard input once a query char follows `/` (clean-tree repro; humans unaffected — e2e routes via the Add-a-block modal). **Papercut:** the gutter "+" (BlockAddButtonHost) can overlap the drag grip's lower half on a short block.

**Verification quirk:** the in-app Browser pane suppresses the position-measured hosts (command bar / quick-bar / drawer) under JS-driven selection — Playwright e2e (real chromium) is authoritative; drive the drawer via node-selection + gear + Advanced. `/playground` (unauthed) is the dev target; `/playground?empty=1` mounts a blank doc.

Completed session narratives (graph-authoring redirection, Groups 1–4, slice-6 stages 0–7, dark mode, image crop, graph systems, math blanks, the deploy trains) are archived in [HISTORY.md](docs/HISTORY.md) (2026-07-15 → 07-21 section).

---

_Backlog / candidate arcs (columns slices 1–5, strict-grid, and slice-6 stages 0–7 are all DONE; completed narratives in [HISTORY.md](docs/HISTORY.md); durable reasoning in [DECISIONS.md](docs/DECISIONS.md); current per-feature status is the table below):_

- **▶ SCHEDULED — runtime budget-ladder: per-question-type inlining variants (do BEFORE the next question-type generation).** The base runtime is **41.8 KiB, over its 40 KiB soft target** (the math-blank runtime pushed it over); the 2026-07-10 amendment ([DECISIONS.md](docs/DECISIONS.md) → "Runtime size budget amendment") says pull a structural lever when a variant nears the ceiling, "scheduled, not discovered." This is that lever. **Mechanism already exists:** [document.ts](packages/renderer/src/document.ts) picks the runtime by scanning the rendered `body` for marker attributes — a base-vs-graphs ternary plus four marker-keyed sidecars (reference-panel / definitions / calculator / feedback), each inlined only when its marker is present. **Approach:** carve the less-common question machinery out of the always-on base into the same conditionally-inlined-chunk pattern, keyed by block-marker attributes, leaving a minimal always-on core (text/numeric blanks + MC + checkpoints/persistence/submission). Split candidates, heaviest/least-common first: free-text (`self_explanation`/`short_answer`/`essay`, `.free-text-input`), `matching`, `ordering`, and the math-blank strategy glue. **Constraints that make this non-trivial (eng-review at pickup):** the runtime invariants must survive the split — `init.ts` is the ONLY DOM walker, `render()` is the ONLY DOM mutator, scoring/strategies register once; the split chunks stay vanilla-TS concatenated like the sidecars (NO `@activity/schema` import, no JS deps); `scripts/bundle-renderer.mjs` grows a chunk-emit step; re-measure every variant against the ladder (target/ceiling) after. **Not blocking today** (no new question-type is queued), but it must land before one does. Wants `/plan-eng-review` (variant taxonomy + walker/registration seams) before build.
- **Re-architecture follow-ons (accepted 2026-07-28, from the eng+design reviews):** (1) **Clever/ClassLink district SSO** — demand-triggered: pick up when a district, not a teacher, is the buyer. (2) **Realtime push arc** — the viewer's store seam is built for it; trigger = first named live feature. (3) **Sampled behavioral telemetry** — only after the census can't answer a concrete question AND the compliance pack is amended. (4) **Print variants** (print-with-my-work; teacher answer-key) — after T8 print parity is green. (5) **Solution-unlock pedagogy pass** (stricter gating, per-teacher setting) — deliberate later decision, ideally informed by census data on check patterns. (6) **/design-consultation brand pass** — when the product's identity question goes live (wider-than-department adoption).
- **Free activity catalog / "Activity Bank"** (Phase 2 cold-start lever; browse + run/assign/print; [free-activity-catalog.md](docs/design/free-activity-catalog.md)). Design-reconciliation DONE + 6/7 decisions author-ruled 2026-07-13 (`visibility='public'`=listed, definer-RPC catalog surface, signed-in audience v1, published-version-only print, own route; multi-author-ready per ruling #1). **One open item: #6 taxonomy/tags — author wants a tags discussion at kickoff** (lean `tags text[]` + GIN). Design-only until pickup.
- **Vocabulary glossary — Phase 4** (tenant-scoped `glossary_entry` store + `glossaryKey` resolution at publish; light implicit-reuse + suggest-don't-apply auto-suggest; both additive to the shipped mark; [vocabulary-definitions.md](docs/design/vocabulary-definitions.md)).
- **Long-term OCR/AI (design captured 2026-06-16, not scheduled):** PDF → activity import ([pdf-import.md](docs/design/pdf-import.md)) + photo-upload answer checking ([photo-grading.md](docs/design/photo-grading.md)). Decisions locked (generic-master QR, teacher-grading-aid v1, transcription-only). Photo-grading needs one refactor: server-shareable answer evaluation.
- ~~**Phase 1 polish:** custom domain for R2.~~ Dead scope per the Cloudflare-exit ruling (2026-07-31) — R2 retires at S9.
- **Other Phase 2 "decide at phase start":** image-hosting quota, `skills` editing UI (block-type priority is COMPLETE — all four pedagogical blocks shipped).

## Status by area

| Area | Status |
|---|---|
| Stages 9–16 (schema, renderer, runtime, editor, publish flow, submissions dashboard) | ✅ Complete; live-verified |
| Database migrations 0001–0006 | ✅ Applied; RLS verified |
| Migration 0007 (submission version pinning) | ✅ Applied (verified 2026-06-12) |
| Migration 0008 (`soft_delete_activity` RPC — delete bug fix) | ✅ Applied 2026-06-17; delete verified working by author |
| Print feature Drops A–D (worksheet config + journal foldable) | ✅ Complete; entirely client-side, no deploy needed |
| Structural columns + grid lines + width presets + drag-between-cells | ✅ Complete; deployed |
| Variable block sizing (Drops 1/3/4: width+align, image height/crop, cell work-space) | ✅ Complete + tests; deployed (publish-activity v34); Drop 2 column-drag cancelled by author (presets remain) |
| Editor UX: sticky toolbar + popover scroll-jump fix | ✅ Browser-verified |
| Phase 2 slice — rich text + inline math in hint/feedback/solution | ✅ Deployed; live UX unverified |
| Image authoring (block + popover + upload + live preview) | ✅ Deployed; R2 cross-origin `<img>` confirmed 2026-06-16 |
| `publish-activity` / `ingest-submission` Edge Functions | ✅ Deployed; ingest runs with `verify_jwt: false` |
| Cloudflare R2 hosting (published HTML) | ✅ Live |
| Auth (Google OAuth, allowlist) / React app / editor stack | ✅ In place |
| CI (GitHub Actions: typecheck/lint/test/build + bundle-staleness guard) | ✅ Added 2026-06-13; verified green locally |
| Markdown paste import | ✅ Complete + verified end-to-end by author; app-only. Format spec + Copy AI prompt + drift guard shipped. **Blank hints (`?…`) + per-answer feedback (`!wrong :: msg`) + math-expression blanks (`{{==expr}}`) now importable** (2026-07-21, `ffe7d5f`) — closed the importer's fill-in-blank feedback asymmetry vs `mc`/`graph`; app-only (rides the SPA deploy) |
| End-to-end manual test | ✅ Free-mode (2026-06-16) + locked-mode/dashboard (2026-06-17) both author-verified end to end |
| Order-independent blank groups | ✅ Complete + deployed; grouped scoring + `{{~}}` import verified live on a published page |
| Inline vocabulary definitions — **block-level content** (2026-07-27) | ✅ All 5 slices built + browser-verified ([definition-rich-content.md](docs/design/definition-rich-content.md)). A definition now carries a `DefinitionBlock[]` — paragraphs, headings, lists, display math, images, static graph figures — the reference panel's content alphabet minus columns/callout (author rulings D2/D3). Authoring: popover for a simple definition, a new modal dialog for a rich one, hard-gated as a data-loss guard. Print: an optional end-of-worksheet glossary appendix (`meta.print.printDefinitionGlossary`, off by default). Import: a ` ```definitions ` fence + `[[term]]` references, sharing the reference fence's line grammar. Runtime cost ZERO (the sidecar clones an opaque `<template>`; still 1.9 KiB). **DEPLOYED 2026-07-27** (publish-activity v120) — reaches a page on its next re-publish |
| Calculator tool (Phase 2.7) | ✅ Stages 1–4 + full graphing UX overhaul, all browser-verified ([HISTORY 2026-07-06](docs/HISTORY.md)). Live since the 2026-07-08 full deploy pass |
| Activity-wide typography (`meta.typography`; R2-hosted WOFF2; drawer control; WYSIWYG) | ✅ Deployed 2026-07-08; live-page font check cleared 2026-07-10 |
| Number-line block (`number_line`, 1-D graded) | ✅ All 6 slices + shape-pills fix, DEPLOYED 2026-07-12 (kit `CWAMAF2L` era; ingest v34 wire 3–7; publish v85+). Browser-verified: dev harness, bundle-rendered page, `/playground`. Live-page eyeball optional |
| Data-plot block (`data_plot`, statistics charts) | ✅ Slice 1 (`display` + `build_dotplot`, wire v8 + storage v9) AND graded builds (`build_histogram` + `build_boxplot`, additive) DEPLOYED 2026-07-12 (kit `V6T6HAUS`; ingest v36 wire 3–8; publish v88). Browser-verified: `/dev/data-plot`, bundle-rendered pages ("1/1", "2/2 correct"), `/playground`. Live-page eyeball optional |
| Interactive-graph block — Stage 5 | ✅ Four interactions (plot-a-point N-handle, plot-a-function incl. bounded curves/rays/segments, graph-an-inequality, shade-region) + static display. DEPLOYED + live-verified (2026-07-10 author pass). The "answer surface as a graph-block field" seam RETIRED (author call 2026-07-10) — composes as display graph + sibling question block instead |
| Numeric blanks (`answerType: 'numeric'` + tolerance; `{{=…}}` import) | ✅ Live (cleared 2026-07-10); no wire change |
| Multiple-choice block (wire v5 + ```mc import + dashboard) | ✅ Live (cleared 2026-07-10); browser-verified |
| Static graph-paper SVG fallback (author's print fix) | ✅ Live (cleared 2026-07-10); committed in `c334b96` |
| Graded function families — quad/exp/log + quadratic inequalities | ✅ Live (cleared 2026-07-10); browser-verified; `38b9fbe` |
| Display-graph continuation arrows + readDrawables/print-arrowhead fixes | ✅ Live (cleared 2026-07-10); `131f62d`; arrows checkbox FULLY verified 2026-07-12; sin-curve `expression` tangent fix deployed 2026-07-12 (`9a68c0e`) |
| MC choice figures (per-choice image/graph, 2-up grid, shared DrawableListEditor) | ✅ Live (author-confirmed 2026-07-10) |
| Matching + ordering question types (wire v6 + storage v7 + ```match/```order import) | ✅ Deployed (author-confirmed 2026-07-10); pointer-DRAG on a real device confirmed 2026-07-12 |
| Calculator parity Tier A (calc inequalities, per-row domains, widened function table, log regression) | ✅ Deployed + author-confirmed 2026-07-11. Tier B (Compute Engine) DEFERRED on pedagogical grounds. Movable points + Σ/Π dropped by author call |
| Migration 0009 (security/performance housekeeping) | ✅ Applied + fully verified 2026-07-11 (8-step MCP verification passed; advisor residue is intentional — see DECISIONS) |
| ```numberline``` + ```dataplot``` markdown import fences | ✅ Committed 2026-07-12, app-only; reach teachers via the SPA deploy (main pushed) |
| Pedagogical blocks — `learning_objectives`, `worked_example`, `faded_worked_example`, `self_explanation` | ✅ DEPLOYED 2026-07-12 (train author-run: ingest v9 then publish). Browser-verified on `/playground`. First three are content/scaffold (no wire bump); self-explanation is ungraded free text (wire v8→v9 `freeResponses`, storage v9→v10, new runtime module + RUNTIME.md contract + dashboard). **Markdown import shipped**: ` ```objectives / ```worked / ```faded / ```explain ` fences (app-only, rides SPA deploy). Nice-to-have author eyeballs: `{{}}`-in-faded-step keystroke, live self-explanation submit→dashboard round-trip |
| Callout block — editor authoring (info/warning/success/note) | ✅ Editor node + NodeView (inline variant switcher) + serialize both ways + slash entry + picker thumbnail; `/playground`-verified 2026-07-21 (`b4558b4`). Activated a schema+renderer ORPHAN (was serialize→null alongside `problem`). App-only, no wire/runtime impact. Markdown import for callout deliberately deferred to the import-format registry work |
| Reference-sheet import (` ```reference ` fence → reference panel) + `graph_figure` block | ✅ Built + browser-verified 2026-07-26. The fence routes content to the panel (side channel — never body blocks; append semantics, first title fills an untitled panel); back-to-back `graph:` lines share one grid; optional `axes:` window. New `graph_figure` schema block (ChoiceGraph promoted; kit-free SVG; never interactive) renderable everywhere, AUTHORABLE only in the panel editor (insert item is `referenceOnly`; main editor registers the node for representability). **DEPLOYED 2026-07-27** (publish-activity v120) — reaches a page on its next re-publish |
| Phase 2.6 manual grading (short_answer + essay + rubrics + grading) | ✅ Code complete (slices 1–5) + **DEPLOYED + live-verified** 2026-07-13. Migrations 0010+0011 applied; `get-feedback` (`verify_jwt:false`) + `publish-activity` deployed; anonymous get-feedback gate confirmed open, advisor clean. In-document rubrics; `grades` table + RLS + direct upsert; teacher grading UI (side-by-side + Needs-grading filter); student feedback via `get-feedback` + a conditional sidecar (base runtime 39.2 KiB). **Markdown import: ` ```shortanswer ` + ` ```essay ` fences added 2026-07-13** (rubric pipe grammar + essay `words:` range; app-only, `/playground`-verified). Only a human app-level round-trip smoke test remains |

Suite as of 2026-08-01 (post-S4 T1–T9 — a dated snapshot; CURRENT counts live in "Last updated" at the foot of this file): schema 340 / graph-kit 384 / **renderer 724** / **viewer 714** / app 993 unit + 145 e2e; typecheck + lint green (0 errors; pre-existing warnings only). S2 added +51: 7 schema upgrade-seam tests and 44 viewer sanitize/shuffle tests (wire-level sentinel leak scan over every block type + variant, strip-path validity, schema-vs-registry suspicious-key completeness, serve-shuffle determinism), plus a tsc-enforced type-projection guard (`sanitized-types.test-d.ts`, checked via the viewer typecheck script — vitest's typecheck mode was red-green-tested and found to swallow errors, so tsc is the enforcement). The reference-sheet fence + graph_figure block added 31 tests (3 renderer content-block cases; 13 importer fence cases incl. graph-run merging, axes scoping, expression rejection, literal-`{{}}`, two-fence append; 3 serialize panel-bridge round-trips; the registry `reference` entry rides the existing guard, which grew a `panel` probe branch; +2 guard-map entries in blockTypeGuards). In-equation math gaps `\gap{answer}` (Model A) importable (`resolveMathGaps` → `\placeholder[id]{}` + a MathPrompt; serialize empties the gap so the answer never leaks; graded by value-equivalence) added 8 app tests (faded step, inline, balanced braces, no-leak schema round-trip; 1 prompt CLAIM). Static graph figures on mc choices (`graph: line y=2x`) + matching sides (`graph: … -> option`) added 8 app tests (shared `parseShowDrawable` extracted from the graph fence; ChoiceGraph, default axis, expression rejected, schema round-trip). Inline vocabulary definitions `[[term :: definition]]` (importer emitInline → a `definition` mark) added 8 app tests (6 importer incl. math-in-definition + schema round-trip; 1 prompt CLAIM). The ```callout import fence added 8 app tests + 2 registry cases (variant aliases, unknown-variant warning, body join, inline math, empty-body fallback, import→schema round-trip). The editor↔importer blank-grammar unification (shared `lib/blankSyntax.ts`) added 16 app tests (`blankSyntax.test.ts` — the `parseBlankSpec` sigil grammar + the `blankAttrsFromSpec` editor mapping). The import-format registry (`importFormatRegistry.ts` + `importFormatRegistry.test.ts`) added 58 app tests binding the registry to the parser (source scan), the converter, and the prompt/doc. The callout editor block added 7 app serialize tests (`callout.serialize.test.ts` — all 4 variants round-trip + unknown-variant coercion + inline math). The blank hint/per-answer-feedback + math-blank import syntax added 23 app tests (15 in `markdownToTiptap.test.ts` incl. a `??`/`!!` escape regression + an end-to-end md→`tiptapToActivity` guard that `BlankToken.hint`/`.mistakeFeedback` populate; 8 in the three-way `markdownImportPrompt` guard). The ```shortanswer/```essay import fences added 25 app tests. The four pedagogical blocks + import fences + Phase 2.6 short_answer/essay/rubrics added ~82 tests across the packages (incl. a runtime `init.test.ts` case proving a `fill_in_blank` NESTED in a `faded_worked_example` is discovered by `buildRefs`, and a jsdom `free-text.test.ts` for free-text capture/gather/persist + the essay word counter). `runtime-doc-contract.test.ts` classifies the pedagogical trio as CONTAINER and the three free-text types (`self_explanation` / `short_answer` / `essay`) as INTERACTIVE (their shared RUNTIME.md contract section is guard-enforced). Runtime bundle **base 41.8 KiB / graphs 49.1 KiB** minified (+ a 1.7 KiB feedback sidecar inlined only on gradable pages) (re-check with `pnpm bundle:renderer`; treat printed numbers as truth). **Both variants are now over the 40 KiB soft target** — under the 60 ceiling, accepted per the 2026-07-10 budget amendment (DECISIONS.md → "Runtime size budget amendment"). **Base has crossed its soft target** (the math-blank runtime landed on it) — the budget-ladder lever (per-question-type inlining variants) is now **SCHEDULED as the top backlog item** (see "Backlog / candidate arcs" above), to land before the next question-type generation. The graph-runtime-into-kit lever was pulled 2026-07-10 — graph work grows the cached kit, not the inline bundles. (Environment note: this machine is the 2026-07-04 recovery clone — old computer died; toolchain re-set-up verified by a full green run. Kit-upload creds live in a gitignored `.env.r2`, auto-loaded by `pnpm upload:graph-kit` — the upload is an explicit command; `pnpm build:graph-kit` is build-only and never touches R2; see `.env.r2.example`.)

## Repo layout

```
activity-platform/
├── docs/
│   ├── design/        — feature designs captured ahead of implementation
│   ├── DECISIONS.md   — architecture decisions + reasoning
│   ├── HISTORY.md     — archived completed-work logs
│   └── COLLABORATION.md — working-with-the-author notes
├── packages/
│   ├── schema/        — Zod types, document model, factories
│   ├── viewer/        — @activity/viewer (components-as-data arc): block registry + design tokens (S0); container shell + block components land S3/T6
│   ├── renderer/      — pure JSON → HTML string; KaTeX inlined; no DOM (retiring from the student path behind the two parity gates)
│   │   ├── RUNTIME.md — runtime architecture + data-attribute contract
│   │   └── src/runtime/ — published-page runtime (DOM TS, own tsconfig; per-file map in RUNTIME.md)
│   ├── graph-kit/     — @activity/graph-kit: shared graphing kit (Phase 2.7). evaluate.ts (math.js seam) + calculator.ts (mountCalculator) + runtime.ts (lazy kit runtime) + runtime-contract.ts (bridge↔kit types). mathlive + mathjs. Lazy-loaded on published pages; imported directly by the editor preview
│   └── app/           — Vite + React 19 + TS + Tailwind v4 + React Router v7
│       └── src/
│           ├── editor/     — Tiptap extensions, NodeViews, popover hosts, toolbar, slash menu
│           ├── components/ — PublishControl, ImportMarkdownDialog, RequireAuth
│           ├── routes/     — Activities, ActivityEditor, ActivityPrint, Submissions, Playground (dev-only)
│           ├── lib/        — serialize.ts, submissions.ts, uploadImage.ts, markdownToTiptap.ts, markdownImportPrompt.ts, foldable/ (print engine)
│           └── __tests__/
├── supabase/
│   ├── migrations/    — 0001–0012 (all applied)
│   └── functions/     — publish-activity, ingest-submission, upload-image, _shared/ (cors.ts, renderer.bundle.js + graph-kit-manifest.ts — auto-generated)
├── scripts/           — bundle-renderer.mjs; seed-test-data.sql; seed-e2e-activity.sql; seed-e2e-locked.sql
└── ...root configs
```

## Key constants

- **GitHub repo:** `ZanReed/activity-platform`
- **Supabase project ref:** `dtqutpdplefmufrrakxs`
- **Storage bucket (legacy):** `activities` — deleted 2026-06-18 after R2 verified end-to-end (note: the app's `.from('activities')` calls are the DB table, not this bucket)
- **Auth:** Google OAuth via Supabase. Site URL `http://localhost:5173` for dev. Allowlist-only signup (Phase 1).
- **Client env:** `VITE_PUBLISHED_URL_BASE` in `.env.local` (gitignored) mirrors the write-only `R2_PUBLIC_URL_BASE` secret; unset → published-page links hide.

## Open questions / deferred decisions

- **Import-format registry seam — BUILT 2026-07-21 (`d69dd92`, B+ variant).** `importFormatRegistry.ts` (declarative fence + blank-grammar index) + `importFormatRegistry.test.ts` bind the registry to the parser (source scan — fails on any fence/option the parser accepts but the registry omits), the converter (behavioural), and the prompt + doc. Chosen over the heavier full-codegen/parser-consuming variants (author call after risk discussion; see [capability-inventory.md](docs/capability-inventory.md) §5). The guard immediately caught + closed the two undocumented graph options (`no-solution-correct`, `no-builtin-feedback`).
- **Editor `{{…}}` input rule ↔ importer — UNIFIED 2026-07-21 (`196393a`).** The two parsers were extracted to a shared pure `lib/blankSyntax.ts` (`parseBlankSpec` + `blankAttrsFromSpec`), so typing a blank in the editor now interprets `~`/`=`/`==`/`?hint`/`!wrong :: fb`/`??`-`!!` identically to pasting it (before, the editor stored the sigils as literal text / made `?hint` an accepted answer). Editor-typed hint/feedback is PLAIN TEXT; the popover enriches it with math (author call). Live-typing verified via unit tests + the Playwright e2e pattern (author-run) — the in-app browser pane can't drive ProseMirror input rules.
- **Callout markdown import — BUILT 2026-07-21 (`d0a17a8`).** A ` ```callout ` fence (`variant:` line + inline body) now produces the callout block, registered in importFormatRegistry.ts so the anti-drift guard requires it documented. Callout is now reachable BOTH ways (editor block `b4558b4` + fence).
- **Inline vocabulary definitions — IMPORTABLE 2026-07-21 (`ae592a6`).** `[[term :: definition]]` (resolved in emitInline like blanks/math; `[[`/`]]` pass markdown-it untouched, verified) marks the term with a `definition` mark; content is plain text + `$math$`, editor popover adds rich formatting + image. A mark not a fence, so guarded by a markdownImportPrompt CLAIM, not the registry.
- **Choice/matching graph figures — IMPORTABLE 2026-07-21 (`d1dfe26`).** A `graph: <show-spec>` mc choice / matching side becomes a ChoiceGraph figure ("which graph shows…", "match the graph to its equation"); shared `parseShowDrawable` extracted from the graph fence (behaviour-preserving); default axis + one drawable, `expression` rejected (kit-free render). **This closes the importer parity arc** — every editor-authored question/content capability now has a markdown path. **This closed the importer parity arc.** Since then the two most-defensible remaining items were also built — choice/matching graph figures (above) and **in-equation math gaps** `\gap{}` (`9f06198`, ideal for faded-example "complete the step"). Remaining not-importable is now genuinely niche/by-design: **underline/sub/superscript** marks (markdown-it stays vanilla; `$math$` covers x²/H₂O), the **`problem`** block (a schema orphan with no editor NodeView either), and activity-level settings (feedback mode, calculator, reference panel, print, typography, submission modes — out of the importer's block-insert model by design). None worth building.
- ~~**Custom domain for R2** (Phase 2 polish)~~ — dead scope per the Cloudflare-exit ruling (2026-07-31); R2 retires at S9. A custom domain question survives only for the Pages-hosted SPA, if/when branding matters.
- **Empty fill_in_blank drag handle attachment** — whether `definingForContent: true` changed the handles-only-on-non-empty behavior is unverified; re-test during a drag-reorder pass. Minor.
- **Blank popover: one-click switch between chips** — deferred design decision, no data loss; full reasoning in [DECISIONS.md](docs/DECISIONS.md) → "Fill-in-blank authoring (Stage 13.5)". Needs a dedicated design pass (FocusTrap/selection entanglement).
- **Section metadata panel** — SectionBreakView's inline title/checkpoint UI is adequate for now; an editor-level panel remains optional.
- **Responsive `--blank-width` sizing** — deferred from Stage 11.
- **Phase 2 block-type priority order** (worked example, faded worked example, learning objectives, self-explanation) — decide when Phase 2 starts.
- **`skills` editing UI** — deferred to Phase 2 per schema scheduling; the field round-trips everywhere, only the editing control is missing. Don't add piecemeal without the per-skill-analytics scope.
- **UX validation with 2–3 other teachers** on the editor patterns before classroom adoption — cost rises sharply once students use activities.
- **Post-success edit edge case** — locked/single paths briefly write-then-remove the persistence blob (wasteful but correct). Low priority.
- **CDN-hosted shared runtime** (Phase 3+): trigger is when republishing for runtime bug fixes gets painful (~50+ active activities).
- **Multi-tenancy / governance when a teacher leaves a district** — Phase 4; helpers are designed for it.
- **Manual grading workflow shape** (Phase 2.6), **media storage/privacy posture** (Phase 2.8), **annotation coordinate space** (Phase 2.9) — each decided at its phase start.

---

**Last updated:** 2026-08-02 — **S6 local-first is BUILT (V1–V10) and browser-verified; the queue of author actions is EMPTY and that was verified against the live project, not assumed.** Two blocks sitting here marked QUEUED (migration 0020, get-activity redeploy) were stale — both already applied/deployed — and are struck above with the evidence. S6 shipped nothing that needs a deploy: no schema, renderer, runtime, or sanitize source moved, and all three bundles regenerate clean. Suite: schema 340 / graph-kit 384 / viewer 972 / renderer 724 / app 1017 unit + **227 e2e across three lanes (2 parked)**; typecheck + lint clean. Durable S6 reasoning is in [DECISIONS.md](docs/DECISIONS.md) (six new entries); the build narrative is archived in [HISTORY.md](docs/HISTORY.md). **Two things deliberately left open, both in [TODOS.md](TODOS.md):** offline reopen is unproven against the built worker (close before S9 cutover), and the ~3 MB app entry chunk should be split before students meet it (pairs with S8's perf budget).

⚠ **STATE.md is 391 lines against its own ~150-line target.** Not touched this session beyond the two entries above, deliberately — parallel sessions have been committing here and a restructure would trample them. It wants a real pass: the S1/S2/0016–0019 "DONE + verified" blocks under Pending author actions are finished narratives that belong in [HISTORY.md](docs/HISTORY.md), which would roughly halve the file.

**S5 (prior session, 2026-08-01)** — **S5 ENG-REVIEWED + RULED (17 decisions, 0 unresolved); S5 BUILD SUBSTANTIALLY COMPLETE — T1–T4, T6–T10, T12 DONE; the PARITY GATE RUNS GREEN IN CI (51 print e2e)** (viewer 714 → 845). ⏳ TWO THINGS NEED THE AUTHOR: generate the Linux screenshot baselines, and read + sign off the contact sheet.

**S5 T1a DONE (`06681df`) — the viewer's print layer stopped being a sketch.** Per-type page breaks (the stylesheet had been applying `break-inside: avoid` to `.viewer-block` wholesale, silently contradicting the registry for the seven `auto` types — paragraphs and lists were being held whole across page breaks); a unit guard now parses the break list back out of the CSS and fails on drift from the registry (CSS can't import TS, so this is the only thing holding them together). **Paper affordances** — circle-me letter per choice, write-the-letter line per matching item, number-the-steps box per ordering row — rendered ALWAYS and revealed by `@media print`, never built on demand, because the browser's own File→Print gives no hook to prepare in; the tests deliberately render in SCREEN mode since that's the mode students actually print from. Letters come from one shared helper matching the renderer's convention (Matching's private alphabet constant is gone). Plus the full treatment vocabulary (blanks→writing lines, MC letters with controls hidden, matching bank + lines, ordering boxes, writing boxes with the essay taller, figure caps, scaffold boxes with the faded example dashed, callout border styles, live board hidden). **S5-9 correction worth knowing: the dark→light print flattening ALREADY EXISTED in tokens.css** — the review's claim that it was missing was wrong (`--color-ink` really is `#f1f5f9` in dark mode, but the print block already forces `#000000`). What was actually owed was the GUARD, now added + red-green tested, so the next print rule reaching for an unflattened colour token fails a test instead of failing on paper.

**S5 T1b DONE (`1f650d6`) — authored layout finally reaches the page.** The viewer had been dropping `Column.width` weights, `Column.minHeight` work space, and per-block `width`/`align` **entirely** — a teacher's 2:1 split rendered 50/50 and a half-width figure rendered full width, on screen and on paper. Rows are now a grid (flex's `1 1 0` cannot express weights by construction); the track list and footprint arrive as inline `--activity-*` custom properties mirroring the renderer's `--columns-template`/`--block-width`/`data-block-align`, which is also what lets the phone breakpoint collapse them (an inline width would follow a student to 375px). Browser-verified with a synthetic 2:1 row: ratio exactly 2.00, 8rem floor → 128px, half-width block 225px inside a 449px column, left-align zeroing the margin. **This is why S5-OV2 exists** — none of these is a block, so the per-block roster could never have caught them.

**⚠ SIBLING FIX APPLIED (author, 2026-08-01):** `short_answer` and `essay` joined `self_explanation` in `breakInside: 'avoid'`, extending S5-OV6 to the two unnamed members of the writing-box family. A rule-based guard now asserts the WHOLE family avoids, so a future free-text type can't ship with a prompt strandable from its answer space.

**S5 T1c DONE (`bf2fc44`) — the document print layer.** `@page` size/margin (a real per-document `<style>` rule, NOT a custom property — `@page` can't read them, and a `var()` attempt would silently fall back to letter on every A4 printer), the `--print-*` vars on the worksheet root (problem spacing + work space scoped to `data-block-category="question"` so prose keeps its own rhythm), the print header (renders NOTHING when all fields are off — an empty ruled box reads as a load failure; `aria-hidden`, since blank lines for a pen have nothing to announce), and the reference print box honoring `printReferencePanel` in both directions. Browser-verified: `@page{size:letter;margin:0.5in;}` injected exactly once, vars on the root, header in the DOM and `display:none` on screen.

**✅ GLOSSARY APPENDIX DONE (`650b823`) — T1 IS COMPLETE.** The paper surface for inline definitions: a structural (not type-directed) collection walk ported from the renderer + a 7-variant `DefinitionBlock` renderer, gated on `printDefinitionGlossary`, appended after every section. **The integration that decides it works:** a test runs a real authored doc through the REAL sanitizer and asserts the appendix still finds the definition — the viewer only ever sees sanitized documents, and the sanitizer walks inline nodes hunting secrets, which is structurally what a definition mark looks like; had it stripped them, every glossary would print empty while all the other tests passed. **Fixture gap closed:** NO fixture carried a definition (they're an inline MARK — no per-block fixture can hold one), so the gate's `document/definition-glossary` case would have passed vacuously. The paragraph fixture now carries a real defined term ("slope"). Ported not imported, deliberately: the viewer must not depend on the renderer it replaces (that import dangles at S5.5); two copies until then, held equal by the gate's document fixture — the S4-6 posture.

**S5 T2 DONE (`eb30d5f`) — kit-free static SVG print twins.** Every kit-backed block renders two figures: the live JSXGraph board for screen, and a static twin that prints. **The extraction:** `graph-svg`/`number-line-svg`/`data-plot-svg` moved wholesale from the renderer into **`@activity/graph-kit/static-svg`** — a pure subpath (no DOM/JSXGraph/MathLive, the `/scorers` discipline). graph-kit is the package both surfaces already depend on and the one that outlives cutover, so it's the only home adding no dependency edge and leaving nothing dangling at S5.5; the renderer re-exports, and **its 724 tests passing unchanged is the proof the move was behaviour-preserving**. `escape`/`attr` moved with them. **The OV4 rule is live and TYPE-ENFORCED:** questions print empty axes, display figures print authored drawables — narrowed on the interaction, so only the display variant *has* a `drawables` field and a question's twin cannot carry one. Both surfaces emit `data-drawables` from that one renderer, which is what makes the gate's count assertion mean anything. **Browser-verified across all 13 kit instances:** 11 question variants at `0`, display graph `1`, display chart `5`, all hidden on screen; visually, clean labelled graph paper vs the same grid plus its authored point. **Bug caught in passing:** `export … from` creates no local binding and `html.ts`'s own helpers call `attr()` — compiled fine, failed at runtime on 107 renderer tests. **Bundles regenerated + leak-checked:** viewer-server 903.1 KiB, grading 2519.3 KiB, renderer 41.8/49.1 KiB — zero JSXGraph/MathLive code in any.

**S5 T3 DONE (`7ccf2e5`) — the print action + readiness barrier.** The Print control waits for pending math to actually render, `document.fonts` to settle, and every image to load or fail — all bounded by one shared budget, printing anyway on expiry with a report naming what didn't settle (a student who *can't* print is worse off than one missing a figure). **It waits on the DOM, not promises:** an earlier draft awaited `loadMathRenderer()`, which is a lie by one tick — the engine being resident ≠ React having re-rendered with it; it polls for `[data-math-pending]` to clear. **Two real implementation bugs the tests forced out:** (1) racing fonts/images against `sleep(remaining)` meant the LOSING arm still consumed the whole budget, so whichever check ran last got none of it — all three now poll one predicate against a shared deadline; (2) load/error listeners miss events that already fired — polling `img.complete` is the whole check, and it goes true on failure too, so a 404 settles instead of holding the budget. The image test double had the same disease (reported `complete:false` forever) and now models real browser behaviour. **Ctrl+P still races the chunk** — accepted residual (`beforeprint` is synchronous, no hook to hold), survivable because every fallback is built legible; S8 trigger already in TODOS. Placement is the top bar per S5-3; also mounted in `/dev/viewer` so T7's gate has something to drive. **Browser-verified live:** with pending math injected, the control reads "Preparing…" + disabled, reports `{ready:true, waited:["math"], timedOut:[]}`, and calls `print()` exactly once.

**S5 T4 DONE (`6afa398`) — the teacher's chosen worksheet font reaches students.** The viewer had ignored `meta.typography` entirely (the `--activity-font-family` seam existed since S0 and nothing set it). Not cosmetic: the fonts on offer are Atkinson Hyperlegible/Andika (low vision, early readers), Lexend (fluency), Comic Neue (dyslexia) — dropping the setting removes an accessibility decision made for a specific child. **One loader, two consumers:** the app's editor loader moved INTO the viewer (the app already depends on it), so there aren't two switch statements over five font ids to drift. `default` returns null rather than a stack, so the stylesheet's own token stays in charge. The root also carries `data-activity-font` — an id is comparable across surfaces whose font PIPELINES differ by design, where a computed family string isn't (S5-4's "assert the name, never the pixels"). **Browser-verified:** selecting Atkinson sets the attribute + var, resolves in computed style, AND registers the family in `document.fonts` — the dynamic `@fontsource` import really fetched files.

**✅ S5 T7 DONE (`b6c1809`) — THE PARITY GATE RUNS, 35/35 GREEN.** Every registered type on BOTH surfaces, the variants that change a printed rule, a no-network proof, a dark-mode print case (closing S5-9), and a print-after-check case. **The first runs found SEVEN real defects** — in the table, the gate, and the fixtures — before it guarded anything: `drawable-count` read the container not the SVG; S5-OV6 improvements were being asserted on the renderer that deliberately still has the old behaviour (now viewer-only BY NAME with reasons — this is what makes the gate spec-referenced rather than output-referenced); `bare-underline` measured border STYLE when Tailwind's preflight sets `solid` on everything with width 0; the lazy-component wait was satisfied by other blocks' wrappers so graph twins were measured before mounting; **callout had ONE fixture** when the rule that matters is that four variants stay distinguishable in grayscale; variant checks ran against whichever instance was first; and two more viewer-only improvements surfaced (the renderer gives every free-text block the same box, and leaves a static `graph_figure` uncapped). **⚠ AND a vacuous-pass hole the red-green pass exposed:** deleting the "solutions never print" rule did NOT turn the gate red, because pills/feedback/solutions only exist AFTER a check — the rules were asserted about elements never on the page. Fixed by checking a section first (the harness now scripts solutions); the same mutation now fails. Also: a suppressed check is now OMITTED rather than emitted as unrunnable — the guard suite rejected the latter, correctly. Run with `pnpm --filter @activity/app exec playwright test print-parity`.

**✅ ALL THREE FIXTURE CLASSES GATED (`b564269`) — 48 cases.** The block roster was gated; structural and document were declared but unasserted. Twelve new cases via harness query params: a 2:1 column row prints side by side **in its authored ratio** (a split that prints 50/50 has lost the teacher's layout as surely as one that stacks), footprint sizing survives, reserved work space is a real floor, sections flow; header fields print as labelled rules in order, A4 reaches `@page`, configured spacing lands on QUESTIONS not prose, and the reference box / glossary each print when on **and are ABSENT when off** (the second direction is what catches a component ignoring the setting — how a teacher ends up with a reference sheet on every worksheet in a unit). Red-green proven both ways.

**✅ S5 T8 DONE (`3fd891c`) — the gate RUNS IN CI, plus the baseline path.** Its own job (needs a browser + dev server; a browser failure should read as "print parity broke", not as a mystery inside `test`), Chromium-only, failure traces uploaded. **Deliberately scoped to print-parity** — the editor's 145-case e2e suite stays local-only; folding it in would cost runtime and make this job's failures ambiguous. That's a separate decision. **Screenshot baselines** land as the layer that catches what the rules don't NAME (a collapsed margin breaks no rule and still ruins a worksheet); viewer-against-viewer only, since comparing the two surfaces is exactly what S5-6 rejected. **⏳ Baselines are Linux/CI-authoritative (S5-7) and NOT YET GENERATED** — the suite SKIPS (22) rather than failing, so a missing baseline never reads as a broken build. Generate via the manual `workflow_dispatch` (`update_print_baselines`), download the artifact, commit the `.png`s. CI never rewrites its own expectations.

**✅ S5 T9 DONE (`dbfc151`) — pagination, the layer print emulation cannot reach.** `emulateMedia({media:'print'})` applies print CSS in ONE CONTINUOUS VIEWPORT and never paginates, so every break rule checked there was a computed VALUE, not a fact about layout. `page.pdf()` is the only thing running Chromium's paged-media layout. **The key test caught itself being useless first:** "the break rules change the layout" compared the fixture worksheet with rules on vs off and got **9 pages either way** (its blocks are small enough that none would straddle, so nothing has to be pushed) — written with `toBeGreaterThanOrEqual`, it passed on that equality and proved nothing. It now makes blocks taller than half a page so `avoid` MUST push each one whole: **32 vs 24 pages**, strict inequality. **Deliberately NOT covered:** per-block "this problem didn't straddle" — Chromium's PDF text is in compressed streams, needing a parser this repo doesn't carry, and the contact sheet shows a human the same property. A decision, recorded.

**✅ S5 T10 DONE (`d7c4832`) — the contact sheet.** A generated side-by-side (every block, renderer beside viewer, both in print media) for the half of parity that isn't automatable. Outside the gate and outside CI on purpose: it produces something to LOOK at, and a "test" whose result is a person's opinion shouldn't go red on a push. Generated + checked (22 rows, 44 images, all references resolving); output gitignored. **⏳ AWAITING THE AUTHOR'S ONE RECORDED READ** — regenerate with `PRINT_CONTACT_SHEET=1 pnpm --filter @activity/app exec playwright test print-contact-sheet`, open `packages/app/print-contact-sheet/index.html`, and record the sign-off here.

**✅ S5 T12 DONE (`8ac8cd9`) — DECISIONS entries** for the parity artifact (and why cross-surface pixel diff was rejected), the spec-referenced property that makes an improvement expressible, the three fixture classes, the pagination gap, and the static-SVG extraction. **T11 is satisfied by construction** — the fixtures module already pins `FIXTURE_SHUFFLE_SEED`, so ordering fixtures are deterministic and print baselines can't flake on a reshuffle.

**⏳ QUEUED FOR THE AUTHOR (both are S5's remaining gate work, neither is code):**
1. **Generate the print screenshot baselines on Linux.** Run the CI workflow manually (`workflow_dispatch` → `update_print_baselines`), download the `print-baselines` artifact, commit the `.png`s. Until then that suite SKIPS (22) rather than failing — a missing baseline never reads as a broken build. Linux-authoritative per S5-7 because font rasterisation differs from macOS and a laptop baseline fails in CI for no reason a human would call a defect.
2. **Read the contact sheet and record the sign-off** (above). That is the third leg of the parity artifact.

**Then S5 is closed and lane B moves to S6** (failure matrix + local-first buffer + queued checks + service-worker cache; finding R5 amends it: sign-out purges SW caches AND the buffer since they hold student WORK, queued checks are user-keyed and never fire under a different session, both explicit e2e cases). **S5.5** (teacher-print migration: ActivityPrint on the viewer tree, answer-key variant, foldable re-point) stays committed as lane B's follow-on, gated on S5 green — the renderer fully deletes only after it.

**S5 T6 DONE (`1acfb04`) — `printExpectations(type, ctx)` is the gate's contract.** The single location for the per-block print rules asserted on BOTH surfaces, plus the three fixture rosters (blocks derived from the registry; structural for columns/sizing/confidence; document for header/reference/glossary/page-size/print-vars/typography — the classes a per-block roster is structurally blind to). Named honestly per S5-5: only break-inside/break-after and the roster derive from PrintSpec; the rest is a written per-block table with a per-surface selector map, because a graph grid caps at 3.5in while a number line caps at 5in under the same treatment. **Ruling S5-OV6 landed with it** — `math_block`, `data_plot`, `self_explanation` now declare `break-inside: avoid`; the registry comment that said "T8 decides" now records the decision. **Verified, not assumed:** `SANITIZER_REV` is still `1-f8328527` (the rev hashes sanitize specs only, so the print change orphans no cache rows and needs **no `get-activity` redeploy**); `printExpectations` is absent from the regenerated viewer-server bundle (it imports FROM the registry and is never re-exported by `server/index.ts` — the V9 leak cannot repeat); and all six guards were **red-green tested by mutation** (dropping the solutions rule, duplicating a callout border style, printing empty axes for a DISPLAY figure — the OV4 bug, dropping a document roster entry, and skipping a check on one or both surfaces each turn the suite red). Viewer 714 → **742**; workspace green; typecheck + lint clean. **⚠ ONE DECISION OWED AT T1:** `short_answer` and `essay` share `self_explanation`'s writing-box structure and its former oddity but were NOT named in the ruling, so they still declare `breakInside: 'auto'` — extend the fix to them, or record why not. (Same "audit the unnamed siblings of the family" lesson as the `plot_ray`/`plot_segment` wire gap.) **Next: T1** (viewer print CSS: per-treatment gaps + structural + document layer) — or T2/T3 in parallel; T6 was the shared contract the other lanes needed first.

**S5 REVIEW SUMMARY.** Scope re-cut with the author: **S5 = student print mode + parity gate; S5.5 = committed teacher-print migration** (ActivityPrint on the viewer tree, answer-key variant, foldable re-point), gated on S5 green; the renderer fully deletes only after S5.5 ("retirement" at S9 = student path). **R4 is CLOSED — the parity artifact is defined and it is NOT cross-surface pixel diff** (rejected: two different DOM/font systems make any threshold permanently red or vacuously loose). The gate = PrintSpec-derived `printExpectations` DOM/computed-style assertions on BOTH surfaces + viewer-only screenshot baselines (linux-CI authoritative) + a `page.pdf()` pagination pass + one recorded contact-sheet sign-off. The outside voice (11 findings) **amended 4 in-review rulings**: document-level print features (header/reference box/glossary/@page/meta.print vars) are STUDENT surface and stay in S5 with document fixtures; display-variant graphs print their AUTHORED drawables via graph-svg **extracted** from the renderer (question variants print empty axes); the print-readiness barrier awaits fonts + images, not just lazy chunks; the harness serves KaTeX assets locally so the network block degrades neither surface. Also ruled: structural fixtures (columns/sizing/confidence rows — the per-block roster is layout-blind); the two "faithful print oddities" **FIXED in PrintSpec now** (math_block + data_plot breakInside, self_explanation prompt/box cohesion — registry.ts said "T8 decides", and this was T8); dark-mode print **forces light tokens** (found in-review: `var(--color-ink)` underlines could print white-on-white; pin + dark-mode e2e fixture); meta.typography wired in S5 (@fontsource, never R2); top-bar Print action now, 7.3A's completion-panel/chip placements land with their chrome. Full record: gstack design doc → "S5 eng review" (rulings S5-0…S5-abs, 12 tasks T1–T12, failure modes, rosters); test plan artifact `user-main-eng-review-test-plan-20260801-134500.md`; TODOS.md gained the S8-keyed eager-KaTeX trigger.

**Prior (same day):** 2026-08-01 — **S4 ENG-REVIEWED (17 rulings, 0 unresolved) + BUILD STARTED: migration + grading engine landed, 4 of 10 tasks done.**

**S4 REVIEWED 2026-08-01 (`/plan-eng-review`, FULL).** 17 decisions ruled; outside voice raised 13 findings, **3 of which AMENDED rulings made earlier in the same review**: the rate limiter (the read path's isolate limiter is documented in-repo as nearly inert → DB-backed per-student instead), the grades widening (freezing `unique(check_id, block_id)` before the teacher UI that decides attempts-vs-latest → **cut from S4**), and the previously-unruled empty-check solution reveal (→ parity kept, property recorded). The **authorization chain was the plan's one security hole** and is now a required branch pin. Full rulings + build order: the "S4 eng review" section of the gstack design doc. Test plan artifact: `user-main-eng-review-test-plan-20260801-120000.md`.

**S4 BUILD — ALL 10 TASKS DONE, LIVE-VERIFIED, NOTHING OUTSTANDING** (21 commits on `main`; viewer 432 → 716, renderer 676 → 724).
- **T1 migration 0020** — `section_checks` + `get_activity_version_for_check` (the authorization chain) + `record_check` (service-role only, per-student rate ceiling, idempotent replay, 0005-style attempt race backstop). Probe-verified 22/22; queued above.
- **T2 grading engine** (`packages/viewer/src/server/grading/`) — blanks incl. consume-once interchangeable groups, MC, matching, ordering, the full graph family dispatched to graph-kit's pure scorers, the raw-document walk, and `gradeSection`. Outbound sanitize reuses S2's deep walk via a new `sanitizeInlineContent` export.
- **T7 expression guards** — bounds before the math engine compiles student input (new server-side threat class).
- **T6 mock↔real conformance** — the real engine asserted against the behavior all 22 S3 components were built on.
- **T3 golden corpus + PARITY GATE** — the ruled renderer-retirement gate. The corpus is DATA (`@activity/viewer/grading-corpus`, so it outlives the renderer); two halves execute it — the server engine, and the published page via `renderActivity()` → jsdom → `buildRefs()` → the runtime's own scorers. Both assert against the corpus's own expected column, never against each other (two engines can agree and both be wrong, and this one was ported by *reading* the runtime). **Red-green tested:** scoring empty as wrong breaks 2 cases, disabling normalization breaks exactly the 3 G1 divergence cases, restoring returns 50/50. Scoped honestly — the graph family is absent because the engine IMPORTS graph-kit's scorers, so its parity is true by construction; `CORPUS_COVERAGE` forces every `auto_gradable` registry type into a justified bucket and a guard fails if a new one appears without an entry.

**Three things the build found that the review did not:**
1. **A wire gap: `plot_ray` / `plot_segment` were ungradable.** The kit has always produced `shape` + endpoint styles, but the check wire dropped them and `InteractiveGraph` never forwarded them — and they are answer content, not decoration ("2 ≤ x < 7" ≠ "2 < x < 7"). Added as OPTIONAL fields, so **no `CHECK_WIRE_VERSION` bump**: absent means "no shape chosen", read as unanswered rather than a lucky default.
2. **Field names the plan assumed wrong.** There is no `strategy` (it's `answerType`, absent ⇒ text) and no `groupId` (an order-independent group is a maximal adjacent run of `interchangeableWithPrevious`). `ordering` has no key field at all — the authored item order IS the key, which is why the grader must read the RAW document.
3. **`ordering`'s omission rule needed a new idea.** The wire carries no `moved` flag, but the serve shuffle is seeded, so the server recomputes the permutation this student was served and reads "identical to what we served" as untouched.

**Author ruling G1 (2026-08-01) — typographic normalization.** Rulings 6.1A (a11y baseline names "server-side unicode-minus normalization") and 7.1A (exact parity; the runtime normalizes nothing) directly conflicted. Ruled: normalize a small closed set of look-alikes (unicode minus, smart quotes, exotic spaces) on BOTH sides. An input-method fix, not a pedagogy change — it never makes a wrong answer right, it stops punishing a right one for its encoding. Explicitly NOT case folding, whitespace collapsing, or NFKC.

- **T4 handler + `check-activity` Edge Function** — 39 branch pins. The authorization chain runs BEFORE the raw document is read and AS THE CALLER; the suite pins that ORDERING via port spies, not just the status, because a handler that 404s *after* reading the document would pass a status-only test and still be wrong. `verify_jwt` stays **TRUE** — deliberately not in the no-verify-jwt trio, since a check is always made by a signed-in student.
- **T9 `grading-server` bundle + CI drift guard + `pnpm deploy:check`.**
- **T8 client-side checking** — typed `CheckError` taxonomy (`stale_client` / `unauthenticated` / `rate_limited` / `offline` / `server_error` / `unknown`, each with `retryable`), so the viewer stops telling a student to retry a failure retrying cannot fix; per-section **idempotency key** kept across a retry and refreshed on a deliberate re-check (a 3-4 s cold start is long enough for a student to press Check twice, which would otherwise show the teacher two attempts for one piece of work); and the **passive stale-version banner** (role=status, student-chosen reload, never automatic — auto-reloading would discard in-flight work).

**⚠ THE BUNDLE CEILING CAUGHT A REAL LEAK ON ITS FIRST RUN.** The grading bundle came out at **8 MB** and failed the build: importing graph-kit's scorers through the package BARREL pulled in **1 MB of MathLive**, exactly what R2 said must never happen (the graded form is already ascii, so the server needs no LaTeX). Fixed by adding **`@activity/graph-kit/scorers`** — a narrow pure-scorer entry with no DOM, no JSXGraph, no MathLive. Bundle is now **2516 KiB**, verified free of both. **Standing rule now in CLAUDE.md: anything server-side imports the scorers from `/scorers`, never the barrel.**

- **T5 check-response leak tests** — the check channel has a DIFFERENT rule from the read path (it deliberately releases solution + feedback after a check), so the scan is per-field and VALUE-based rather than key-name-based. The fully-loaded secret-bearing fixture moved to `tests/helpers/leakFixture.ts` so both channels scan the same document. **The fixture had a real ambiguity this exposed:** one sentinel stood for both answer material and releasable content, so the check suite went red on solution text it is supposed to return; they are now split (`STR` never released on any channel / `RELEASABLE` stripped from the read path but returned by a check). Red-green tested — disabling the outbound sanitizer fails it with the poisoned answers in the payload.

**✅ MIGRATION 0020 APPLIED + VERIFIED LIVE 2026-08-01 (12/12).** Structure exact (2 SELECT policies, **0 write policies**, RLS forced, 5 indexes, both functions DEFINER + pinned `search_path`, `record_check` grants exclude authenticated/anon/PUBLIC). Behaviour verified impersonated in a rolled-back transaction: **A2 foreign-parent version REFUSED** (`Not available`) and **B1 `authenticated` CANNOT call `record_check`** (`permission denied`) — the two security boundaries — plus anon refused, attempt numbering 1→2, idempotent replay returning the ORIGINAL verdicts, the rate ceiling firing, student-reads-own / stranger-sees-nothing / teacher-reads-own-activity, and a direct student INSERT refused by RLS. **Zero residue** (0 probe rows, 0 probe versions) and the ACL completeness query still returns exactly ONE anon-executable function — the documented 3.2A `get_activity_public_meta` exception — so 0020 added no anon surface.

- **T10 `scripts/verify-check-e2e.js`** — drives the REAL student path end to end (get-activity resolve → content → check-activity), so a disagreement between the read API and the grader about ids fails it. Covers what neither the unit suites nor the SQL matrix can: a real session JWT surviving the round trip, the bytes the BROWSER receives carrying no answer key, persistence, attempt increments, idempotent replay.

**✅ `check-activity` DEPLOYED + SMOKE-VERIFIED 2026-08-01.** Live at **v3, `verify_jwt: true`** — correct, and deliberately NOT in the no-verify-jwt trio. Verified without credentials (plain curl): an unauthenticated POST gets **401 `UNAUTHORIZED_NO_AUTH_HEADER`** (Supabase's own gate, refusing before our handler runs — the flag is on and enforcing) and a CORS preflight gets **204** echoing the Pages origin with the right allow-headers. **Also noted from `list_edge_functions`: `get-activity` is now v6**, so the queued S2 redeploy (sanitizer derive step + handler extraction) has landed; the read cache's stale rows self-orphaned on the `SANITIZER_REV` change as designed.

**✅ S4 IS COMPLETE AND LIVE-VERIFIED 2026-08-01 — E2E 13/13 on the deployed function.** Run against activity `6a84c8cb` (a real 11-item section: 8 blanks + 1 MC + 2 free-text). A real check returns **200, 11 items graded**; **C2 the foreign-version parentage probe REFUSED live with 404** (the security hole the eng review's outside voice found, now proven closed above the database, not just in it); C3 unknown version 404 with the same no-oracle message; C4/C5 typed 400s; **D1 the bytes the BROWSER received carry no answer-key field**; 401 without auth.

**⚠ THE E2E CAUGHT A REAL PRODUCTION BUG THAT EVERY UNIT TEST MISSED.** `_shared/cors.ts` emits `{ error, details: { code } }`, but `httpCheckService.checkErrorFor` read `body.code` at the TOP level — so `wire_version_mismatch` never matched and a stale tab fell through to the generic un-retryable `unknown`, meaning **T8's whole `stale_client` path could never fire in production** (a student would get a dead end instead of "reload"). Root cause: the handler test's fake `CorsKit` spread `details` at the top level while the real helper nests it, so **both sides of the integration were written against the same wrong assumption and both passed their own tests**. The double now mirrors `cors.ts` exactly and a regression pin asserts the real body shape. Standing lesson: a test double that does not match the real implementation is worse than no double — it manufactures confidence.

**✅ FINDING R2 CLOSED — cold-start mitigation RULED (author, 2026-08-01): accept the latency, NO warm-ping cron.** Measured n=42 on the deployed function: **p50 855 ms, p90 1474 ms, p95 1503 ms**, min 598 ms, one 4183 ms cold-start outlier matching S2's independent 3–4 s figure. Warm p95 is within tolerance for an explicit action that already shows a pending state (2.1A); the cold start lands on ONE student per period and the classroom pattern self-mitigates. Full reasoning + what would reopen it: [DECISIONS.md](docs/DECISIONS.md) → "Check-RPC latency".

**🧹 E2E residue: 44 `section_checks` rows** on activity `6a84c8cb` from two timing runs (`section_checks` has no DELETE policy by design). Harmless — they are the author's own account on their own test activity — but clear them when convenient, service role:
```sql
delete from section_checks where activity_id = '6a84c8cb-fc49-4338-ab49-927ba6254f20';
```

**~~PRIOR STEP, now done — run the E2E.~~** Paste `scripts/verify-check-e2e.js` into the browser console on the Pages app **while signed in, with a published activity on the account**. Section E measures p50/p90/p95 over 20 warm checks plus the first-call number. **That number is what finding R2's cold-start mitigation ruling has been waiting on** ("measure the batched RPC first; if p95 misses, options are a warm-ping cron or accepting with explicit Checking… UI — don't pre-build either"). The script **writes ~20 real rows** (`section_checks` has no DELETE policy by design) and prints scoped cleanup SQL at the end. It deliberately does NOT re-test the 429 ceiling: tripping it live costs ~60 junk rows to re-prove what the 0020 probe already verified against the real database (case B8).

**Two guard gaps the T8 work exposed and closed:** (1) the styles token guard only policed `--color/--state/--callout` names, so the banner's `var(--font-size-sm)` — a name from no token family we have — silently resolved to nothing and nothing failed; it now checks EVERY `--*` reference against tokens.css (red-green tested). (2) The store swallowed untyped errors from the check service, so a client-side `TypeError` was indistinguishable from the server being down; untyped failures now log.

**QUEUED FOR THE AUTHOR (after 0020 is applied):** `pnpm deploy:check` — no flag, `verify_jwt` stays true.

**⚠ Unexplained one-off:** `sanitize.test.ts` → "differs across students and across versions" failed once during this session and did not reproduce in 13 subsequent runs (8 of them the file in isolation). The test's own comment claims the fixed PRNG means it "can never flake". Not chased further; recorded so a second sighting is treated as a pattern rather than a surprise. The seeded shuffle is load-bearing for S4's ordering omission rule.

**Prior:** 2026-07-31 — **S3 FOUNDATION COMPLETE (V1–V8) + wire v2; ⚠ ONE AUTHOR RULING NOW BLOCKS V9.**

**🎉 S3 VIEWER IS END-TO-END 2026-07-31 — the student route is LIVE at `/a/:activityId` (432 viewer + 984 app tests).** Real activities now load from the S2 read API and mount for a signed-in student. **Read client** (`viewer/src/client/readClient.ts`, injected transport — no supabase-js in the viewer) owns two things the route shouldn't: the **stale-version retry** (version-keyed immutable URLs mean a republish is invisible until re-resolve; the server answers 404 + `current_version_id` and `load()` follows it once) and an **error taxonomy** (`unauthenticated` / `unavailable` / `unservable` / `offline` / `unknown`) so the failure UI switches exhaustively instead of branching on status codes. **Screens:** pre-auth gate naming the activity [3.2A] → skeleton with ~8s slow-escalation + Retry [1.2A] → worksheet → one honest failure screen per kind [Q3A], with Retry offered ONLY on recoverable kinds. **`createHttpCheckService` written against the frozen wire before S4 exists** — proves the contract is implementable and gives S4 a concrete consumer; until the RPC deploys a check fails into the store's designed "Couldn't check — try again" with every answer intact, so reading and answering work TODAY and grading lights up on deploy with no client change. **Verified against the live deployed function** (real title rendered anonymously). **⚠ Privacy finding → new pending action above.**

**S3 COMPONENT LAYER DONE 2026-07-31 — 22 components + styling, 419 viewer tests.** Styling landed as ONE token-only stylesheet (`src/styles/viewer.css`) for all 22 blocks + container chrome, built to the ruled paper-worksheet direction. **Token-only is ENFORCED** (`tests/styles.test.ts`): no hex/rgb/named colour literals, shadows must be tokens, and every `--color/--state/--callout` name used must be a REAL declared token (a typo would otherwise fail silently as an unset property). Lengths deliberately not policed — hairlines, `ch`/`em`, and the `@page` margin have no token and shouldn't get invented ones. The same suite pins the ruled floors so they can't lapse: 44px targets, visible focus ring, reduced-motion, phone breakpoint, and print-as-BLANK-worksheet (chrome hidden, `break-inside`, callout variants via BORDER STYLE for grayscale). **Two browser-only catches:** list markers had vanished (the app's reset strips ul/ol bullets; `list-style` is now explicit so the viewer never depends on the ABSENCE of a host reset), and the guard's first run correctly flagged `--callout-*-print-border` as undeclared — they're border STYLES living in `staticTokens`, not `colorTokens`. Browser-verified at 1366×768 in both themes; print rules confirmed live. README gained a Styling section.

**ALL 22 BLOCK COMPONENTS BOUND 2026-07-31 — the S3 component layer is COMPLETE (408 viewer tests).** The remaining 16 landed in one pass on the exemplar templates: 9 statics, 5 interactive, 2 containers, math_block. **~109 of the new tests were auto-generated by the conformance factory** the moment each binding landed — the D4 investment's largest dividend. Notable calls: **Ordering/Matching use buttons + a native `<select>`, deliberately NOT drag-and-drop** (drag is unusable by keyboard and hostile on touch; the old runtime's pointer-drag needed separate real-device verification — a drag affordance can be layered on the same state later); **FillInBlank** labels each blank "Blank 2 of 3" (a control mid-paragraph gets no accessible name from prose) and renders per-BLANK verdicts; **math_block is two blocks in one** per `familyOf` — promptless = static KaTeX, gap-bearing = lazy MathLive over a KaTeX fallback; **GraphFigure stays KIT-FREE** (plain SVG — the reason it exists apart from a display graph). **⚠ THE TYPE SYSTEM CAUGHT A DESIGN GAP:** `Problem` cannot render an authored solution — the sanitizer strips `problem.solution` like every other, so it's server-released after the section check. **S4 CONTRACT NOTE (recorded in the file):** `SectionCheckResult.solutions` is keyed by BLOCK id so no wire change is needed, but **the grading RPC must include solutions for STATIC blocks in a checked section** — a grader walking only blocks that submitted responses would never unlock a problem's solution, and it would look like a content bug. Browser bug fixed: gap-bearing math_block rendered twice (MathLive field + a red KaTeX error, since KaTeX can't parse `\placeholder`); fallback now renders `\square` and hides once the field mounts. Two conformance assertions made *precise* rather than weakened (containers answer through children → the invariant is "never a response key under its own id"; kit-backed lazy blocks can't show a focusable control in jsdom → browser-verified instead). Browser-verified: all 22 types render in the whole-worksheet view, 32 slots, 0 crashes, 0 unbound. Server bundle held at **899 KiB** with every binding in place.

**CANVAS FAMILY COMPLETE 2026-07-31 — number_line + data_plot built on the graph exemplar; 6 of 22 components done, 299 viewer tests.** Both copied the kit-backed pattern with no rediscovery — the StrictMode per-mount host and the sized-container requirement transferred as inherited knowledge. **number_line** needed ungraded mode in its kit widget (3 dereferences; handle count came from the key) and carries the only real geometry delta: 1-D positions ride the shared `graphs` category as `[value, 0]`, and `plot_interval`'s endpoint STYLES travel in `domain` because they're part of the answer ("2 ≤ x < 7" ≠ "2 < x < 7"). **data_plot** needed NO kit change — its widget already took an optional key, since its answer is computed from `data` the student must receive (the registry's documented `derivableFromServed` residual); the surface still drops the client-computed `correct`, so "the client scores this one block" never becomes a precedent. Shared bits → `canvasChrome.ts`; `graphSurface.ts` → `kitSurfaces.ts` (three seams now). Browser-verified: both number-line variants + all three interactive charts mount with real handles, zero failures. One browser-only bug fixed: a `minHeight` canvas leaves no DEFINITE height for the host's `height:100%`, so the kit fell back to its own default and the dotplot filled the viewport. **The conformance factory picked up both bindings automatically — 20 of the 28 new tests are ones nobody wrote.** Server bundle held at 899 KiB (the new lazy bindings stayed client-side; the guard works).

**S3 V9 COMPLETE 2026-07-31 — interactive_graph built, board gap CLOSED, all 6 variants browser-verified.** Chain: graph-kit ungraded mode (ruling A) → sanitizer derive step → the component → the StrictMode fix. **The empty-boards gap was a React dev-mode race, not a kit or recipe problem:** StrictMode runs mount→cleanup→mount, both async mounts shared the canvas ref element, and the kit begins with `mount.textContent=''` — whichever overlapping mount cleared LAST wiped the other's board DOM (witness: `JXG.boards` held 6 live boards, 4 with EMPTY containers, a different 4 each run — pure race timing, why it never reproduced twice). Fix: each mount creates its own child host div, cleanup removes it synchronously (`c635204`); stress-churned 6 mount/unmount cycles — zero empty canvases, `JXG.boards` steady at 6, no leak. Learning logged (`strictmode-shared-container-imperative-mount`). **⚠ BUNDLE REGRESSION FOUND + FIXED (the important one):** component bindings on registry entries leaked the whole component tree into the get-activity Edge Function, because the read API imports that registry for sanitize specs — **888 KiB → 2.8 MB at V5 (missed then) → 21 MB at V9**. Bindings moved to `registry/bindings.ts` (client-only); registry.ts is pure data again; bundle back to **899 KiB**, with a size ceiling in `scripts/bundle-viewer-server.mjs` so it can't regress silently. Two more component fixes from the browser runs: a swallowed mount rejection (now an honest per-block message) and a zero-height canvas (JSXGraph needs a sized container).

**✅ RULING RESOLVED 2026-07-31 (author chose A) — graph-kit ungraded input mode is BUILT.** `answerKey` is now optional on `mountGraphQuestion`; absent ⇒ nothing scores, no mistake classification, `correct` forced false with a new additive `scored: false` beside it so no consumer can read a verdict out of a keyless widget. **Sub-finding it exposed:** handle count and curve family were derived FROM the key but are question SHAPE, not answer (a student sees two handles either way; the coordinates are the secret) — ungraded mode takes them from a new `questionShape` config, and graded mode ignores it entirely so the two paths cannot disagree. `questionRecipe` exported (the mount needs JSXGraph and stays browser-verified; the graded/ungraded fork is pure and now covered by 14 tests, half of them pinning that GRADED behavior is unchanged). graph-kit 370→384. **Kit upload queued above.** **Immediate next step for the component:** the served block must carry `questionShape`, which means the sanitizer needs a *derive* step (today it only strips) — the derived values are provably non-secret, but that capability plus its leak tests is the next slice, ahead of the `interactive_graph` component itself.

**Original finding, for the record — the graph-kit question widgets needed the answer key.** Found building V9: `mountGraphQuestion` (and its system/data-plot siblings) take `answerKey` (`correctPoints` + `tolerance`) as a REQUIRED config and use it in 17 places to score client-side — but S2's sanitizer strips exactly those fields, so **the kit's question widgets cannot be mounted by the server-authoritative viewer.** Nothing is broken today (published pages + editor are unaffected); this blocks only the `interactive_graph` component and the graph-family blocks behind it. This is precisely what the kit-backed exemplar was added to surface — at block #15 it would have been expensive. **Options + Claude's recommendation (A) are in the design doc → "OPEN RULING".** In short: **A** = make `answerKey` optional in the kit (ungraded input mode; additive, one implementation, ships behind the standard kit-upload-then-deploy order) · **B** = a viewer-side widget over the kit's board primitives (no risk to published pages, but a second interaction implementation to keep in sync) · **C** = serve a placeholder key — **rejected, recorded so nobody re-proposes it** (it reintroduces the exact leak the sanitizer exists to prevent). **What landed anyway, safe under every option: CHECK_WIRE_VERSION 2** — the `graphs` category (block id → geometric work), store `setGraphWork`, index routing, mock grading. It carries **work only, never `correct`** (the old submission wire's `correct`/`earned` were client-computed because published pages had the key baked in; Q2B ends that). The `unsupported` escape hatch stays live-but-empty, with a test asserting the empty roster is a claim rather than an omission.

**S3 V1–V8 (the foundation, all green):** The whole test-and-scaffolding layer the DX review ruled is built, so the remaining ~19 block components are now a copy-the-exemplar loop rather than a design exercise. **V4** container shell + `blockIndex` (derives check payload ids from the served document via a deep walk; graph-family gradables recorded as `unsupported`, never dropped) + per-block `BlockBoundary` (one bad component can't blank the worksheet; a crashed GRADABLE block surfaces as a check shortfall; the fallback deliberately uses no state-vocabulary tokens). **V5** `InlineContent` (fixed mark nesting order, lazy-KaTeX math with a readable LaTeX fallback, definition disclosure) + `StatePill` (closed 4-state union bound to the `--state-*` trios — a fifth state is now a compile error) + the exemplar trio Paragraph/MultipleChoice/ShortAnswer, one per family; registry gained a typed `binding` with D16's eager/lazy axis. **V6** the conformance factory — bind a component and 8–10 family-contract tests you never wrote run against it, roster derived from the registry so a new binding is covered automatically; **red-green tested** (injecting a family violation failed exactly 2 tests). **V7** `/dev/viewer` app route, browser-verified: select → check → ✓ pill with aria-live → section `checked`, all 32 fixture blocks render with 0 crashes, real KaTeX chunk loads. Its ruled env-less criterion **failed on first test and exposed a real app bug**: `lib/supabase.ts` threw at module load and `SessionProvider` called `getSession()` on mount, so ANY missing env var blanked the entire app — both fixed (lazy Proxy client + `supabaseConfigured` guard), which also means a clean clone no longer greets you with a blank page. **V8** `packages/viewer/README.md` front door + root add-a-block checklist viewer rows + conformance failures that name the broken rule and cite the family spec. **Next: V9** (kit-backed exemplar — `interactive_graph` templates the canvas/graph-kit surfaces before graph-family mass production; also the trigger for CHECK_WIRE_VERSION 2, since wire v1 has no graph response category) then the remaining components. **Lane A (S4) is unblocked and the seam is waiting**: `packages/viewer/src/check/wire.ts` is the frozen contract S4 must import (it already types verdicts + feedback + the post-check solution-unlock channel), and `createMockCheckService` is the behavior S4's RPC must reproduce. Prior entry follows.

**Prior:** 2026-07-30 — **S3 STARTED (lane B): DX plan review RULED + test infrastructure V1–V3 landed.** `/plan-devex-review` ran full-depth on the S3 slice (17 decisions D1–D17 + outside-voice 14 findings, ALL recorded in the gstack design doc → "S3 DX review" section — read it before building components). Landed and green (viewer suite 132→158, typecheck+lint clean): **V1** component harness (jsdom+RTL+jest-dom scoped to `tests/components/`, eslint added — root lint no longer skips the viewer; harness sanity spec caught the RTL-cleanup-without-globals leak on first run) · **V2** generated fixtures (`@activity/viewer/fixtures` — authored realistic blocks for all 22 types + variants, always pushed through the REAL parse→sanitize→shuffle pipeline; 11 guards incl. registry completeness + auto_gradable realism) · **V3** check wire contract (`src/check/wire.ts` = the FROZEN S4 seam: `CHECK_WIRE_VERSION`, item-id-keyed maps, section granularity, verdicts+feedback+**solutions** unlock channel) + scriptable gate()-controlled mock + store seam (`createViewerStore`: fire-time snapshots per 2.2A, section status machine, `VIEWER_STORE_SCHEMA_VERSION` hydration gate per D9). **Next in order: V4** container shell (section check trigger + per-block ErrorBoundary w/ crashed-gradable visibility) → **V5** exemplar trio (paragraph + multiple_choice + short_answer; inline renderer w/ lazy KaTeX per D14; eager-statics/lazy-heavies per D16) → **V6** conformance factory → **V7** `/dev/viewer` (must boot env-less) → **V8** README+checklist. Task detail: design doc "S3 build order" + `tasks-devex-review-20260730-*.jsonl`. S4 note: the wire module is the contract S4 MUST import; R2's engine investigation stands. Prior entry follows.

**Prior:** 2026-07-29 — **S0–S2 test-setup review + gap closure.** Verified every S0/S1/S2 deliverable has its testing story (S0 registry/tokens guards, S1 manual-only + compliance pack, S2 leak/upgrade/type guards — all run green), then closed the two named gaps: (1) the get-activity handler extracted into the viewer-server bundle behind DI ports + 29 branch-pin tests (viewer suite now 132; see the queued redeploy above); (2) `supabase/migrations/README.md` gained a standing "Regression re-runs" section (re-run `verify-0013-0014.sql` A–D + `verify-0017.sql` after any auth/RLS/grant migration — the mitigation for the DB's no-CI-harness gap) and its stale "0017 queued" header fixed (0001–0018 applied). S3–S9 review findings ACTIONED same day: the no-old-pages amendment is now propagated into the gstack design doc (amendment header + ⚠ markers on 7B/TV2-A/T1A/T9; S9 demolition explicitly spares the 3.2A anonymous meta branch), and the doc gained a "Pre-build review findings (2026-07-29) — PROPOSED" section: **R1** ordering/matching check-RPC responses must be item-id-keyed (gates T5 wire) · **R2** grading-server engine — investigated: graph-kit's scorers are pure/DOM-free by design, MA-D3's ascii-stored form means NO MathLive server-side, engine is `mathjs/number`; runtime's non-math scorers are Element-coupled so they port as corpus-pinned semantics; cold-start (3–4 s measured) vs p95 is the open risk, mitigation ruled at T5 · **R3** viewer component-test harness decided at T6 kickoff; local-first buffer re-derives the STORAGE_SCHEMA_VERSION invariant · **R4** print-parity artifact defined before T8 (Playwright print-media both surfaces, registry-guarded fixture set) · **R5** T7 amended: sign-out purges SW+buffer, queued checks user-keyed · **R6** housekeeping (cache GC on S7 aggregation, census folded into S9 publish rewrite, throttled-lab TTI proxy, upgrade-fixture note now in `upgrade.test.ts`). **None ruled — walk the author through R1–R6 before the gated tasks start.** Prior entry follows.

**Prior:** 2026-07-28, third session — **S2 READ API COMPLETE (code + tests + docs; migration 0017 + first deploy queued for the author above).** Lane A's first step: T2 (definer RPC + upgrade-on-read + durable per-version cache + immutable headers) + T3 (registry-driven answer-key sanitizer + wire-level leak tests) + the 3.2A anonymous meta endpoint. Eight design decisions author-approved up front; full record in [DECISIONS.md](docs/DECISIONS.md) → "Read API S2". The shape: ONE Edge Function `get-activity` (`--no-verify-jwt`, self-verifying; anonymous meta / authed resolve / authed version-keyed content served `private, immutable`); access = any authenticated user reads the published CURRENT version (link-share behind sign-in; classes are not ACLs), enforced by `get_published_activity` running as the caller; durable cache = `activity_version_reads` DB table keyed `(version_id, sanitizer_rev)` where **`SANITIZER_REV` is computed from the registry's sanitize specs** (spec change ⇒ stale rows orphaned automatically, no hand-bump to forget); `ordering`'s serve shuffle is serve-time (seeded `version:user:block:field` — reloads never reshuffle, cache stays student-independent). New code: `packages/schema/src/upgrade.ts` (4A seam, zero migrations today, v1 fails loudly per the greenfield hard-cut), `packages/viewer/src/sanitize/` (generic SanitizeSpec transform + `sanitized-types.ts` type projection + shuffle), `packages/viewer/src/server/` bundled to NEW committed `_shared/viewer-server.bundle.js` (`pnpm bundle:viewer-server`, CI drift-guarded — separate from the renderer bundle so the read path never loads KaTeX), `supabase/functions/get-activity/`, `supabase/migrations/0017_read_api.sql` + `scripts/verify-0017.sql`. Two hardenings beyond the S0 contract, both recorded in DECISIONS: in-band secrets stripped by an **unconditional deep walk** (a prompted `math_inline` in ANY content array can't leak on a declaration miss; `inlineBlankSecrets` is now declarative), and a real S0 gap caught+fixed — **`MathPrompt.acceptableAnswers` was missing from `MATH_PROMPT_SECRET_FIELDS`** ("also accept" answers would have shipped to students; exactly the leak class the sentinel suite exists for, and it would have caught it). Docs migrated per Q4A: CLAUDE.md (no-verify-jwt trio + the new bundle rule), functions README (get-activity row, shared-code entry, deploy section), migrations README (table caught up 0010–0017; the stale "we don't store student accounts. Ever." line rewritten). NOT in S2 (fence held): no grading RPC (S4), no viewer UI (S3), no publish-flow change, no census (S7); R2 HTML publishing untouched until S9. S0+S1 narratives (same day) archived to [HISTORY.md](docs/HISTORY.md).

_Prior entries archived in [docs/HISTORY.md](docs/HISTORY.md)._
