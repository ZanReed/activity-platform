# STATE.md

A living "where am I" snapshot. Update at the end of each work session — replace the relevant sections, don't append. Keep it under ~150 lines: move finished-work narratives to [docs/HISTORY.md](docs/HISTORY.md), durable reasoning to [docs/DECISIONS.md](docs/DECISIONS.md). Project rules live in [CLAUDE.md](CLAUDE.md).

## Pending author actions

Things only the author does (pushes, deploys, migrations), queued and waiting:

1. **Reminder:** any future redeploy of `ingest-submission` needs `--no-verify-jwt` (see CLAUDE.md).

_(Nothing queued — all caught up. `main` pushed to origin; existing `number_line` interval activities re-published for the shape pills. The `data_plot` GRADED BUILDS deploy fully shipped 2026-07-12: kit `CWAMAF2L → V6T6HAUS` (carries shape-pills + histogram/box), manifest `a0f2d9d`, `ingest-submission` v36 widened-union `verify_jwt:false` (MCP-verified), `publish-activity` v88. Prior: `number_line` shape pills (kit `CWAMAF2L`); `data_plot` slice 1 (ingest v35, publish v86).)_

All 2026-07-08 → 2026-07-12 deploy trains — the 0009 security/performance pass, calculator-parity Tier A, the bounded-curve interaction, matching/ordering (wire v6), MC choice figures, graded function families, numeric + multiple-choice, the confidence/solution-reveal dedupe, and the confidence-radio locking fix — are deployed and **author-confirmed live**; narratives + clearance records archived in [HISTORY.md](docs/HISTORY.md) → "2026-07-10/11 — deployed trains". **Current live kit hash: `graph-kit-V6T6HAUS.js`** (the data_plot graded-builds re-hash — carries the number_line shape-pills + `build_histogram`/`build_boxplot`; uploaded to R2 + manifest committed `a0f2d9d`; `publish-activity` v88 redeployed 2026-07-12 so new publishes pin it — older hashes incl. `CWAMAF2L`/`WXYPUC5W` stay on R2 for already-published pages). Source of truth: `supabase/functions/_shared/graph-kit-manifest.ts`.

## Current focus

**`data_plot` GRADED BUILDS (histogram + box) SHIPPED + DEPLOYED — all 5 GB slices (2026-07-12).** Kit `V6T6HAUS` on R2 (manifest `a0f2d9d`), `ingest-submission` v36 (widened `DataPlotResponse` union, `verify_jwt:false`, MCP-verified) + `publish-activity` v88 live. A teacher can now author + publish a graded histogram or box plot; the graded data_plot block covers all three stats charts. The two remaining graded constructions on the shipped block: **`build_histogram`** (the student drags each bar's frequency) + **`build_boxplot`** (drag five tick-snapping handles clamped between neighbours; scored within `tolerance` vs the TI-84 exclusive-median five-number summary — method LOCKED). Design + 9 approved decisions in the follow-up section of [docs/design/data-plot-block.md](docs/design/data-plot-block.md). **Additive — NO wire/storage bump** (new `DataPlotResponse` union members, the graph ray/segment precedent). Built: schema+response union `e6bb062`; pure `scoreHistogram`/`scoreBoxplot` + two dedicated SVG boards + `mountDataPlotQuestion` branch `58fbee6` (browser-verified `/dev/data-plot`); renderer per-chart shell + runtime gather branch `2b32bce` (**bundle-rendered-page-verified**: build histogram [2,3] + box [1,2,4,6,7] → "2/2 correct", zero console errors); editor Type picker + box TI-84 readout + tolerance field `19769c4` (`/playground`-verified: box + histogram preview recompute); dashboard per-variant formatting (`formatBins`/`formatFive`) `913744d`. Suite: schema 244 / graph-kit 325 / renderer 516 / app 468 (**1553**); typecheck + lint green. Runtime bundle **base 37.5 / graphs 44.3 KiB** (the widgets ride the lazy kit; only the +0.3 gather branch touched the inline bundle). The graded data_plot block now covers all three stats charts (dot / histogram / box).

**`data_plot` block family (statistics charts) SHIPPED + DEPLOYED — slice 1 (`display` + `build_dotplot`), 2026-07-12.** Kit `WXYPUC5W` on R2 (manifest committed `1744eb2`), `ingest-submission` v35 (wire 3–8, `verify_jwt:false`, MCP-verified) + `publish-activity` v86 live. A teacher can now author a data_plot (graded dot plot or a display chart) and publish it. Optional live eyeball only (bundle-rendered + `/playground` already verified). The stats sibling of `interactive_graph`/`number_line`, reusing the same block→kit→pure-scorer template a third time. Design + 9 author-approved decisions in [docs/design/data-plot-block.md](docs/design/data-plot-block.md). Slice-1 scope: a **`display`** mode (dot plot / histogram / box plot as a static, ungraded stimulus — pure SVG the renderer draws, no kit/runtime, composes with a sibling question block) + a graded **`build_dotplot`** (the student stacks dots to reproduce a dataset's distribution). **DONE + committed (6 slices):** (1) schema — `DataPlotBlock` + `DataPlotConfig` (reuses `NumberLineConfig` axis + histogram extras) + `DataPlotInteraction` union, `data: number[]` source (the answer is COMPUTED from it — decision 3a), wire **v7→v8** (`dataPlotResponses` map) + ingest accepts 3–8, `96e3da6`. (2) renderer — `data-plot-svg.ts` (static dot/histogram/box + `dotCounts`/`histogramBins`/`fiveNumberSummary` TI-84 exclusive-median) + block shell + print/no-JS SVG, `96e3da6`. (3) kit — pure `scoreDotplot` + a **dedicated SVG dot-plot board** (decision 6b, no JSXGraph — click/keyboard add/remove dots, tick snap) + `mountDataPlotQuestion`, `ee9a5bc` (browser-verified at `/dev/data-plot`). (4) runtime bridge — `dataPlotExt` seam mirroring `numberLineExt` + `attachDataPlotRuntime` in the kit + **`STORAGE_SCHEMA_VERSION` 8→9** + runtime wire v8 + `document.ts` variant scan (only a GRADED data_plot triggers the graphs bundle; display stays on base), `e2f8a39`. **BROWSER-VERIFIED on a bundle-rendered page** (inlined graphs runtime + lazy local kit): display box plot renders static, build→check→"1/1 correct"→reload-restores dots+score+verdict, zero console errors. (5) editor NodeView — `DataPlot` node + `DataPlotView` (a numeric **data-table** + a live static preview via `renderDataPlotSvg`; a Type picker spans build-dotplot / display dot·histogram·box) + serialize round-trip (replaces the omit-stub) + slash/Insert "Data plot", `39c26c6` (`/playground`-verified: live preview + mode switch recomputes the box plot). (6) dashboard — `dataPlots` index + `formatDotValues` + a "Data plot" Submissions table, `bd26f19`. **As-built deltas:** no author board (compute-from-data authoring makes drag-to-define moot); display is pure static SVG (no hydration). Suite: schema 238 / graph-kit 313 / renderer 511 / app 465 (1527); typecheck + lint (0 errors) green. Runtime bundle **base 37.5 KiB / graphs 44.0 KiB** minified (graphs +2.8 over number_line — over the 40 KiB soft target, under the 60 ceiling, accepted per the 2026-07-10 amendment; per-type lazy chunks is the lever if it trends toward 60).

**`number_line` block family SHIPPED + DEPLOYED (all 6 slices, 2026-07-12).** Kit `WVG7FA6Y`, `ingest-submission` v34 (wire v7, `verify_jwt:false`) + `publish-activity` v85 live. A teacher can author + publish a number_line; students plot/graph on the live page. Optional live eyeball only (bundle-rendered + `/playground` already verified). Durable per-feature status is the Status-by-area table below.

**▶ `number_line` block family (1-D graded) — SHIPPED + DEPLOYED, all 6 slices (2026-07-12).** Design + all 7 decisions in [docs/design/number-line-block.md](docs/design/number-line-block.md) (author chose the recommended answers). Plot points / drag inequality intervals with open/closed endpoints, reusing the `interactive_graph` block→kit→pure-scorer template. **DONE + committed:** (1) schema — `NumberLineBlock` + `NumberLineConfig` + `NumberLineInteraction` union (plot_point, plot_interval; interval omits a bound → ray), registered in Block+ColumnCellBlock+factories; submission `numberLineResponses` map at **wire v6→v7** (own parallel map), ingest accepts 3–7 + migrates; `b5fe4aa`. (2) renderer — `renderNumberLine` shell + `renderNumberLineSvg` print/no-JS fallback + CSS; `b5fe4aa`. (3) kit **pure scorer** `592c15b`. (4) **kit widget — JSXGraph 1-D board + `mountNumberLineQuestion`/`mountNumberLineAuthor`** `31de219` (browser-verified at `/dev/number-line`). (5) **runtime bridge `565ca6d`** — a `numberLineExt` seam mirroring `graphExt` (+ `.noop.ts` stub) in `graph-integration.ts` (walk `data-block-type="number_line"`/`.number-line-canvas`, state seed, score-fold in checkpoints, gather `numberLineResponses` in submission, persist) + `attachNumberLineRuntime`/`buildNumberLineChrome`/`renderNumberLineChrome` in the kit `runtime.ts` + contract types in `runtime-contract.ts`; **`STORAGE_SCHEMA_VERSION` 7→8** (blob `numberLines` map) + **runtime `submission.ts` wire 6→7** + `document.ts` variant scan now triggers the graphs bundle on a number_line-only page. BROWSER-VERIFIED on a **bundle-rendered** page (inlined graphs runtime + lazy kit, not the dev harness): plot→check (1/2)→verdicts→reload-restore (point, interval bounds+styles, confidence, lock)→submit (wire-v7 payload with `numberLineResponses`); zero console errors. (6) **editor NodeView `5a551d6`** — `NumberLine` Tiptap node + `NumberLineView` reusing `mountNumberLineAuthor` (drag) + a numeric editor (point values / interval bounds + open-closed + unbounded=ray) + serialize round-trip (replaces the omit-stub) + slash-menu/Insert "Number line" entry + column-cell content expr + problemNumbering; browser-verified at `/playground`. (7) **dashboard `9ea33e4`** — `numberLines` index map + `formatNumberLineInterval` + a "Number line" table in the Submissions route. **✅ DEPLOYED 2026-07-12 (deploy train complete):** kit uploaded + re-hashed `E6G7VLZK → graph-kit-WVG7FA6Y.js` (entry + 7 chunks verified live on R2) + manifest committed `eee04e4`; `ingest-submission` redeployed accepting wire v3–v7 with `verify_jwt:false` (v34, MCP-verified — the `--no-verify-jwt` flag survived); `publish-activity` redeployed (v85, after ingest — safe order held) so new publishes pin `WVG7FA6Y` + emit wire v7. Live-page eyeball optional (author authors a number_line block + publishes). Suite: schema 222 / graph-kit 304 / renderer 487 / app 459 (1472); typecheck + lint (0 errors) green. Runtime bundle **base 36.4 KiB / graphs 41.2 KiB** minified (graphs over the 40 KiB soft target, well under the 60 KiB hard ceiling — a real new feature on the graphs variant, accepted per the 2026-07-10 budget amendment; per-type lazy chunks is the structural lever if it ever trends toward 60).

**✅ FIXED — number_line `plot_interval` shape buttons (author-reported 2026-07-12; branch `fix/number-line-shape-buttons`, merged to main).** The interval widget now offers the IDENTICAL shape controls as the graph ray/segment function — three pills **"Ray →" / "Ray ←" / "Segment"** plus contextual style pills (a shared `createIntervalShapeControls` in [number-line-question.ts](packages/graph-kit/src/number-line-question.ts), used by both `mountNumberLineQuestion` and `mountNumberLineAuthor`) replacing the old per-end closed/open/unbounded pills. Shape → board end-states: segment = both bounded; ray_positive = left bounded + right unbounded (→ +∞); ray_negative = mirror. A ray is one omitted bound, so the pure `scoreNumberLineInterval`, the stored interval, the board (draws the ray via the existing `unbounded` end-state), the wire, and storage are ALL unchanged — a control-surface swap only. Ray labels are static (a 1-D line has only two directions; no need for the graph's 8-way glyphs). Editor `NumberLineView` numeric panel got the same three shape pills (deriving shape from which bound is present), keeping the numeric bound inputs. BROWSER-VERIFIED: student `/dev/number-line` (shape pills, Ray → yields max-omitted payload, board draws ray, restore of a stored ray derives Ray → + scores correct, zero console errors) and editor `/playground` (board + numeric-panel pills in sync, Ray → collapses the JSON to a single bound). Suite green (graph-kit 313 / app 465), typecheck + lint clean. **✅ DEPLOYED 2026-07-12:** kit-only → re-hashed `WXYPUC5W → graph-kit-CWAMAF2L.js` (uploaded to R2, manifest committed `95e3991`) + `publish-activity` redeployed (no ingest redeploy — no wire bump). Author actions queued: push `main` (delivers the editor-side pills via the app deploy) + re-publish any existing number_line interval activities.

**No open eyeballs.** Both prior ones cleared 2026-07-12 (author-confirmed): matching+ordering pointer-DRAG works; the drawable-row continuation-arrows checkbox works as intended. The deferred sin-curve arrow bug that surfaced during that check is now FIXED and DEPLOYED (`9a68c0e`, browser-verified; kit `E6G7VLZK` + `publish-activity` redeployed 2026-07-12).

**▶ 2026-07-08/09 sessions — SHIPPED, DEPLOYED, and author-live-verified 2026-07-10:** graph leak fixes + mistake feedback + first-class ray/segment with student shape toggles (true-direction glyphs, teacher-side authoring), reference-panel pop-up, activity-wide typography, editor toolbar reorganization (+ text-style picker, config drawer). Full narratives moved to [HISTORY.md](docs/HISTORY.md) → "2026-07-08/09 — graph session, reference pop-up, typography, toolbar".

**▶ 2026-07-08 session — the graphing feature build-out (Drops 0–7), all committed-ready, 902 tests green.** (0) Runtime split into TWO build variants behind the `graphExt` seam (`runtime/graph-integration.ts` + `.noop.ts`; base 17.0 KiB — under target; graphs variant ~22 KiB inlined only on graph pages; document.ts picks by body scan; CI guard covers both bundles). (1) Graphs fit in columns (ColumnCellBlock + editor content expr + screen-only min-width floor with overflow-x scroll; browser-verified 2/3-col). (2) Schema foundations: FunctionModel = linear/quadratic/exponential/logarithmic/vertical (+ `fitLogarithmic`; params match the fitters); plot_function→`models[]`, shade_region→`regions[]` (arrays-of-one, systems additive); `partialCredit`/`allowNoSolution`/`noSolutionCorrect` flags; `EndpointStyle`; partial scorers (`score*Partial` → {earned,total}). (3) **Freeform command line**: `packages/graph-kit/src/formula.ts` `parseGraphFormula` — ANY equation format (slope-intercept/standard/point-slope/bare expr/`x=k`), inequalities (sign→strict+side, incl. vertical left/right), point lists, `for x >= 0` domain clauses; numeric solve-for-y + sample-and-fit family detection; `formatModel` round-trips; editor `FormulaField` two-way synced with handles (browser-verified: standard form, quadratic, vertical incl. board line-through-handles, error steering to Display). (4) **Inequalities**: `graph_inequality` interaction (boundary=FunctionModel so quadratic/vertical work; strict; shadeSide above/below/left/right; array-of-one); kit widget control bar (solid/dotted pills, shade-side pills + click-side-on-board, half-plane shading via hand-built filled curve); **wire v3→v4** (InequalityResponse + noSolution + earned/total; CheckpointResult.score now fractional; ingest accepts v3 AND v4, migrates on write; runtime STORAGE_SCHEMA_VERSION 4→5); partial credit + no-solution flow end-to-end (runtime fractional section scores + payload); editor picker + typed-inequality authoring (browser-verified). (5) **Display upgrades**: drawables gained curve style/shade/domain, `expression` (plots ANY formula by sampling), `ray`, segment `endpoints`, point open/closed — display board renders all. (6) **Rays/segments**: `FunctionInteraction.domains[]` (parallel to models) filled by the `for …` clause + drawn endpoints on display; the STUDENT endpoint-drag widget UX is the flagged follow-up (scoring stays on curve params until then). (7) **Markdown ```graph DSL** (docs/markdown-import-format.md): axes/prompt/answer/show/options lines through the same parser; importer + 6 tests; pdf-import.md + photo-grading.md graph sections. **Follow-ups (same-day second pass): ALL BUILT** — the Copy-AI prompt now teaches the ```graph fence (doc re-embedded, drift guard green); the drawable-list editor edits every kind (+ formula/ray rows, open/closed + dashed toggles); the narrow-column advisory warns on graphs in 3+-column layouts; and the **endpoint-drag widget landed**: domain-restricted plot_function questions grow amber GLIDER handles riding the curve (drag to place the ray/segment ends) + Start/End open-closed pills, scored per part (`scoreDomainParts`, position ±0.25 + style) and folded into correctness/partial credit; response carries an optional `domain` object (additive within v4); restore/lock wired. **Author verify CLEARED 2026-07-10 (fourth session):** inequality widget bar + domain gliders confirmed working by the author.

**▶ Phase 2.7 — Graphing track. Stages 1–4 SHIPPED + a full graphing-calculator UX pass, all browser-verified.** The **`@activity/graph-kit`** package is the kit, consumed two ways from one impl (published pages lazy-`import()` it; the editor preview imports it). Stage 1 (scientific) + Stage 2 (graphing, JSXGraph lazy-split); **Stage 3** (`2bc3f68`+`9df8738`) — the Texas Algebra I regression flow (Data view: table → linear/quad/exp → equation + r², TI-84 conventions, `allowedRegressionModels` flag); **Stage 4** (`f643087`) — the Desmos-defining **expression list** (color-coded MathLive rows: curves, `(a,b)` points, `a=3` sliders with live-drag re-plot, `maxExpressions` cap). **Then a large author-driven UX overhaul ([HISTORY 2026-07-06](docs/HISTORY.md)):** click-drag pan + on-graph zoom/reset/fit buttons + drag-resizable **floating draggable panel**; MathLive **virtual keyboard rendered inside the panel** (custom button pad dropped in graphing; single layout, ☰ menu hidden) with a clear toggle button + **responsive keyboard scaling**; list/board **splitter**; **hover-trace** `(x,y)` readout; bare-number rows show `= value`; remembered size/position/split; full-width click target; **sticky axes** (scale stays visible when an axis pans off-screen). Comma-key gap CLOSED (`dd5466f`, browser-verified): the graphing keyboard now grafts a comma keycap onto MathLive's numeric layout after the decimal separator, so touch-only students can type a point `(a,b)` — verified it renders, inserts, and plots. Spreadsheet-paste into the Data table was **dropped** from the todo (author call 2026-07-06; revisit later if wanted). **Kit on R2:** the re-hashed kit (comma key + graph widget + display board) uploaded + live as of the 2026-07-08 full deploy pass.

**▶ Stage 5 — graded interactive-graph block: SLICE 1 (2.7a plot-a-point) SHIPPED and LIVE (live-page verifies cleared 2026-07-10).** The design doc ([interactive-graph-block.md](docs/design/interactive-graph-block.md)) predated the kit; the reconciled shape: the graded widget rides the SAME lazy graph kit as the calculator (not a separate `graph-widget.js`), and the runtime is inlined (no per-publish `runtime.js`). Four coherent commits: **schema** (`db70beb`) — `InteractiveGraphBlock` + `AxisConfig` + a `plot_point` discriminated union (line/region are future variants, no migration), `graphResponses` submission map at a **v2→v3** bump (`PointResponse`), factory + Block-union registration; **renderer** (`db70beb`) — the block shell (prompt, focusable `role=application` canvas, aria-live feedback) + additive data-attrs (`data-graph-config`/`-answer-key`/`-kit-src`, threaded from `calculatorKitUrl`, omitted on print) + screen/print CSS; **kit widget** (`f949c33`) — `board.ts` `createPointAnswerBoard` (fixed-window, snap-to-grid draggable point, arrow-key nudge + Shift-fine, clamped) + pure `isPointCorrect` scorer + `mountGraphQuestion` entry (dynamic-imports the board so JSXGraph stays lazy) — **browser-verified at `/dev/graph-question`** (render, keyboard nav, position narration, tolerance scoring, lock, restore; fixed a real bug where `JSXGraph.initBoard` clobbers the container's `role`/`aria-label`); **runtime** (`d3fd35a`) — init walks the block into a `GraphRef`, the `graphs.ts` sidecar `import()`s the kit + mounts + bridges moves into `state.graphs`, checkpoints score each graph as one unit (unanswered = omission), submit gathers `graphResponses` + folds it into the score (payload v3), `STORAGE_SCHEMA_VERSION 3→4` persists graphs, render owns the block chrome (feedback/result/solution/confidence/lock), `ingest-submission` now enforces v3. Runtime bundle **19.7 KiB** (within the 20 KiB target — getting tight). Verified: jsdom spine tests on real renderer markup + the widget browser pass; the deployed published-page path was author-verified live 2026-07-10. **Slice-1 follow-ups landed:** N-handle multi-point — the widget grows one draggable handle per correct point, Tab cycles them (releases focus at the ends), scored consume-once ("plot ALL," like blank groups); replaces the ambiguous either-root simplification (`75ddbc9`, browser-verified 2-handle). Dashboard now renders `graphResponses` — a "Graph questions" table showing plotted point(s) vs the answer key (`4364039`). **Slice 2 — editor NodeView SHIPPED (`f7d3dd8`):** the block is now authorable in the real builder (slash-menu "Interactive graph"). Built B-shaped — an interaction picker + progressive-disclosure "Advanced settings" so `plot_line`/`shade_region` slot in additively. `mountGraphAuthor` reuses the SAME kit board in author mode (the teacher drags handles → `correctPoints`, "what the teacher sees is what the student gets"); `InteractiveGraph` Tiptap node with an editable-prompt `NodeViewContent` + structured attrs; `serialize.ts` round-trips both directions (replaces the omit-from-editor guard). Browser-verified at `/playground` (insert → author → live answer readout); serialize round-trip + schema-valid output unit-tested. Fixed a nested-focusable a11y bug (host + inner board both had `role=application`). **Coordinate-system taxonomy decided (future-proofing):** number lines (1D, K-8) and stats charts (histogram/box/dot) will be SEPARATE block families (`number_line`, `data_plot`) reusing this same block→kit→pure-scorer template — NOT forced under `interactive_graph`'s 2D AxisConfig. **2.7b plot-a-line SHIPPED (`1768087`):** a `plot_function` interaction — the student places two points and the widget fits + draws the line through them, scored on slope/intercept (per-parameter tolerances). Fully additive (no version bumps). **Future-proofed for quadratic/exponential/logarithmic:** built around a `family` (discriminated union) + the EXISTING regression fit engine (`fitLinear`, …), so growing 2 points (line) → 3 (parabola) is one schema member + one `fitX` call. board.ts draws a live curve through the handles; the editor picker gained "Plot a line" (drag two handles → slope/intercept re-derived live). Browser-verified at `/playground`. **2.7c shade-region SHIPPED (`bc75b72`):** the student drags a polygon's vertices to cover a target region (shaded live); scored by area overlap — **intersection-over-union ≥ minOverlap** — via `pointInPolygon` (ray cast) + `polygonOverlap` (100×100 grid sampling, no clipping dependency, pure + testable). board.ts draws a filled polygon through the handles; editor picker gained "Shade a region" (drag 3–6 vertices → correctVertices; minOverlap in advanced). Browser-verified. **The graph block now offers four interactions: plot a point, plot a line, shade a region, and DISPLAY (static, ungraded).** **Graph-as-stimulus — DISPLAY half SHIPPED this session (not yet committed by author):** a `display` member of the `GraphInteraction` union carrying `drawables: Drawable[]` (a discriminated union on `kind`: `point`, `curve` [reuses `FunctionModel` → quad/exp/log light up here AND in `plot_function` at once], `segment`, `polygon`; `label` text-drawable deferred/YAGNI). Additive → **no schemaVersion bump, no `GraphResponse` variant**. Renders a **read-only figure** via `createDisplayBoard`/`mountGraphDisplay` (JSXGraph, no handles/drag/pan, `role="img"`); the renderer emits it as `data-block-category="content"` with `data-graph-drawables`, **pulls no problem number** (`isNumberedBlock` false), and the runtime mounts it through a thin `wireGraphDisplays` sidecar that keeps it OUT of `graphs`/`graphBlockIds` (never scored, never in the submit payload) — so a standalone exemplar with an empty prompt falls out for free. Editor: the Type picker gained "Display (static graph)" + a live read-only preview + a per-drawable numeric list editor (add/edit/remove point/line/segment/polygon; dragging-to-place is a future enhancement); slash-menu gained a first-class "Static graph" insert. Decided (per author): display is a **mode of `interactive_graph`**, not a separate block family — so reuse across features compounds. **(The "answer surface as a graph-block field" seam sketched here was RETIRED 2026-07-10, author call — graph + typed/MC answer composes as a display graph + a sibling question block instead; see the Status-by-area row.)** Fully test-covered; the editor preview board + per-drawable list editor UI are now BROWSER-VERIFIED (2026-07-12 `/playground`: Static-graph insert → live preview board renders; drawable rows expose numeric inputs + open/dashed/**arrows** checkboxes + label + remove + freeform add + "+ polygon" + advanced). Remaining browser-UNVERIFIED surface: the deployed R2 display path only (same class as the graded R2 path). **(Graded quad/exp/log families themselves SHIPPED `38b9fbe` 2026-07-10 — see the Status-by-area "Graded function families" row; the `family` scaffold + fit engine that made them cheap are fully wired.)** **Next candidates on this template:** the `number_line` (1D, K-8) and `data_plot` (histogram/box/dot) block families reusing this same block→kit→pure-scorer shape. Decisions in [docs/design/calculator-tool.md](docs/design/calculator-tool.md). Suite: schema 151 / graph-kit 97 / renderer 323 / app 266 (837).

**▶ Phase 2 — Inline vocabulary definitions SHIPPED (code complete + browser-verified; `publish-activity` redeployed 2026-07-06 — reaches a page on its next re-publish).** Teacher selects text → "Define" → types a definition; the published page shows it in a popover. Four slices, all green: **schema/renderer** (`2329580`) — marks moved from a string enum to an object discriminated union (a `z.preprocess` upgrades legacy string marks on read, so stored activities keep parsing with no `schemaVersion` bump), `DefinitionMark { definition, glossaryKey? }`, renderer emits the escaped `<span class="definition" data-definition…>` (reserved `data-glossary-key` only when set), serialize round-trips both ways; **runtime** (`e26dea7`) — a self-contained ~1.7 KiB `definitions.ts` sidecar (mirrors the reference-panel sidecar; inlined only when a page has a definition) opens a popover on click/tap/Enter/Space, Esc/outside/scroll closes, focus returns; **editor** (`16e0470`) — `Definition` mark + "Define" toolbar button + a single-host `DefinitionPopoverHost`/`DefinitionEditPopover` (mark-based via `getMarkRange`; commit-on-close, a blank draft removes the mark). Browser-verified on `/playground`: apply→popover→type→commit, re-open prefilled, Remove keeps the word, dotted-underline cue (computed-style confirmed). **Rich-content upgrade (`dc55271`):** definition content went from a plain string to formatted text + inline math + one optional image — the popover now uses the shared `InlineRichTextEditor` (the hint control) + a compact image control (URL paste, or upload via `upload-image` when an activityId is present); the renderer pre-renders the content into a hidden `<template class="js-definition-content">` the sidecar clones into the popover (`data-definition` kept as a plain-text fallback); the schema's content type forbids nested definitions and a `z.preprocess` upgrades the legacy string. Browser-verified: rich editor + image control, URL→preview→commit, image-only definition, re-open prefills content + image. Reasoning in [docs/design/vocabulary-definitions.md](docs/design/vocabulary-definitions.md). **Phase 4 (future):** the account/tenant-scoped glossary store + implicit-reuse + auto-suggest (same doc). Tests: schema 101 / renderer 290 / app 245; typecheck clean.

Earlier (done + deployed): Phase 2 **reference panel** live on published pages (author-confirmed 2026-06-18; narrative archived in HISTORY/DECISIONS); the redundant-`Underline`-extension cleanup (`3aaf139`, 2026-06-19). Other Phase 2 "decide at phase start" items still open: image-hosting quota, matching/ordering UI (MC shipped 2026-07-10), block-type priority, `skills` editing UI, custom R2 domain.

Recently completed (newest first; durable detail in [DECISIONS](docs/DECISIONS.md)/[HISTORY](docs/HISTORY.md)):

- **Sin-curve continuation-arrow tangent fix** (2026-07-12, graph-kit-only; `9a68c0e`) — display-graph `expression` drawables drew continuation arrowheads from a two-sample chord near the window edge; on an oscillating curve (sin(3x)) that chord straddles a local extremum between adjacent samples → near-flat direction while the true tangent is steep (was ~65–69° off). Smooth model curves (line/quad/exp/log) don't curve enough between samples, hence only the sampled-`expression` path looked broken. Fix: derive the heading from the LOCAL slope at the exit via a small central difference (`outwardDir` in [display-arrows.ts](packages/graph-kit/src/display-arrows.ts)), oriented outward by the end's x-sign; chord kept as fallback for the log-asymptote case (fn non-finite one side). Model-curve arrows unaffected. Regression test added (graph-kit 280→281); browser-verified at `/dev/graph-question` (new `sin-expression` scenario — arrows track the exit tangent to 0.1°). Committed + DEPLOYED (kit `graph-kit-E6G7VLZK.js` uploaded to R2, manifest committed `8d55fef`, `publish-activity` redeployed 2026-07-12). Live-page eyeball optional — the change is display-only and dev-harness-verified.

- **Graded ray/segment answer field accepts curve+domain formulas** (2026-07-12, app-only, no deploy artifact) — bug: a graph question already set to a ray/segment couldn't take an equation like `x^2 {-1<x<1}` — its answer field only understood `ray …`/`segment …` command syntax + two bare points, so a curve formula errored with "start with ray or segment." The `plot_function` field handled it fine; the static-display add box (`drawablesFromFreeform`) handled everything. Fix: extracted the `plot_function` branch's curve-routing into a shared pure `routeCurveFormula(raw, prevModel?)` in [boundedCurveLogic.ts](packages/app/src/editor/nodeViews/boundedCurveLogic.ts) (family+bounds → interaction: nonlinear+domain = bounded plot_function; linear+domain = ray/segment; bare = full curve; coords/inequalities steered to their own types with teacher-safe messages), and made the ray/segment branch fall through to it (command words first, then formula — mirroring the static-graph router). Both graded fields + the display add box now route identically; the `{…}` clause normalizes to the ` for …` display form for free (answerText already uses `formatCurveDomain`). App-only (editor NodeView) — no schema/renderer/runtime touch, so no bundle/redeploy. 7 new `routeCurveFormula` unit tests (app 445 → 452); typecheck + lint green. Browser GUI drive left for author (editor surface resists synthetic events). Committed `5167adf`.

- **Doc-drift review + add-a-block-type guards** (2026-07-10, docs-only + tests, no deploy) — full documents-vs-code drift audit; fixes committed (`ea4d8ad`: stale STORAGE_SCHEMA_VERSION in CLAUDE.md, stale RUNTIME.md constants/bundle sizes, both Phase 2.7 design docs marked SHIPPED with as-built deltas, deploy-ordering rules promoted to CLAUDE.md + functions README). Then (`0241689`) three structural guard tests so the MC-in-columns class of omission (3ffb6d4) fails loudly: schema ColumnCellBlock/Block union parity; editor column-cell acceptance checked against the real ProseMirror schema (enabled by lifting the extension list to `editor/editorExtensions.ts`, behavior-preserving, playground-verified); dashboard index parity inside columns across all ActivityIndex maps. README's add-a-block-type checklist expanded to the full as-built surface; ROADMAP annotation sweep (MC/vocab/2.7 marked shipped, superseded architecture sketches corrected).

- **Order-independent blank groups** (2026-06-17; deployed + verified live on a published page 2026-06-18) — a blank can be flagged `interchangeableWithPrevious`; the renderer compiles adjacent runs into a shared `data-blank-group` id and the runtime scores each group with consume-once bipartite matching (each correct answer satisfies one blank; document-order tiebreak; grouped blanks resolve at check/submit, not immediate-mode blur). Authoring: a popover checkbox (hidden on a block's first blank) + a ⇄ chip cue; markdown import via a leading-tilde marker (`{{~3}}`); dashboard drill-down shows "x or y (any order)". Client-authoritative scoring, so no Edge Function change. Schema 89 / renderer 268 / app 232 green. DECISIONS → "Order-independent blank groups".
- **E2E locked-mode + dashboard** (2026-06-17) — author-confirmed working. Locked-mode publish → check freezes the checked section's inputs + check button, solution reveals, score shows; submit → row appears in the Submissions dashboard with per-blank answer-key/student detail. Prep was code-side de-risking (`seed-e2e-locked.sql` validated against the known-good free seed; freeze path traced + unit-covered). Completes the End-to-end manual test item and unblocked the legacy-bucket cleanup (since done — bucket deleted 2026-06-18).
- **CI branch-filter narrowing** (2026-06-17) — CI's `push` trigger limited to `main` (was unfiltered, firing on every branch). Feature branches now get a single PR run instead of double-firing on push + PR; merges/direct commits to `main` still run. `pull_request` still covers all PRs.
- **Deferred test coverage** (2026-06-13) — `init.test.ts` now covers `state.blanks`/`state.blocks` (one defaulted entry per ref, empty when none). The blank-edit popover's draft/commit/close decisions were extracted to a pure `blankPopoverLogic.ts` (behavior-preserving lift out of `BlankEditPopover`/`BlankPopoverHost`) and unit-tested (`blankPopoverLogic.test.ts`, 25 cases — `computeFlush`, `resolveAnswerBlur`, `resolveAcceptableCommit`, `filterFeedbackForCommit`, `isSameBlankSelection`). Refactor browser-smoke-verified: edit commits on outside-click close, whitespace stripped, selection released.
- **Foldable × columns verification** (2026-06-13) — a top-level columns container flows whole through the foldable, never splits, `fr` tracks resolve against the fixed panel width. Tests + dev-only `/dev/foldable-columns` bench. DECISIONS → "Structural columns + variable block sizing".
- **CI + housekeeping** (2026-06-13) — `.github/workflows/ci.yml` (typecheck → lint → test → build → bundle-staleness guard, on push + PR); app `typecheck` + root `lint` scripts; `packageManager: pnpm@11.1.2` pin; pre-existing lint errors cleared (now 0 errors / 2 intentional warnings).
- **Variable block sizing** (2026-06-12/13) — COMPLETE + deployed (publish-activity v34). DECISIONS → "Structural columns + variable block sizing"; design in [docs/design/variable-block-sizing.md](docs/design/variable-block-sizing.md).

Standing follow-up: none — the image resize drag-handles + the other real-mouse GUI passes were confirmed working by the author (2026-06-16).

Also queued:

- **Phase 1 polish:** custom domain for R2.
- **Free activity catalog (design captured 2026-06-16, target Phase 2):** public catalog of free first-party math activities a newcomer can run as-is — a cold-start lever pairing with self-signup ([docs/design/free-activity-catalog.md](docs/design/free-activity-catalog.md)). The Phase 5 marketplace's free/discovery slice pulled forward; "use" = run-in-place read-only (not clone-to-edit). Phase 2 slice = browse + run/assign/print; consumer submission dashboards depend on Phase 3 assignment scoping.
- **Vocabulary glossary — Phase 4 (inline definitions shipped 2026-06-19; see Current focus).** Remaining piece is the account/tenant-scoped `glossary_entry` store + `glossaryKey` resolution at publish, leaning to the light **implicit-reuse** model (platform remembers a teacher's definitions and offers them back; an official managed glossary, if built, is opt-in, never forced) + the suggest-don't-apply **auto-suggest** editor aid. Both are additive to the shipped mark (the `glossaryKey` attr + `data-glossary-key` are already reserved). Design in [docs/design/vocabulary-definitions.md](docs/design/vocabulary-definitions.md).
- **Long-term OCR/AI features (design captured 2026-06-16, not scheduled):** PDF → activity import ([docs/design/pdf-import.md](docs/design/pdf-import.md)) and photo-upload answer checking ([docs/design/photo-grading.md](docs/design/photo-grading.md)). Both reuse existing pipelines (markdown-paste DSL converter; the submission pipeline); decisions locked: generic-master QR, teacher-grading-aid v1, transcription-only AI rule. Photo-grading flags one real refactor — make answer evaluation server-shareable.
- **Calculator tool / graphing track (design captured 2026-06-19, target Phase 2.7):** a teacher-configurable, Desmos-style calculator (scientific → single graph → regression → multi-expression), self-built (Path B) on a shared **graphing kit** (JSXGraph + MathLive + an evaluator) that the graded interactive-graph block (existing 2.7 design) also consumes — one track, two faces. Ungraded scaffold (reference-panel sibling: no submission, no answer key, no schema bump); lazy-loaded on click; per-activity teacher restriction flags with inline author preview. Texas Algebra I regression (linear/quad/exp) pulled forward to stage 3. Design in [docs/design/calculator-tool.md](docs/design/calculator-tool.md); folded into ROADMAP Phase 2.7.

## Status by area

| Area | Status |
|---|---|
| Stages 9–16 (schema, renderer, runtime, editor, publish flow, submissions dashboard) | ✅ Complete; live-verified |
| Database migrations 0001–0006 | ✅ Applied; RLS verified |
| Migration 0007 (submission version pinning) | ✅ Applied (verified 2026-06-12) |
| Migration 0008 (`soft_delete_activity` RPC — delete bug fix) | ✅ Applied 2026-06-17; delete verified working by author |
| Print feature Drops A–D (worksheet config + journal foldable) | ✅ Complete; entirely client-side, no deploy needed |
| Structural columns + grid lines + width presets + drag-between-cells | ✅ Complete; deployed |
| Variable block sizing (Drops 1/3/4: width+align, image height/crop, cell work-space) | ✅ Complete + tests; deployed (publish-activity v34); Drop 2 column-drag cancelled by author (presets remain) |
| Editor UX: sticky toolbar + popover scroll-jump fix | ✅ Browser-verified |
| Phase 2 slice — rich text + inline math in hint/feedback/solution | ✅ Deployed; live UX unverified |
| Image authoring (block + popover + upload + live preview) | ✅ Deployed; R2 cross-origin `<img>` confirmed 2026-06-16 |
| `publish-activity` / `ingest-submission` Edge Functions | ✅ Deployed; ingest runs with `verify_jwt: false` |
| Cloudflare R2 hosting (published HTML) | ✅ Live |
| Auth (Google OAuth, allowlist) / React app / editor stack | ✅ In place |
| CI (GitHub Actions: typecheck/lint/test/build + bundle-staleness guard) | ✅ Added 2026-06-13; verified green locally |
| Markdown paste import | ✅ Complete + verified end-to-end by author (multiple smoke tests); app-only, no deploy. Format spec + Copy AI prompt + drift guard shipped |
| End-to-end manual test | ✅ Free-mode (2026-06-16) + locked-mode/dashboard (2026-06-17) both author-verified end to end |
| Order-independent blank groups | ✅ Complete + deployed (`publish-activity` redeployed 2026-06-18); grouped scoring + `{{~}}` markdown import verified live on a published page |
| Inline vocabulary definitions (rich text + math + optional image; renderer template + runtime sidecar + editor authoring) | ✅ Code complete + browser-verified (`/playground`); `publish-activity` redeployed 2026-07-06 — reaches a page on its next re-publish |
| Calculator tool (Phase 2.7) | ✅ Stages 1–4 + full graphing UX overhaul (floating panel, in-panel MathLive keyboard, splitter, hover-trace, sticky axes, calc results) — all browser-verified at `/dev/calculator` ([HISTORY 2026-07-06](docs/HISTORY.md)). Re-hashed kit (comma key + graph widget + display board) uploaded + `publish-activity` redeployed in the 2026-07-08 full deploy pass — live. |
| Activity-wide typography (`meta.typography`: font menu + base size; R2-hosted WOFF2; drawer control; editor/print/foldable WYSIWYG) | ✅ Built + browser-verified + DEPLOYED 2026-07-08 (fonts on R2, publish-activity + app live). Live-page font check cleared 2026-07-10 |
| Number-line block (`number_line`, 1-D graded) | ✅ CODE COMPLETE (all 6 slices; 2026-07-12) — schema+wire-v7+ingest `b5fe4aa`, renderer+print-SVG `b5fe4aa`, kit scorer `592c15b`, kit widget `31de219`, runtime bridge (numberLineExt + storage v8 + wire v7) `565ca6d`, editor NodeView + serialize `5a551d6`, dashboard table `9ea33e4`. Browser-verified: dev harness, a bundle-rendered page (plot→check→score→restore→submit), and `/playground` authoring. ✅ DEPLOYED 2026-07-12 (kit `WVG7FA6Y` on R2; `ingest-submission` wire v7 `verify_jwt:false` v34; `publish-activity` v85). Live-page eyeball optional (see Current focus) |
| Data-plot block (`data_plot`, statistics charts) | ✅ CODE COMPLETE (all 6 slices; 2026-07-12) — schema+wire-v8+ingest-3–8 `96e3da6`, renderer+print-SVG `96e3da6`, kit scorer+SVG board+mount `ee9a5bc`, runtime bridge (dataPlotExt + storage v9 + wire v8 + variant scan) `e2f8a39`, editor NodeView+data-table+serialize `39c26c6`, dashboard table `bd26f19`. Slice-1: `display` (dot/histogram/box static stimulus) + graded `build_dotplot`. Browser-verified: `/dev/data-plot`, a bundle-rendered page (build→check "1/1"→reload-restore, + static display box plot), `/playground` authoring (live preview + mode switch). Slice-1 ✅ DEPLOYED 2026-07-12 (kit `WXYPUC5W`; `ingest-submission` wire 3–8 `verify_jwt:false` v35, MCP-verified; `publish-activity` v86). **GRADED BUILDS `build_histogram` + `build_boxplot` ✅ DEPLOYED (5 GB slices `e6bb062`→`913744d`, 2026-07-12) — additive, NO wire/storage bump; bundle-rendered-page-verified "2/2 correct"; kit `V6T6HAUS`, `ingest-submission` v36 widened-union `verify_jwt:false` (MCP-verified), `publish-activity` v88.** |
| Interactive-graph block — Stage 5 | ✅ Four interactions: plot-a-point (spine `db70beb`/`f949c33`/`d3fd35a` + N-handle `75ddbc9` + dashboard `4364039` + editor NodeView `f7d3dd8`), plot-a-line (`1768087`), shade-region (`bc75b72`), and static-display. All DEPLOYED + live-verified (2026-07-10 author pass). The "answer surface as a graph-block field" seam was RETIRED (author call 2026-07-10) — graph + typed/MC answer composes as display graph + question block instead |
| Numeric blanks (`answerType: 'numeric'` + tolerance; strategy seam; editor checkbox; `{{=…}}` import) | ✅ Live (deploy train cleared 2026-07-10); browser-verified (`1a997fd`); no wire change |
| Multiple-choice block (schema + wire v5 + renderer + runtime + editor + ```mc import + dashboard) | ✅ Live (ingest v5 + publish-activity + app deployed, cleared 2026-07-10); browser-verified at `/playground` (`c334b96`/`dce312a`/`bb96f7d`/`7a8584d`) |
| Static graph-paper SVG fallback (author's print fix) | ✅ Live (cleared 2026-07-10); committed in `c334b96` |
| Graded function families — quad/exp/log + quadratic inequalities (seeding fix, "Plot a function" picker, quadratic classifiers, dashboard fitted equation, dev-harness scenarios) | ✅ Live (deploy train cleared 2026-07-10); browser-verified at `/dev/graph-question`; `38b9fbe` |
| Display-graph continuation arrows (window-exit placement, per-drawable toggle, print-SVG parity) + readDrawables ray/expression drop fix + print ray-arrowhead fix | ✅ Live (same train, cleared 2026-07-10); browser-verified; `131f62d`. Drawable-row arrows checkbox now FULLY VERIFIED (2026-07-12 `/playground`: toggling "arrows" off flipped the checkbox, wrote `arrows: false` into the drawable's document model, and removed the arrowheads from the live preview) — no longer pattern-only |
| MC choice figures (per-choice image/graph, 2-up grid, editor figure panel, shared DrawableListEditor, `![alt](url)` mc-fence import) | ✅ Live (`publish-activity` redeployed, author-confirmed 2026-07-10 sixth session) |
| Matching + ordering question types (schema + wire v6 + storage v7 + pointer-drag/keyboard runtime + editor NodeViews + ```match/```order import + dashboard tables) | ✅ DEPLOYED (wire-v6 train, author-confirmed 2026-07-10 sixth session); browser-verified pre-deploy on a bundle-rendered page. Pointer-DRAG (transform follow, midpoint swaps) on a real mouse/touch device — author-confirmed working 2026-07-12 |
| Calculator parity Tier A (calc inequalities + gate flag, per-row domains, widened function table, display-curve inequality authoring + round-trip, importer inequality domains, logarithmic regression end to end) | ✅ DEPLOYED + author-confirmed 2026-07-11 (kit `graph-kit-AG2PA3TB.js` + publish-activity + app). NOTE: the kit has re-hashed several times since as later fixes shipped; the manifest now points at `graph-kit-E6G7VLZK.js` (sin-curve arrow fix), uploaded + `publish-activity` redeployed 2026-07-12. Older hashes stay live on R2 for already-published pages. Movable points + Σ/Π dropped by author call |
| Migration 0009 (security/performance housekeeping: view `security_invoker`, RPC EXECUTE lockdown, helper `search_path`, initplan policy rewrites, FK covering indexes) | ✅ APPLIED + fully verified 2026-07-11 (author applied; Claude ran the 8-step read-only verification via Supabase MCP — all passed; app smoke author-confirmed; advisor re-run clean but for the intentional residue). DB-only: no app/renderer/Edge-Function code change |

Test counts at last session: schema 244 / graph-kit 325 / renderer 516 / app 468 (1553 total; +data_plot graded-builds histogram/box scorer/gather/serialize/index tests); typecheck + lint (0 errors) green. Runtime bundle **base 37.5 KiB / graphs variant 44.3 KiB** minified (data_plot + its graded builds; graphs over the 40 KiB soft target — under the 60 KiB ceiling, accepted; the histogram/box widgets ride the lazy kit, not the inline bundle) (matching + ordering added ~15 KiB of interaction machinery — growth accepted by author call, decision 10 in the matching/ordering design doc; that growth triggered the 2026-07-10 budget AMENDMENT: 40 KiB target / 60 KiB ceiling, minified — reasoning in DECISIONS.md → "Runtime size budget amendment"). The four near-identical per-question-type confidence/solution-reveal loops were DEDUPED 2026-07-11 into one shared `renderSolutionAndConfidence` helper in `render.ts` (−0.5 KiB per variant; behavior-preserving, covered by the existing render/confidence jsdom tests) — `publish-activity` redeployed (author-confirmed). **Follow-up same day:** the confidence-radio locking asymmetry that dedupe surfaced was corrected across ALL FIVE question types — blank/MC (`render.ts`, `locked` now a required helper param) AND graph blocks (`graph-kit/src/runtime.ts`, same `section.locked` condition as the widget's own `setLocked`) now freeze their confidence radios in locked mode, matching what matching/ordering already did. Tests pin it (renderer 461 → 463, graph-kit 279 → 280). Shipped kit-first (`upload:graph-kit` → `deploy:publish`; the graph half re-hashed the kit → `graph-kit-P3IFHSRV.js`) — DEPLOYED + author-confirmed 2026-07-12. Remaining structural lever if a variant trends toward 60: per-type lazy chunks. The graph-runtime-into-kit lever was PULLED 2026-07-10 — graph work grows the cached kit, not the inline bundles. (Environment note: this machine is the 2026-07-04 recovery clone — old computer died; toolchain re-set-up verified by a full green run. Kit-upload creds live in a gitignored `.env.r2`, auto-loaded by `pnpm upload:graph-kit` — the upload is now an explicit command; `pnpm build:graph-kit` is build-only and never touches R2 (2026-07-10 DX-audit split); see `.env.r2.example`.)

## Repo layout

```
activity-platform/
├── docs/
│   ├── design/        — feature designs captured ahead of implementation
│   ├── DECISIONS.md   — architecture decisions + reasoning
│   ├── HISTORY.md     — archived completed-work logs
│   └── COLLABORATION.md — working-with-the-author notes
├── packages/
│   ├── schema/        — Zod types, document model, factories
│   ├── renderer/      — pure JSON → HTML string; KaTeX inlined; no DOM
│   │   ├── RUNTIME.md — runtime architecture + data-attribute contract
│   │   └── src/runtime/ — published-page runtime (DOM TS, own tsconfig; per-file map in RUNTIME.md)
│   ├── graph-kit/     — @activity/graph-kit: shared graphing kit (Phase 2.7). evaluate.ts (math.js seam) + calculator.ts (mountCalculator). mathlive + mathjs. Lazy-loaded on published pages; imported directly by the editor preview
│   └── app/           — Vite + React 19 + TS + Tailwind v4 + React Router v7
│       └── src/
│           ├── editor/     — Tiptap extensions, NodeViews, popover hosts, toolbar, slash menu
│           ├── components/ — PublishControl, ImportMarkdownDialog, RequireAuth
│           ├── routes/     — Activities, ActivityEditor, ActivityPrint, Submissions, Playground (dev-only)
│           ├── lib/        — serialize.ts, submissions.ts, uploadImage.ts, markdownToTiptap.ts, markdownImportPrompt.ts, foldable/ (print engine)
│           └── __tests__/
├── supabase/
│   ├── migrations/    — 0001–0008 (all applied)
│   └── functions/     — publish-activity, ingest-submission, upload-image, _shared/ (cors.ts, renderer.bundle.js — auto-generated)
├── scripts/           — bundle-renderer.mjs; seed-test-data.sql; seed-e2e-activity.sql; seed-e2e-locked.sql
└── ...root configs
```

## Key constants

- **GitHub repo:** `ZanReed/activity-platform`
- **Supabase project ref:** `dtqutpdplefmufrrakxs`
- **Storage bucket (legacy):** `activities` — deleted 2026-06-18 after R2 verified end-to-end (note: the app's `.from('activities')` calls are the DB table, not this bucket)
- **Auth:** Google OAuth via Supabase. Site URL `http://localhost:5173` for dev. Allowlist-only signup (Phase 1).
- **Client env:** `VITE_PUBLISHED_URL_BASE` in `.env.local` (gitignored) mirrors the write-only `R2_PUBLIC_URL_BASE` secret; unset → published-page links hide.

## Open questions / deferred decisions

- ~~Graph kit pulls MathLive on graph-ONLY pages~~ — RESOLVED 2026-07-10: `mountCalculator` on the entry is an async wrapper; MathLive/calculator is its own lazy chunk (network-verified: no MathLive fetch on a graph-only page). Cost accepted: +1 RTT on first calculator open.
- ~~Runtime bundle budget: graph plumbing~~ — the lever was PULLED 2026-07-10 (graphs variant 27.8→23.8; plumbing now in the kit). Base is now **35.8 KiB / graphs 38.5 KiB** (numeric + MC + matching + ordering); budget amended 2026-07-10 to 40 KiB target / 60 KiB ceiling (DECISIONS.md), so headroom is restored — pull the dedupe/per-type-variant levers before any future bump. Keep the scorers pure + DOM-free so Phase 5 server-grading — which should strip `data-graph-answer-key`/`data-mc-answer` from question HTML first — is cheap.
- ~~Dashboard doesn't index question blocks inside columns~~ — FIXED same session (`3ffb6d4`): `buildActivityIndex` now recurses into column cells, and the regression test surfaced that `multiple_choice` was missing from `ColumnCellBlock` + the editor column content expression entirely (MC couldn't be placed in columns) — both added, `MultipleChoice` registered in `ReferencePanelEditor` (the drawer guard test caught it, exactly as designed).
- **Custom domain for R2** (Phase 2 polish): `pub-<hash>.r2.dev` → `activities.<brand>.com` once a domain is owned; ~5 min of Cloudflare DNS after that.
- **Empty fill_in_blank drag handle attachment** — whether `definingForContent: true` changed the handles-only-on-non-empty behavior is unverified; re-test during a drag-reorder pass. Minor.
- **Blank popover: one-click switch between chips** — deferred design decision, no data loss; full reasoning in [DECISIONS.md](docs/DECISIONS.md) → "Fill-in-blank authoring (Stage 13.5)". Needs a dedicated design pass (FocusTrap/selection entanglement).
- **Section metadata panel** — SectionBreakView's inline title/checkpoint UI is adequate for now; an editor-level panel remains optional.
- **Responsive `--blank-width` sizing** — deferred from Stage 11.
- **Phase 2 block-type priority order** (worked example, faded worked example, learning objectives, self-explanation) — decide when Phase 2 starts.
- **`skills` editing UI** — deferred to Phase 2 per schema scheduling; the field round-trips everywhere, only the editing control is missing. Don't add piecemeal without the per-skill-analytics scope.
- **UX validation with 2–3 other teachers** on the editor patterns before classroom adoption — cost rises sharply once students use activities.
- **Post-success edit edge case** — locked/single paths briefly write-then-remove the persistence blob (wasteful but correct). Low priority.
- **CDN-hosted shared runtime** (Phase 3+): trigger is when republishing for runtime bug fixes gets painful (~50+ active activities).
- **Multi-tenancy / governance when a teacher leaves a district** — Phase 4; helpers are designed for it.
- **Manual grading workflow shape** (Phase 2.6), **media storage/privacy posture** (Phase 2.8), **annotation coordinate space** (Phase 2.9) — each decided at its phase start.

---

**Last updated:** 2026-07-12 (data_plot GRADED BUILDS — histogram + box, all 5 GB slices, committed, NOT yet deployed). Design pass (both builds; box method LOCKED = TI-84 exclusive-median + tolerance; additive, NO wire/storage bump) → approved → built end to end: schema + `DataPlotResponse` union members `e6bb062`; pure `scoreHistogram`/`scoreBoxplot` + a bar-height board + a five-handle clamped board + `mountDataPlotQuestion` branch `58fbee6` (`/dev/data-plot`-verified: both render/build/score/restore); renderer per-chart shell + answer-key tolerance + runtime gather branch `2b32bce` (**bundle-rendered-page-verified**: histogram [2,3] + box [1,2,4,6,7] → "2/2 correct", zero console errors); editor Type picker + box TI-84 readout + tolerance field `19769c4` (`/playground`-verified); dashboard `formatBins`/`formatFive` `913744d`. All green (schema 244 / graph-kit 325 / renderer 516 / app 468 = 1553); bundle base 37.5 / graphs 44.3 KiB. **DEPLOY TRAIN COMPLETE:** kit `CWAMAF2L → V6T6HAUS` uploaded (shape-pills + histogram/box) + manifest committed `a0f2d9d`; `ingest-submission` redeployed for the widened union (v36, `verify_jwt:false`, MCP-verified) + `publish-activity` v88 (after ingest). Commits unpushed on main (author pushes → triggers the SPA deploy for the editor picker). Prior footer: 2026-07-12 (number_line ray/segment shape pills — parallel session fixed on main `4ab2dba` + deployed kit `WXYPUC5W → CWAMAF2L`, manifest `95e3991`, publish-activity redeployed kit-only). Prior footer: 2026-07-12 (data_plot slice 1 — display + build_dotplot, DEPLOYED: kit `WXYPUC5W`, ingest v35 wire 3–8 `verify_jwt:false`, publish v86, MCP-verified). Prior footer: 2026-07-12 (number_line block all 6 slices DEPLOYED, kit `WVG7FA6Y`). Prior footer: 2026-07-11 (migration 0009 security/performance pass — applied + MCP-verified).
