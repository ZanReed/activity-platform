# STATE.md

A living "where am I" snapshot. Update at the end of each work session — replace the relevant sections, don't append. Keep it under ~150 lines: move finished-work narratives to [docs/HISTORY.md](docs/HISTORY.md), durable reasoning to [docs/DECISIONS.md](docs/DECISIONS.md). Project rules live in [CLAUDE.md](CLAUDE.md).

## Pending author actions

Things only the author does (pushes, deploys, migrations), queued and waiting:

000. **Numbering/label decouple (Slices 1+2 + follow-ups) — SHIPPED + DEPLOYED + PUSHED 2026-07-20 (DONE, nothing queued).** (`pnpm deploy:publish` author-run; 5 commits `0142f89`→`80c7ea3` pushed.) (design: `~/.gstack/projects/ZanReed-activity-platform/user-main-design-20260720-015819.md`; `/office-hours` + `/plan-eng-review` CLEARED.) Every numbered question block (`fill_in_blank`, `multiple_choice`, `matching`, `ordering`, `number_line`, graded `interactive_graph`/`data_plot`) gains a per-block **`label`** (`auto`/`custom`/`none`): `auto` (default, **absent** — byte-identical round-trip, NO `schemaVersion` bump) numbers as today; `custom` shows authored text out-of-sequence; `none` shows no number (the notes keyword-blank) — **still graded + still in the results view** (suppression drops only the gutter, never gradeable markup). The numbering rule is now single-sourced in `@activity/schema` (`isPageNumbered`/`isPageNumberedType`/`pageLabel` + `isGradeable`), consumed by the renderer switch, `isNumberedBlock`, and the editor's `problemNumberAt` (was four hand-synced encodings); renderer gutter via one shared `renderNumberGutter`; editor via shared `labelNodeAttr` + `ProblemNumberGutter`/`labelPrefix` + a block-agnostic "Numbering" drawer control; serialize attaches/reads `label` centrally in the two dispatchers (`LABELED_BLOCK_TYPES`). Suite green: schema 320 / graph-kit 370 / renderer 640 / app 690; typecheck + build clean; renderer bundle regenerated + committed. Commits `0142f89` (fill_in_blank vertical) + `1813019` (all block types) + **Slice 2** (math_block). **Slice 2 DONE 2026-07-20:** `math_block` with Model A gaps is now a numbered problem on the page (gutter + consumes a slot; a plain display equation stays unnumbered, byte-identical path) AND its gaps are indexed in `buildActivityIndex` (keyed by placeholder id into the same blanks map) so they appear in the teacher results view instead of being orphaned; `math_block` also gained the `label` field for parity. Fixes the pre-existing defect where equation-gap answers rendered unnumbered and vanished from the dashboard. **AUTHOR: `pnpm deploy:publish` only** (renderer output changed — new pages honor suppression/custom + numbered equations; already-published pages pick it up on re-publish). NO ingest/wire/kit/storage change. **This deploy pairs with the Model A deploy train (#00): both touch the math_block renderer, so deploy them together.** **Follow-ups DONE 2026-07-20:** (1) sub-part lettering — a numbered multi-blank `fill_in_blank` shows `(a)/(b)` before each gap (shared `renderFillInBlankContent`; not for suppressed/custom/faded); (2) `math_block` worked `solution` — a post-check worked-explanation reveal that never leaks the gap answer (mirrors `fill_in_blank`; reuses the fill-in-blank solution-reveal machinery via a gated registration in the Model A init walk — no scoring impact, no storage bump). Still deferred (low value): `(a)(b)` lettering inside a multi-gap LaTeX equation; answer-in-gap reveal (deliberately not built — leaks the answer).

00. **Math blanks MODEL A (in-equation MathLive prompts) — SHIPPED + DEPLOYED + owner-eyeballed 2026-07-20 (DONE, nothing queued).** ([docs/design/math-blanks.md](docs/design/math-blanks.md) → "Model A — eng review resolution" + "MA-T7 authoring affordance"; `/plan-eng-review` + `/plan-design-review` both CLEARED.) A gap *inside* a rendered equation the student fills with a math expression, graded by the shipped Model B `mathEquivalent`. Architecture = **SWAP** (static KaTeX ↔ MathLive on kit load, mirroring the graph static-SVG↔board precedent); single-gap v1; ascii in a hidden mirror `<input>` reusing 100% of the blank machinery; NO wire/schema/storage bump. **All tasks DONE + green:** MA-T1 schema `prompts[]` · T2 renderer SWAP emission (`data-math-prompt-latex` + mirror inputs; the answer never leaks — `serialize()` empties the placeholders at publish, draft keeps them author-private) · T3 LaTeX↔ascii converter leaf · T4 kit MathLive mount · T5a init-walk registration (whole no-kit capture/score/gather/restore path, headless-verified) + T5b kit hand-off bridge · **T7 editor authoring — e2e-VERIFIED (real chromium, 6 tests):** insert-blank button + `⌘⇧B`, answer-in-gap WYSIWYG, reused `BlankEditPopover` in a trimmed `mathPromptMode` (equivalence/tolerance/also-accept, decoupled from the field's edit lifecycle), gap signifier, inline-view parity via shared `MathPromptControls` · **MA-D5 verdict sync:** `render()`→`renderMathPrompts(state)` pushes each gap's result+lock into its MathLive field (`setResult`→`setPromptState`), so a checked gap turns green/red + locks in-field · **MA-T6 self-host MathLive fonts on R2** (shared `configureMathLive` — also fixed a calculator-only bug; fontsDirectory derived from the kit URL, jsDelivr fallback in dev; `pnpm build:mathlive-fonts` uploads the 20 KaTeX woff2). Suite green: schema 311 / graph-kit 370 / renderer 629 / app 680 + 6 math-prompt e2e; typecheck+lint clean. **AUTHOR deploy train — RUN + DONE 2026-07-20:** `pnpm build:mathlive-fonts` (20 woff2 on R2) → `pnpm upload:graph-kit` → new kit **`graph-kit-XB5CUURV.js`** (manifest committed `c99eef3`; supersedes RWCPNEWM, carries MA-T6 self-hosted fonts + the calculator-bug fix) → `pnpm deploy:publish`. NO `ingest` (wire v9), NO storage bump. Owner eyeball on a live math-prompt page — PASSED (gap answered `a+a` for key `2a`: green-on-check + dashboard math render). Deferred: reveal-the-answer-in-gap (`math_block` has no solution-reveal trigger); N-gap per equation (schema-ready fast-follow). **Follow-up 2026-07-20 (app-only, rides the SPA — NO deploy, does not touch this deploy train):** the plain **fill-in-blank** block now has authoring parity with the math prompts — a focus-gated **`+ Blank`** button (mirrors `MathPromptControls`, in a `.fill-in-blank-block__chrome` footer shown only while the block is being edited) plus a faint power-user tip pointing at the `{{answer}}` shorthand; both the button and the **normalized `⌘⇧B` / `Ctrl⇧B`** shortcut (same chord as the math-field `insertPrompt`, now also a Tiptap keymap scoped to `fillInBlank`) insert an empty-answer blank at the caret and open its popover focused via a new `insertBlankAndEdit` command. The earlier `__`-input-rule idea was tried and **dropped** (didn't fire reliably in the app; superseded by the button/shortcut). e2e-verified (real chromium, `blank-signifier.e2e.ts` rewritten, 7 tests: chrome show/hide, button + `⌘⇧B` insert-and-focus, answer-persists, out-of-block passthrough, `{{}}` regression); app unit 684 + typecheck green.

0. **Math blanks (Model B) — DEPLOYED + PUSHED 2026-07-19; owner J1b eyeball PASSED 2026-07-20 (DONE, nothing queued) ([docs/design/math-blanks.md](docs/design/math-blanks.md) → "Eng review resolution"; eng-reviewed + outside-voice-hardened + design-resolved).** Teachers can now author a fill-in-blank whose answer is a **math expression**, graded by numeric-sampling equivalence (`2a ≡ a+a ≡ a*2`) via the lazy graph-kit. Full stack T1–T7 shipped: `mathEquivalent` in graph-kit (T1), schema `answerType:'math'`+`equivalence` (T2), runtime held-sync-reference `'math'` strategy + eager kit preloader + kit-fail tally exclusion (T3), jsdom integration + save/restore (T4, **no storage bump**), blank-popover 3-way Text/Numeric/Math selector + equivalence/tolerance (T5), Submissions KaTeX-render math answers (T6), blank-discoverability ghost-text signifier (T7). Suite: schema 302 / graph-kit 362 / renderer 610 / app 659 + 108 e2e, typecheck+lint clean. **AUTHOR-RUN + DONE 2026-07-19:** `pnpm upload:graph-kit` (kit now exports `mathEquivalent`; manifest committed) → `pnpm deploy:publish` (new pages carry the math-blank runtime + `data-answer-type` emit); arc pushed. NO `ingest` redeploy (wire v9); NO `STORAGE_SCHEMA_VERSION` bump. **Owner J1b eyeball — PASSED 2026-07-20** (author-verified live: math blank answered `a+a` for key `2a`, correct + dashboard math render). (Model A = in-equation MathLive prompts is a separate arc — see Pending #00.)


_(2026-07-19: former Pending #1 (graph-kit board dark-theming — `pnpm deploy:publish`), #2 (push the unpushed board-dark/color-pass commits), and #3 (`supabase db push` for `0012_restore_activity`) are all AUTHOR-RUN + DONE. Nothing queued from that batch. Historical detail retained below for one session, then archive to HISTORY.)_

1. **[DONE 2026-07-19] Graph-kit BOARD dark-theming — DEPLOYED (`pnpm deploy:publish` author-run).** ([docs/design/graph-kit-board-dark.md](docs/design/graph-kit-board-dark.md); eng-reviewed).** The graph/number-line/data-plot BOARDS now render dark in the editor (were light-on-dark). Boards SELF-DETECT theme from their container's `color-scheme` (no caller plumbing — D2); the calculator opts out (light unit — D3); a live toggle remounts + re-colors via a new `useEffectiveTheme()` (D4); **light is pixel-identical** (only dark overrides — JSXGraph defaults left untouched). Slices `6879e2a` (detectBoardTheme + boardColors + pins) · `2a9ed9f` (interactive graph board) · `b4661e7` (number-line + data-plot boards) · `89d7e60` (editor reactivity). Verified in-browser (self-detect via visProp, live-toggle remount, `/dev/data-plot` dark screenshot, calculator light); 350 graph-kit + 656 app tests green. **The editor DATA-PLOT preview also themed** (`d5a9afb`): it uses the RENDERER (`renderDataPlotSvg`), so that SVG now emits structural colors as `style="stroke:var(--gk-board-*,lightHex)"` and editor.css defines `--gk-board-axis/label/ink` (light-dark) — editor preview goes dark (verified #94a3b8/#cbd5e1 dark, #64748b/#475569 light), published falls back to light until published-dark. Renderer bundle regenerated (`bundle:renderer`, committed). **AUTHOR: `pnpm upload:graph-kit` DONE — kit `graph-kit-NNS6KNUS.js` live, manifest committed. NOW just `pnpm deploy:publish`** (one redeploy: picks up the new renderer bundle for the data-plot preview + points publish-activity at the NNS6KNUS manifest). Published output value-identical in LIGHT (var fallbacks == old hex); NO ingest/wire/schemaVersion change. **Unpushed commits to push:** the whole board-dark arc + `d5a9afb` renderer preview + the prior color-pass manifest `850ceca` + dev-route item 3 `7d0a00a`.
2. **Graph-kit color pass — DEPLOYED + LIVE 2026-07-19 (done; [docs/design/graph-kit-color.md](docs/design/graph-kit-color.md); eng-reviewed).** Consolidated graph-kit's 145 color literals → 60, all in TWO source-of-truth files (new [graph-colors.ts](packages/graph-kit/src/graph-colors.ts) 52 — board roles + series palettes + `GK_CHROME`; drawable-palette.ts 8, untouched). Slices `c473bd0` (module + 3 value-identity pins) · `09db977` (board dedup — killed ANSWER_COLOR×3 / AXIS_COLOR×2, aliased imports) · `84bb1cc` (chrome sweep — calculator KIT_CSS → `var(--gk-*)` single-sourced from `GK_CHROME`, **dark-READY** for a later published-dark re-point; question inline styles → `GK_CHROME`). Value-identical (colors unchanged): 348 graph-kit tests green; `/dev/*` browser-verified. **Owner-run + LIVE:** `upload:graph-kit` → new kit **`graph-kit-NXZNIEUK.js`** (manifest committed `850ceca`) → `deploy:publish`. NO ingest/wire/schemaVersion change; already-published pages keep the old hash, new publishes get the new one. Dark mode for graph-kit / published pages stays DEFERRED (runtime board theming + styles.ts).
3. **Dark mode (teacher chrome + editor) — SHIPPED + PUSHED 2026-07-18/19 (app-only, done).** 5 slices (`d1c2443`→`c1995a3`) + doc `93bc6b0` + dev-route item 3 (`7d0a00a`, unpushed) ([docs/design/dark-mode.md](docs/design/dark-mode.md); eng-reviewed, D1–D7). `light-dark()` on every `@theme`/`--ed-*` role, `color-scheme` picks the side (system pref + bottom-left System/Light/Dark toggle + pre-paint FOUC guard), `@media print` forces light. Editor `.tsx` got the fix-6 literal sweep it was missed by (134) + the 5 Dev benches (item 3, `var(--color-*)`). New `e2e/dark-contrast.e2e.ts` (6 tests, WCAG AA across the dark ladder — caught + fixed dark accent-strong 3.7:1). 656 unit + 6 e2e green. **Two PRE-EXISTING issues (flagged as task chips, NOT dark-mode's) — ADDRESSED 2026-07-19 (author): light `text-success` sub-AA (was 3.3:1); the 4 red `number-line-formula.e2e.ts` tests on `main`.**
1. **[DONE 2026-07-19] Activities delete-with-undo — MIGRATION DEPLOYED (`supabase db push` author-run; `0012_restore_activity` live).** The Activities list now deletes optimistically with a bottom-left **Undo toast** (reversibility over confirmation; replaced the inline "Delete?" confirm). Undo calls a **new `restore_activity` RPC** — migration [`0012_restore_activity.sql`](supabase/migrations/0012_restore_activity.sql): SECURITY DEFINER mirror of `soft_delete_activity`, own inline owner check (can_edit_activity gates on `deleted_at is null` so it's unusable for restore), clears `deleted_at`, audits `activity.restore` (new enum value via `add value if not exists`, 0010 precedent). No slug conflict on restore (`unique(owner_id,slug)` is non-partial → tombstone keeps its slug). **AUTHOR-RUN + DONE 2026-07-19: `supabase db push`** applied 0012 — Undo now restores. No function/kit/ingest/wire change; app rode the SPA. App green: typecheck + 642 unit (new [Activities.test.tsx](packages/app/src/__tests__/Activities.test.tsx): delete/undo/auto-dismiss/failure, 5 tests). Design-confirmed via `/design-review` (goodwill 45→~75 on first-run); slug.ts made internal-only + random-token suffix past attempt 4. Part of UX-lens fixes ([docs/design/ux-lens.md](docs/design/ux-lens.md)); fixes 1/2/7 (token sweep + AA contrast) merged earlier (`a42ea76`). **Fix 3 (Submissions drill-down rewrite) also SHIPPED 2026-07-18, app-only (nothing queued):** the eight per-type tables became ONE reading-order list — new `docOrder` on every ActivityIndex info (one slot per indexed block; display blocks consume none; a problem's blanks share its slot), items = header (Problem N · type badge · result · confidence-when-present) + prompt + type-specific body; removed-block responses sink last; `[left, dashed]`-style jargon humanized; function-system branch preserved. 7 new tests (2 docOrder pins + 5 component via a `SubmissionDetailForTest` export). **Fix 5 (header emoji→lucide + Publish rework) also SHIPPED 2026-07-18, app-only (nothing queued):** the editor header is now one lucide icon+label chip row — config chips (Settings/Printer/BookOpen/Calculator) + nav chips (FileText/BarChart3/ClipboardPaste) + **Publish as the last chip** (`variant="primary"` filled slate-900, Globe icon, after a divider), each with a hover `title`. Publish invoke logic extracted to a `usePublish` hook; the old green success pill + standalone `PublishedLink` (two copy-link affordances) consolidated into ONE `PublishStatus` line below the header (Live dot · Open ↗ · Copy link, "Published v{N}" after a session publish). `PublishControl.tsx` deleted. DevConfigDrawer bench updated to mirror the real header (browser-verified: clean cohesive row, primary Publish reads as primary, no console errors). 4 new `usePublish` tests. **Header iterated further 2026-07-18 (author-directed):** Publish is a distinct filled **button** (not a chip) — first attempt (Publish-as-chip) overflowed 768px and wrapped to a lonely line. Final layout = ONE row, three groups (`flex-wrap justify-between`): back-link · lucide config+nav chip toolbar · (save indicator + Publish). To fit one line, **Print layout moved out of the editor toolbar into the Print view route** ([ActivityPrint.tsx](packages/app/src/routes/ActivityPrint.tsx)): the shared `PrintSettingsBody` now renders there and its edits are the **saved** print config, autosaving via `useAutosave` (replaced the print route's session-only overrides — author-ratified: one source of truth, loses the one-off-tweak feature). `ConfigKey`/drawer lose `'print'`. Browser-measured fit: 660px children in 768px (108px headroom, no wrap). Whole app suite 653 green. **Fix 6 (@theme token bridge) also SHIPPED 2026-07-18 (app-only, no deploy) — eng-reviewed via /plan-eng-review first.** `index.css` gains an `@theme` block defining ~31 semantic `--color-<role>` tokens (each → a Tailwind built-in shade, so the generated `bg-surface`/`text-muted`/… utilities are pixel-identical to the literals). Swept ~274 chrome palette literals across 10 files to the utilities (role-mapped Node script w/ word-boundary lookarounds, not blind sed; only `text-white` + the dark-toast `text-blue-300/200` left as intentional). `editor.css` re-points its shared `--ed-*` at the same `@theme` vars (single source; editor-only tokens stay). Two ratified shifts: `--color-success`=emerald-600 (chrome green-700 "correct" unified onto editor emerald) + `--color-muted`=slate-600 (AA-safe on white+tinted, correct-by-construction). Verified: Home computed-color canary = 0 diffs, dev-bench + playground tokens resolve, no console errors, 653 tests green (updated 1 dot-cue class assertion), full monorepo typecheck clean. Eng-review decisions logged. **All 6 UX-lens fixes now done.**
2. **Inequality-system student controls redesign — BUILT + DEPLOYED 2026-07-18 (nothing queued).** Replaced the N-row control bar (it covered the lower half-plane) with a **one-line chip strip** + tap-to-select a boundary → a compact contextual popover for just that line + **click-the-half-plane-to-shade**; a contextual click-to-shade hint; a compact keyboard-accessible **Flip shade** control (click-to-shade is pointer-only). Author-directed after a student-side screenshot. Commits `483112e` (redesign) · `e4811c4` (hint) · `e0e618e` (remove Shade above/below buttons + fix ✕ leaking pointer events to the board) · `a5cbaa4` (Flip shade). Kit-only (`board.ts` `createSystemAnswerBoard` `setSelected` + click handler w/ `onSelect`/`onRegionShade`; `graph-question.ts` `mountGraphSystemQuestion` strip/popover). Functions board unaffected (no hooks → no-op). **Author-run + DONE:** `pnpm upload:graph-kit` → new live kit **`graph-kit-VTQZCLFW.js`** (manifest committed) → `pnpm deploy:publish`. NO ingest/schema/wire change (student UI only; scoring/emit unchanged). graph-kit 345 green; browser-verified on `/dev/graph-question`.
2. **Multi-answer graph systems — BOTH PHASES BUILT + DEPLOYED + owner-verified 2026-07-18 (fully DONE, nothing queued).** **Phase 1 (inequality systems)** `e7e84fe`/`dade489`/`00a0e7c`/`480444e` + spec `0ea602a`; **Phase 2 (functions-systems, the fast-follow)** `4f6049e`/`9faf0fc`/`760baa8`/`3841e57`. Both on `main`. Full-stack, mirrored: additive response members `graph_inequality_system` + `plot_function_system` in `GraphResponseV4` (**NO `submission.schemaVersion` bump** — Ray/Segment precedent, ingest stays v9); a shared `maxBipartiteMatch` behind `scoreInequalitySystem` + `scoreFunctionSystem` (order-independent match-all, ratified J1 over greedy); runtime emit (`gs.parts` / `gs.curveParts`, **storage v10→12**); one shared N-object student board (`createSystemAnswerBoard` — inequalities add a per-boundary side/style control bar + overlapping translucent shades = intersection; functions are plain curves, no control bar); N-row authoring in InteractiveGraphView (static preview at N>1; **N=1 byte-identical**). Also fixed a latent fresh-load bug (system widgets reported at first paint → runtime restore spuriously marked untouched systems answered; removed first-paint report + gated restore on `gs.answered`). Browser-verified on `/dev/graph-question` + `/playground` for both. **Deploy train — ✅ AUTHOR-RUN + DONE 2026-07-18 (one combined train, both phases):** `pnpm bundle:renderer` (committed) → `pnpm upload:graph-kit` → new kit **`graph-kit-PAEDPXRK.js`** covering BOTH systems (manifest committed; superseded the interim inequality-only `EXAG55I5`) → `pnpm deploy:ingest` (accepts both `graph_inequality_system` + `plot_function_system`) → `pnpm deploy:publish` (new runtime emit + storage v12). NO `submission.schemaVersion` bump (ingest stays v9). New pages emit the systems; already-published pages pick them up on re-publish. **Owner GS-J1b CLEARED 2026-07-18** — a real 2-inequality system AND a 2-curve function system authored, live-published, answered as a student, dashboard scored each correctly. (Optional backlog only: a GS-M11/FS dashboard-render Playwright e2e — the type-branch seam shipped in Submissions.tsx and is owner-verified.) Spec: [TEST_SPEC.md](TEST_SPEC.md) "Slice: Graph systems", [RATIFICATION_LOG.md](RATIFICATION_LOG.md); design [docs/design/graph-systems.md](docs/design/graph-systems.md).
2. **Image crop mode — FULLY BUILT + DEPLOYED + owner-verified 2026-07-18 (nothing queued).** Group 3's deferred crop item, done end to end. `/office-hours` + `/plan-eng-review` + `/test-spec` (fresh-context sweep) — design [docs/design/image-crop.md](docs/design/image-crop.md), spec [TEST_SPEC.md](TEST_SPEC.md) "Slice: Image crop mode" (CR-M1..M10 + CR-INV1/2/3 + CR-S1..S4 + owner CR-J1b), ratification [RATIFICATION_LOG.md](RATIFICATION_LOG.md). **Foundation (`fbab914`):** schema (`crop {x,y,w,h}` + `srcAspect`, both-or-neither; `height` REMOVED), renderer crop math, serialize round-trip. **Authoring half (`67d2af1` renderer, `20e3dff` app):** the 2-D crop-mode gesture UI in ImageView (`cropGeometry.ts` pure frame math + `ImageCropEditor.tsx` gesture surface + `cropMode.ts` id-keyed request store), the **Crop** command-bar primary (image primaries = Crop + Replace), alt/caption + Reset-crop → Advanced drawer, source-only slimmed popover, `updateImageAttrs` clears crop+srcAspect on src-swap (CR-INV2), editor preview renders the SAME window as the renderer (CR-S3 parity). **Renderer caption fix (`67d2af1`):** `<figcaption>` moved OUT of the aspect/overflow box into an inner `.block-image-window` (a figure sibling) so captions aren't clipped (CR-S2/M4) — bundle regenerated. Tests: CR-M1/M2/M3/M8/M9 + CR-M4/S2 + CR-M5/M7/M10 + CR-INV1/2 + CR-S3/S4 (8 crop e2e) + 17 cropGeometry unit. Whole monorepo green (schema 290 / graph-kit 331 / renderer 588 / app 633 + 97 e2e). **Author-run + DONE 2026-07-18:** `publish-activity` redeployed (renderer caption-window fix now live; NO ingest/kit change, no `schemaVersion`/wire/storage bump — additive authoring-time crop). **Owner CR-J1b PASSED** — cross-browser eyeball on a real cropped published page confirmed positive. Nothing left.
2. **Group 3 sizing slice — DEPLOYED 2026-07-17 (only an owner eyeball left).** Width/align sizing for `interactive_graph` + `data_plot` + `number_line` (the same `sizingFields` fragment images use): schema (`cabece6`), renderer emission + bundle (`aae0d82`), shared `useBlockWidthResize` hook extracted from ImageView (`86d9011`), editor plumbing + serialize round-trip (`63c96f8`), shared `BlockSizingField` drawer control (`a9d4243`), board re-fit via JSXGraph built-in `resize` (`b827fc5`). Sized these three from the **drawer** (chips + align + reset), NOT edge drag-handles (D6 — a handle crushes their authoring UI); handles stay image-only. **Author-run + DONE:** `upload:graph-kit` → new kit `graph-kit-GKJHYSRN.js` (manifest committed `f44ad23`) → **`publish-activity` redeployed**. NO ingest redeploy (authoring display sizing, additive optional width/align — no wire/`schemaVersion`/storage bump). Older kit hashes stay on R2, so already-published pages keep working until re-published. **Owner manual-QA still owed (TEST_SPEC SZ-J1b, the one thing automation can't cover):** open a sized interactive graph on a freshly-published page, resize the window narrow, confirm the board re-fits with no clip (predict-then-run + break-it). Spec + ratification: [TEST_SPEC.md](TEST_SPEC.md) "Slice: Group 3 sizing", [RATIFICATION_LOG.md](RATIFICATION_LOG.md); design [editor-refinement-pass.md](docs/design/editor-refinement-pass.md) "Sizing slice". **BUG FOUND (owner SZ-J1b) + FIXED 2026-07-18 (`6ff38d4`):** the dial did nothing on publish/print because the board canvases kept their `max-width` cap (28rem/34rem) even when sized — any fraction above the cap (e.g. 60% of 760px = 456 > 448) resolved to the cap = identical to unsized. Fix lifts the cap when `.block-sized` (mirrors images; screen + print), browser-harness-verified (unsized 448 / 60% 437 / 40% 291). Renderer CSS-only → bundle regenerated + committed; **`publish-activity` REDEPLOYED + pushed 2026-07-18 (DONE, nothing queued)** — new pages honor the sizing dial on publish/print; already-published pages pick it up on re-publish (NO kit/ingest/wire change).
2. **Columns-universal-container refactor slices 1–5 DONE.** Slices 1–4 merged to `main` (`94e1fb0`); **slice 5 `publish-activity` redeploy AUTHOR-RUN 2026-07-15** (`pnpm deploy:publish`, bundle confirmed zero-drift beforehand) — newly-published pages now render rows-of-columns (`renderRow`, `.block-row`, `data-block-type="row"`); already-published R2 pages get it on their next re-publish. NO ingest redeploy (submission wire unchanged). **Only slice 6 (Notion-hybrid editor) remains in the arc — now IN PROGRESS.**
2. **Reminder:** any future redeploy of `ingest-submission` needs `--no-verify-jwt` (see CLAUDE.md).
2. **Block accents de-striped — `publish-activity` REDEPLOY queued (2026-07-14).** `learning_objectives` AND `worked_example` published blocks swapped their colored stripe (objectives left-stripe, worked-example top-stripe) for a full 1px accent hairline + `--color-note-bg` tint + a leading icon in a neutral title ([styles.ts](packages/renderer/src/runtime/styles.ts) + [learning-objectives.ts](packages/renderer/src/blocks/learning-objectives.ts)); bundle regenerated + committed. Renderer change → **redeploy `publish-activity`** so newly-published pages pick it up (already-published pages get it on their next re-publish; no wire/ingest change). The editor-side change for `self_explanation` / `free_response` is app-only (their published blocks never had a stripe) and rides the SPA — no deploy.
3. **Phase 2.6 grading — FULLY DEPLOYED + live-verified 2026-07-13 (nothing queued).** Migrations 0010 + 0011 applied; `get-feedback` deployed with `verify_jwt:false`; `publish-activity` redeployed. All confirmed against live: 0010's 7 checks pass; 0011 cleared the `grades_touch_and_audit` advisor finding (EXECUTE now only postgres/service_role); an anonymous POST to get-feedback passes the gate (404 from the lookup, not 401) → the sidecar can reach it. My grading work introduced **zero net-new security advisor findings**. **NO `ingest-submission` redeploy** anywhere in 2.6 (wire unchanged). **The only thing left is a human smoke test in the app:** author a rubric → submit as a student → grade it → confirm the feedback panel appears on the published page. (Deploy footgun learned + captured in CLAUDE.md: `get-feedback` needs `--no-verify-jwt` like ingest; a first `deploy:feedback` silently no-op'd — a *successful* function deploy bumps the version, so check that.)

1. **Graph-authoring redirection — DEPLOYED 2026-07-17 (nothing queued).** Authored drawable `color` (palette KEY; `DRAWABLE_PALETTE` single-sourced in graph-kit) rendered on both draw paths (`board.ts` + renderer `graph-svg.ts`). **Author-run:** `upload:graph-kit` → new kit `graph-kit-R4HUMJDF.js` (manifest committed) → `publish-activity` redeployed. NO ingest redeploy (drawables are authored display content; additive optional field, no wire/storage bump). Part A (preview eye toggle) + the DrawableListEditor uniform-row rewrite were app-only (rode the SPA). Browser-verified on `/playground`. Commits `1640838`/`2ec7644`/`8259cb3`/`50119dc` + manifest bump. **Nice-to-have author eyeball:** an MC per-choice graph (shares the rewritten DrawableListEditor) + a live-page color check on a freshly-published activity.

_(Otherwise nothing queued — the faded-numbering `publish-activity` redeploy was author-run 2026-07-13. Phase 2.6 grading is fully deployed + live-verified. The pedagogical-blocks deploy train (author-run 2026-07-12) is done: `ingest-submission` accepts wire **v9**, `publish-activity` + all four blocks live. Storage v10. **Current live kit hash: `graph-kit-XB5CUURV.js`** (Model A complete — math-prompt mount + MA-T6 self-hosted MathLive fonts + calculator-bug fix; deployed 2026-07-20. Superseded `RWCPNEWM`/`NME3URET`/`VTQZCLFW`/`NXZNIEUK`/`NNS6KNUS`/`MNGFCB2R` — older hashes stay on R2 so already-published pages keep working until re-published). Source of truth: `supabase/functions/_shared/graph-kit-manifest.ts`. Deploy clearances archived in [HISTORY.md](docs/HISTORY.md).)_

## Next major arc — STRICT-GRID: COMPLETE (SLICES 1 + 2, all T1–T8) SHIPPED 2026-07-21

**Strict-grid editor migration ([docs/design/strict-grid-editor.md](docs/design/strict-grid-editor.md)).** The editor's ProseMirror tree now IS the stored rows-of-columns model (doc `(row|sectionBreak)+`, every block inside a `column`); serialize's collapse/unwrap is deleted. **App-only — NO renderer/publish/wire/storage change** (stored shape unchanged; bundle regen = zero drift). Killed the editor-vs-storage tech debt.

**SLICE 1 (T1–T6) — DONE + on `main` (7 commits `3d91692`→`bcffebb`, NOT pushed).** Whole monorepo green: schema 321 / graph-kit 370 / renderer 651 / app 707 unit + **134 e2e**; typecheck clean; lint 0 errors; build clean. New files: `editor/extensions/Doc.ts` (strict top node, row-first fill), `editor/extensions/StrictGridNormalize.ts` (empty-state + trailing-para + re-coalesce appendTransaction, undo-safe, + `transformPasted` flatten), `editor/strictGrid.ts` (activeBlockAt / topLevelRowAt / isTopLevelStack(InsertPos) / empty-state helpers), `lib/serializeTestBridge.ts` (test bare⇆strict adapter), `e2e/strict-grid.e2e.ts` (7 CRITICAL/structural pins). Key gotchas fixed live: **StarterKit TrailingNode disabled** (its doc-level fallback appended a stray `sectionBreak` after every edit) + a strict-grid trailing paragraph in the normalizer instead; **Doc content `(row|sectionBreak)+`** (row-first) so delete-all fills a clean row not a sectionBreak; 1-col rows render FLAT (`editor-columns--stack`, grip only on multi-col). Details + task checkboxes in the design doc.

**SLICE 2 (T7 seam affordances + T8 import) — DONE + on `main`.** Whole monorepo green: schema 321 / graph-kit 370 / renderer 651 / app **712** unit + **142 e2e**; typecheck + lint(0) clean; bundle zero-drift (app-only). New: `editor/extensions/RowSeamCaret.ts` (keymap — boundary Backspace SELECTS the seam node; ArrowDown/ArrowUp cross the row seam; all **Playwright-verified** — the in-app Browser pane is NOT authoritative for key events, it drops them). `Columns.ts` gains `dissolveRow` (non-destructive merge-back: multi-col → one full-width stack, concatenating every column's blocks) + `addRowBelow` (escape-the-row). Toolbar: the column cluster is gated to MULTI-col rows (`isActive('row')` was true for every stack now — regression fixed) + **Merge** / **Row below** escape hatches. wrapInColumns atom-block case (node-selected image/graph → column 1) closed. T8 import: `wrapBlocksStrict` at the two ActivityEditor call-sites makes import strict-valid (markdown-import pin validates against the REAL schema) AND the ` ```columns ``` ` **Markdown fence is built** (`parseColumnsFence` in markdownToTiptap.ts): columns divided by a `---` line, one block per line per column (paragraph / `$$math$$` / `{{blank}}`), 2–6 cols → a strict `row` node; format doc + copy-paste prompt updated in sync (prompt↔doc equality test guards it); 9 unit tests + end-to-end render verified (imports render as a real multi-col row with math/blanks). App unit now **721**.

**DEFERRED from the strict-grid arc (all optional):** transparent cross-row-merge caret → Notion-hybrid paradigm (TENSION-1, by design); rich per-column Markdown import (lists/headings/nested question fences — editor-only for now); a persistent per-row (widget) dissolve affordance if the toolbar button proves insufficient. Minor cosmetic: the PlaceholderHint "/" hint now also shows in empty multi-col cells (was top-level-only) — harmless.

## Current focus

**MOST RECENT: strict-grid slice 1 (above).** Prior active arc: **editor-refinement-pass** ([docs/design/editor-refinement-pass.md](docs/design/editor-refinement-pass.md) — the cross-block feedback list, rulings, and 4-group sequencing; the earlier slice-6 Notion-hybrid arc it builds on is in [notion-hybrid-editor.md](docs/design/notion-hybrid-editor.md)). App/editor-only unless a Group says otherwise. All committed on `main`, **NOT pushed** (author pushes). Harness after strict-grid slices 1+2 (incl. columns import fence): **142 e2e + 721 app unit** green (`pnpm --filter @activity/app test:e2e`). Lint: 0 errors, 12 accepted warnings (`react-refresh` render-fn-export pattern + `react-hooks/exhaustive-deps`).

**DONE this session (2026-07-16→17):** harness **86 e2e + 610 unit** green; lint 0 errors.
- **Graph-authoring redirection — COMPLETE + DEPLOYED (the whole arc).** Ran the full pipeline (`/plan-eng-review` ×2 + `/plan-design-review`) then built: **(A)** preview-as-student eye toggle on all 3 graphing blocks — an id-keyed `usePreviewToggle` store (useSyncExternalStore) shared between the NodeView (hides chrome) and the **quick-bar** (owns the 👁 button, next to delete/dup/settings — moved there after it collided with the popover); **(B)** `DrawableListEditor` rebuilt as ONE uniform Desmos-style expression list (swatch + monospace text + quiet kebab + per-row options), pure `drawableText.ts` round-trip spine (`formatDrawable`/`updateDrawableFromText`, merge-preserve extras + kinds gate + no-op guard); **(C)** authored per-drawable **color** — `color` palette-key on the schema `Drawable` (z.enum) + `DrawableAttr`, single-source `DRAWABLE_PALETTE` in graph-kit, rendered on BOTH draw paths (`board.ts` live/author board + renderer `graph-svg.ts` static/print SVG, per-color arrow markers, floored shade fill, answer-key stays INK). MC per-choice figures ride the same list (`expression` excluded, kit-free). Also: figure editor **Done** button + collapsed **thumbnail** (MC + matching), display graph **side-by-side** board/list (container query, stacks narrow), display-graph **caption toggle** (collapsed behind a "+ Caption" affordance, reveals + focuses on click; the content hole stays mounted). **DEPLOYED 2026-07-17** (kit `R4HUMJDF` → publish-activity; no ingest); authored-color live-page render **author-eyeballed OK**. Commits `1640838`/`2ec7644`/`8259cb3`/`50119dc`/`131928e`/`807bf4d`/`ddf6b5a` + graph-authoring e2e (6 tests) + STATE archive/caption.
- **Group 1 (bugs)** — quick-bar ⚙ gear gating (`hasConfigurableControls`); data-plot numeric-input draft-commit (`DraftNumberInput`, blank tolerance = 0). (`5c3837f`)
- **Group 2 (settings coherence) — COMPLETE.** blank-popover reorg (answer config always-visible, hint/feedback under one "Advanced options"); data-plot settings → drawer; interactive-graph prompt-on-top; **interactive-graph settings → drawer extraction** (eng-reviewed CLEARED → test-specced RATIFIED [TEST_SPEC.md](TEST_SPEC.md) → `graphAnswerHelpers.ts` leaf + `GraphSettings.tsx`; INV1 = a settings write never changes `interaction.type`). Commits `0cc3893`/`9b3925d`/`0a53e60`/`ffa0116`/`44c7ea3`.
- **Math virtual keyboard** no longer auto-pops when editing math (removed the focusin→`show()` in `useMathFieldEditing` + `FormulaField`; `'manual'` policy stays). (`647e588`)
- **Graph + data-plot + number-line design realignment** (author "doesn't feel like the rest of the project"): all three NodeViews' raw inline hex → `--ed-` tokens; graph/data-plot/number-line **settings drawers themed** to the AdvancedDrawer field classes (`__group-title`/`__field--toggle`/shared `ToggleRow` in QuestionSettings); the `--toggle` layout bug fixed (checkbox next to label, not stranded); data-plot Data input + all three question-prompt inks tokenized; display graph starts **blank**; **data-plot + number-line prompts moved above the board** (like the graph). Commits `abeb682`/`b02edd8`/`86058e1`/`b0906d3`/`f19b417`/`feed45f`.
- **Number-line settings → drawer** (`d2dab13`) — the last inline-settings holdout; new `NumberLineSettings.tsx`, matches graph/data-plot.
- **Empty-prompt placeholder signifier** (`e102ca9`) — new reusable `PromptField` (ghost "Type the question…" on an empty prompt, gone on type). Applied to the 3 graphing blocks; **now rolled out to every prompt block** (`3eae8a5`, 2026-07-19 — see the follow-up note above).
- **Number-line interval authoring = formula-first** (`2339b65`) — type an inequality (`2 < x <= 5`, `x < -3`) → board previews the segment/ray with open/closed ends; shape-pills + bound-rows removed (drag still works, field re-derives). Shared `editor/numberLineFormula.ts` parse/format, also feeding the ```numberline import fence. plot_point stays numeric.

**OWED (author) — ALL CLEARED 2026-07-19:** the graph settings drawer manual-QA pass (TEST_SPEC D6), the number-line open/closed-circle rendering from a typed inequality, and the standing **full VISUAL REVIEW** of the Notion-hybrid editor surface were all author-verified 2026-07-19. Nothing owed here.

**Small OWED follow-up — DONE 2026-07-19 (`3eae8a5`, app-only, no wire/deploy):** `PromptField` (empty-prompt placeholder) rolled out to the remaining prompt blocks — MC / matching / ordering / fill-in-blank / self-explanation / short-answer / essay (swapped the bare `NodeViewContent` for the shared `PromptField`; removed the now-unused `NodeViewContent` import in each). Fill-in-blank uses "Type the sentence…", the rest "Type the question…". Browser-verified on `/playground` (fill-in-blank + MC placeholders render faint + correctly positioned, no console errors); 656 app unit tests green. The empty-prompt signifier now covers every prompt block.

**DEFERRED backlog (next sessions), roughly in priority order:** _(Graph-authoring redirection — DONE + deployed this session, see above.)_
1. **Group 3 — mostly DONE; one flagged follow-up.** Image crop-mode DONE (Pending #1); sizing slice DEPLOYED + bug-fixed (Pending #2); **inequality author-board shading DONE 2026-07-18 (`8188dd5`, app-only — nothing queued):** authoring a "Graph an inequality" question now shades the correct half-plane on the author board (was a bare line). `mountGraphAuthor` now enables the existing `shadeBoundary` machinery (`setShadeSide`/`setBoundaryDashed`) and pre-sets the authored side+strict via a new `GraphAuthorConfig.inequality` field (InteractiveGraphView passes it). Side driven only by the typed inequality (no onSideClick → no desync); shade tracks handle drags; sign flip re-applies via the `formulaEpoch` remount. **Editor-only** — `mountGraphAuthor` is never in the published runtime, so it rides the SPA deploy; NO kit upload / NO publish-activity redeploy / no wire change. Browser-verified on `/playground` (shade renders for y>x above/dashed, flips to below for y<x, no errors). Grep audit also found: **ray/segment bounded-domain authoring already works** (graphAnswerHelpers + boundedCurveLogic; "a straight line with a range becomes a ray or segment"), and **display-drawable inequality shading already works** (drawableFormulaLogic parses `y>2x+1` → shaded curve, both render paths). **Multi-answer graph questions (inequality systems) — BUILT + GREEN 2026-07-18, DEPLOY TRAIN QUEUED (Pending #1)** ([docs/design/graph-systems.md](docs/design/graph-systems.md)). `/office-hours` + `/plan-eng-review` + `/test-spec` (RATIFIED, [TEST_SPEC.md](TEST_SPEC.md) "Slice: Graph systems") → built slice-by-slice. Teachers can now author a system of inequalities (N typed rows); students plot N boundaries on one board and the overlapping shades render the intersection; scored **match-all, order-independent** via `scoreInequalitySystem` (maximum **bipartite** matching — ratified J1 over greedy, on grading-blast), honoring `partialCredit` (matched/N). **Full-stack, 4 slices** (`e7e84fe`/`dade489`/`00a0e7c`/`480444e`): additive `graph_inequality_system` response member (**NO `schemaVersion` bump** — Ray/Segment precedent, ingest stays v9; storage v10→11), runtime emit with N=1 byte-identity preserved, `createSystemAnswerBoard`/`mountGraphSystemQuestion` (kit), N-row authoring + static preview in InteractiveGraphView. Browser-verified `/dev/graph-question` + `/playground`. Deploy train + owner GS-J1b in Pending #1. Functions-systems = deferred fast-follow (reuses this wire + scorer). _(Image crop rework + inequality author-board shading — all shipped.)_
2. **Group 4 — ENG-CLEARED (Model B) 2026-07-19, READY TO BUILD:** blank discoverability signifier + **math-blank-in-equation** ([math-blanks.md](docs/design/math-blanks.md), "Eng review resolution"). `/plan-eng-review` + a Claude outside-voice pass. **Scope re-sequenced to Model B first + signifier; Model A (in-equation MathLive prompts) DEFERRED to its own review** — the outside voice caught a real hole: blanks are NOT scored like graphs (see below), and Model A front-loads all the irreversible/untested surface (MathLive-as-input, additive-only RUNTIME.md contract, mobile/a11y/print/CDN-fonts) before B's sampling engine is proven; matches design D1. **Locked (corrected) decisions:** grading is **SYNC via a held module-level `mathEquivalent` reference** the eager-loaded kit populates + a `'math'` entry in `strategies.ts` that the EXISTING sync re-score path calls — NOT compute-live-into-state (that gets clobbered to string-match false at submit, because `scoreBlanksInScope` re-scores in both `checkSection` AND `gatherResponses`). Kit-fail = unscored-but-submitted + excluded from the section tally. `mathEquivalent` single-sourced in graph-kit on `compileFunction`, sampling the **union** of key+student free vars over a **signed** domain (so `sqrt(x^2)≢x`). KaTeX-render math answers in Submissions. jsdom+owner-manual tests; blur+check cadence. 1 CRITICAL pin (text/numeric byte-identity). Tasks T1–T8 + 2 parallel lanes in the doc. NO wire/ingest change (blanks map reuse, wire v9); **NO `STORAGE_SCHEMA_VERSION` bump** (A1b resolved without a `BlankState` field — held-sync-reference re-scores at check + stores value/result atomically). Deploy = kit upload → publish-activity redeploy + bundle:renderer. **DEPLOYED + PUSHED 2026-07-19: T1–T8 all shipped** (mathEquivalent · schema+pin · runtime held-ref+preloader+tally · jsdom integration+save/restore · blank-popover 3-way selector · Submissions KaTeX · ghost-text signifier · kit upload + publish redeploy); **only the owner J1b eyeball remains — see Pending #0.** New pitfall learning logged: `blank-scoring-is-sync-rescore-not-read-only`. **Signifier form RESOLVED via `/plan-design-review` 2026-07-19 → FORM A (ghost text only):** inline ghost text via the existing fill-in-blank `PromptField` (show-when-no-blank, hide-forever-after); author dropped the hybrid's quick-bar leg as unnecessary. Rejected the in-text `+`-underline widget (off-idiom — collides with gutter insert-`+` and a real blank's underline). Spec in math-blanks.md T7.
3. **Slice-6 stage 6 (snap motion) — SHIPPED 2026-07-21** (app-only, no wire/deploy; `/plan-eng-review` CLEARED — 8 findings folded, outside-voice pass reversed the detection mechanism to explicit meta-tagging; full record in [notion-hybrid-editor.md](docs/design/notion-hybrid-editor.md) stage-6 entry): new `SettleMotion` extension (settle meta + `armSettle` request store at the 3 insert funnels + PM `uiEvent:'drop'`; NO step-diffing — Enter/attr-writes/conversions are pinned false-positive classes), two keyframes (insert scale+fade origin-top / move bounce-only), accent-styled built-in dropcursor (glide descoped — element recreated per update), consolidated reduced-motion block (also closed the pre-existing 3-of-4 gap), animationend-per-block clear + destroy-safe janitor. 13 unit + 7 e2e green (animationend proven sub-900ms; janitor verified live in a hidden tab). **Deferred:** ⌘⇧↑/↓ keyboard-reorder settle (debounce design — [TODOS.md](TODOS.md)). **Two pre-existing issues found; one fixed, one chip-filed:** (a) **FIXED 2026-07-21** (`26457d4`) — 5 stale e2e (gutter ×3 / quickbar / select-state, stale since `e5862ee`'s right-gutter rework: "Insert above"→"below", the onBar pointer-hold, and the grip/"+" overlap); whole e2e suite green again (127 passed). While fixing, noted a **minor real UX papercut**: the bottom-left "+" (BlockAddButtonHost) can overlap the drag grip's lower half on a short block — reaching for the grip can hit "+". (b) chip-filed: the slash menu dies under synthetic keyboard input as soon as a query char follows `/` (clean-tree repro; humans unaffected — snap-motion e2e routes via the Add-a-block modal instead).
4. **Slice-6 stage 7 (toolbar diet) — CLEANUP SHIPPED 2026-07-21; focus mode DEFERRED** (app-only; author-ruled cleanup-only). Toolbar was already slim (block insertion left it in the 2026-07-08 reorg). Cleanup: removed the faded-worked-example's inline "Label steps" header toggle — the last inline control duplicating a settings-mode field (`showStepLabels` lives in ⚙ settings mode via `blockControls.ts` `simple`; progressive disclosure). Kept: block-style picker stays in the toolbar (author overrode the doc's "move to docked bar" as Docs-native friction); column cluster stays contextual. 705 unit + advanced-drawer e2e green; `/playground`-verified (header now [number·icon·title], no inline toggle). **DEFERRED — focus mode is its own mini-arc:** the dim-the-rest toggle needs a caret-tracking ProseMirror plugin (CSS can't identify the caret block under one contenteditable — the wall that also deferred stage-1 gutter focus-parity), off-by-default/optional; wants its own design+eng pass. **The Notion-hybrid editor arc (slice 6) is now effectively complete through the shipped stages; open remainders: focus mode (deferred), input-parity/a11y touch pass, slice 6.5 smart-defaults.** **Dropped:** auto-feedback default-flip (author call — revisit only on pushback).

**Verification quirk:** the in-app Browser pane suppresses the position-measured hosts (command bar / quick-bar / drawer) under JS-driven selection — Playwright e2e (real chromium) is authoritative; drive the drawer via node-selection + gear + Advanced. `/playground` (unauthed) is the dev target; `/playground?empty=1` mounts a blank doc.

Recent commits (this session, newest first): graph-authoring e2e · `ddf6b5a` eye→quick-bar + side-by-side display · `807bf4d` figure Done + thumbnail · `131928e` kit manifest (color deploy) · `50119dc` uniform expression list + drawable color · `8259cb3` color field + drawableText spine · `2ec7644` DRAWABLE_PALETTE · `1640838` preview-eye toggle · `68fe391` quiet row tools + de-striped panels · `83ef878` question-block settings → drawer · `06e2bd9` toolbar routes to nested fields (mini toolbars deleted) · `e30f3f5` stage-5 picker preview cards + Start here · `c7c2e82` drop enter-edit primaries · `8b6f00b` settings mode · `dfc85c1` drawer single home + Advanced→Settings · `3507be9` quick-bar hover-fix + 3 icons · `298bc3c` quick-bar · `e2a1be0` rubric custom field · `07b2f15` Advanced drawer core · `48b0ab6`/`910945b`/`794377e` descriptors · `4419e64` select state · `f1b6d45` gutter · `9eb25b2` registry+harness.

---

_Older focus (pre-slice-6, retained for reference):_ The columns-as-universal-container refactor (slices 1–5) is DONE; slice 6 is the last of that arc. Other candidate arcs when slice 6 wraps:

1. **Free activity catalog / "Activity Bank"** (Phase 2 slice: browse + run/assign/print; [docs/design/free-activity-catalog.md](docs/design/free-activity-catalog.md)) — the onboarding/cold-start lever. **Design-reconciliation pass DONE + decisions AUTHOR-RULED 2026-07-13** (see the doc's "Design reconciliation" → rulings section): 6 of 7 green-lit — `visibility='public'` = listed (toggle by PublishControl), definer RPC catalog surface (`can_read_activity` untouched — it gates submissions; a row policy would leak `draft_content`), signed-in audience v1, full print engine on published-version-only, "Activity Bank" own route. Ruling #1 carries a standing constraint: design for **any owner opting activities into the free bank** (multi-author-ready attribution/RPC/toggle), not first-party-only. **One open item: #6 taxonomy/tags — author wants a tags discussion at kickoff** (options sketched in the doc; lean is `tags text[]` + GIN). Design-only by author instruction — no code until pickup.
2. **Phase 2 block-type priority — COMPLETE.** All four pedagogical blocks SHIPPED 2026-07-12 (code complete; deploy train queued): `learning_objectives`, `worked_example`, `faded_worked_example`, `self_explanation`. Nothing left in this family. (Phase 2.6 short_answer/essay — the GRADED free-text siblings — reuse self-explanation's `freeResponses` map when that arc starts.)
3. **Vocabulary glossary Phase 4** (tenant-scoped store + implicit reuse + auto-suggest; [docs/design/vocabulary-definitions.md](docs/design/vocabulary-definitions.md)).

Recently completed narratives are archived in [HISTORY.md](docs/HISTORY.md) (2026-07-10 → 07-14: pedagogical CONTENT/scaffold blocks, self-explanation, Phase 2.6 short_answer/essay + manual grading, the four-block + graded-free-text markdown fences, the editor design/UX pass — color tokens, add-block window, math-focus, insert-menu reorg — and the number_line/data_plot trains). Durable reasoning lives in [DECISIONS](docs/DECISIONS.md); current per-feature status is the Status-by-area table below.

Also queued:

- **Math blanks — fill-in-the-blank inside an equation (design captured 2026-07-14, [docs/design/math-blanks.md](docs/design/math-blanks.md)):** the author ask "math blocks with blanks with math blocks", reframed to MathLive prompts (a gap in a rendered equation the student fills with a math expression). Design-only; 7 decisions (§D) await author ruling at kickoff. Key findings: likely **no wire bump / no ingest redeploy** (prompt answers reuse the string `BlankResponse` shape + `blanks` map), grading is math.js **numeric-sampling** equivalence in the lazy graph-kit (symbolic CAS is out of scope), and `BlankToken.answerType` already has the `text|numeric` seam a `'math'` mode extends. Proposed as two slices — Model B (`answerType:'math'` on an ordinary blank) then Model A (in-equation prompts). No code until pickup.
- **Columns as the universal container — PLAN LOCKED via `/plan-eng-review` (2026-07-14), ready to implement ([docs/design/columns-universal-container.md](docs/design/columns-universal-container.md)):** reshape the doc root to sections → **rows** → columns → blocks (a row is a full-width single column by default; "add columns" splits it; the `columns` block type is removed). Ratified calls: full reshape; **migration subsystem CUT** (all test activities deleted by author, so no `migrate()`, no migrate-on-read); `schemaVersion 1→2` with the parser `z.literal(2)` rejecting v1; **Column holds `block+`** (a stack, not one-block-per-row); `row` gets its OWN node group (not `block`) so `column`'s `block+` can't re-nest a row; **1-col row renders FLAT** (grid only for multi-col); port the pure `Columns.ts` helpers, don't reimplement; sections stay a `sectionBreak` marker model. Outside-voice pass folded in 8 editor/serialize hazards — the **real critical path is the editor keymap/node model** (cross-row Backspace/Enter merge under `isolating`, non-destructive merge-back, empty-state backfill, split-into-columns/escape-the-row), NOT migration. Six ratified slices (schema → renderer/runtime/print → editor structural → editor verbs → deploy → Notion-hybrid UX as a separate later pass). **Pending author action before/at implementation:** confirm all v1 activities are deleted (done) + reconcile the `document.ts:8-13` migrate-on-read comment to the greenfield hard-cut. No ingest redeploy (wire unchanged); `publish-activity` redeploy at slice 5. **Slices 1–3 SHIPPED + WHOLE MONOREPO GREEN on branch `rows-refactor` (1736 tests).** Slice 1 (`ed21768`, schema): `layout.ts` (Row/Column), `Section.rows`, `Block` minus columns, `schemaVersion 2` reject-v1. Slice 2 (`3407887`, renderer): `renderRow` (flat 1-col = zero DOM overhead; grid only for multi-col), `.block-columns`→`.block-row`, `data-block-type` `columns`→`row`, bundle regenerated. Slice 3 (`45b72fd`, app): **editor uses the pragmatic bridge (Option A, author-ruled 2026-07-15)** — stored schema is fully rows-of-columns, but the editor keeps its block stream (single-col = bare blocks; multi-col = a `row` node = the renamed `columns` node); `serialize` wraps bare blocks into a 1-col Row on save + unwraps on load (lossless, 3 round-trip pins); `buildActivityIndex` walks rows→columns→blocks. **Slice 4 (`109338e`) = the one meaningful verb Option A left**: a `wrapInColumns` command + a "Split into columns" slash/insert item — moves the current top-level block's content into column 1 of a new multi-col row, column 2 empty, caret into it (browser-verified: side-by-side, zero errors). (add/remove-col, width, grid, cell-height already shipped in slice 3; the outside-voice keymap/merge/empty-state hazards were strict-model-only and are moot; the strict "everything in a grid" editor is slice 6.) **Browser-verified on `/playground`:** editor mounts, single-col typing works, inserting a 2-col layout creates a side-by-side `row` with working column controls, JSON emits `row`/`column`, zero console errors (verification caught a `DevFoldableColumns` module-level crash the tests missed). **Slices 1–4 SHIPPED + MERGED to `main` (`94e1fb0`, not pushed); the refactor is functionally complete. Remaining: (a) author `git push`; (b) slice 5 = author-run `publish-activity` redeploy (see Pending #1); (c) slice 6 = Notion-hybrid editor paradigm — **DESIGN LOCKED + REVISED** ([docs/design/notion-hybrid-editor.md](docs/design/notion-hybrid-editor.md)). Locked via `/design-consultation`, then steelmanned + goal-aligned via `/plan-design-review` (2026-07-15) which folded in 8 fixes — root cause: the original leaned on Notion power-user conventions for a Docs-native teacher audience. Net direction now: calm full-brightness canvas; **command bar ANCHORED to the block** (not floating); **click = edit/caret** (Docs-like; selection is grip/`Esc`-only); two-tier `Advanced` **+ smart defaults + just-in-time inline surfacing**; snap-motion on insert/reorder/columns; block-picker **visual previews**; **persistent gutter affordance** (touch/a11y parity); first-run **"Start here"** starters; grouped Advanced. Built on existing `--ed-` tokens; per-block control inventory. **ENG-REVIEWED via `/plan-eng-review` (2026-07-15)** — build architecture locked: a **control-descriptor registry** is the spine (each block extension declares its `{primary, advanced}` controls; one root host renders from the selected block's descriptor — no per-block mounting/Stage-13.5 hazard, no monster switch); **smart-defaults + just-in-time surfacing SPLIT to a separate slice 6.5** (net-new unvalidated heuristics, own spike, gates nothing); an **interaction test harness** (Playwright/browse) is required (repo has none) + unit-test the pure parts; feasibility fixes folded (CSS-driven gutter not a PM decoration, selection via grip+`Esc` only, static SVG picker thumbnails, prove the descriptor pattern on 2-3 simple blocks before the ~20-NodeView extraction). Verdict: **ENG CLEARED — buildable.** Design-only; ready to build (stage 0 = registry + harness first). Mockups + revision scorecard + eng ASCII diagram in the doc. **STAGE 0 SHIPPED (2026-07-15, app-only, no wire/deploy):** the architectural spine is in — a **control-descriptor registry** ([blockControls.ts](packages/app/src/editor/blockControls.ts), keyed by node-type name, same one-place discipline as `slashMenuItems`), a single root **`BlockCommandBarHost`** ([components/BlockCommandBarHost.tsx](packages/app/src/editor/components/BlockCommandBarHost.tsx)) mounted as a 4th sibling of the popover hosts in Editor.tsx (watches selection, on a `NodeSelection` with a descriptor renders the docked bar anchored canvas-relative at the block's top-right — the `--ed-ink` pill, springs in via new `--ed-spring`/motion tokens), proven on **3 blocks**: `mathBlock` (real primary **Edit** → `openMathFieldMeta`), `paragraph`/`heading` (generic Duplicate/Delete). Image deliberately excluded from the stage-0 trio (its existing `ImagePopoverHost` fires on the same node-selection — coexistence is a stage-3 migration concern, not architecture-proof). **New Playwright interaction harness** (the eng-required test infra the repo lacked): `@activity/app` devDep + `playwright.config.ts` + `e2e/*.e2e.ts` (named `.e2e.ts` so vitest's `{test,spec}` glob never collects them — runners disjoint by filename), run via `pnpm --filter @activity/app test:e2e`, auto-starts the vite dev server. 6 e2e specs (bar appears/swaps/absent-on-caret, **on-screen geometry guard**, Edit-opens-field) + 5 vitest unit tests (registry lookup + node-type parity guard). A rAF two-phase measure (mirrors BlankPopoverHost) fixed a pre-layout mis-anchor caught in browser verification (bar had landed at y=3481/x=-12; Playwright `toBeVisible` missed it → added the geometry assertion). Typecheck + 1741 unit + 6 e2e green; `/playground` browser-verified (screenshot: dark Edit pill at block top-right). **Stages 1 + 2 also SHIPPED this session (app-only, on `main`).** **Stage 1 (gutter):** persistent quiet rest dot (CSS `::before` per top-level block) → `⋮⋮`+`+` cluster on hover; `+` folds into the DragHandle (opens the Add-a-block window above the hovered block via `onNodeChange`), the separate insert-line is removed; `position:relative` on blocks anchors the dot (no image/columns/math regression). **Focus/tap parity DEFERRED** to a dedicated touch/a11y pass (author-ruled — CSS `:focus-within` can't identify the caret block under one contenteditable, and touch needs a real device; `/` covers the keyboard floor). **Stage 2 (select state):** accent selection outline+ring on `.ProseMirror-selectednode`; **grip-click selects**; new `SelectBlock` ext lifts a caret to a block NodeSelection on **`Esc`** (click stays edit); command bar (stage 0) shows in the Select state. Harness now 16 e2e (6 stage-0 + 5 gutter + 5 select) + 578 unit, all green; browser-verified (outline + Duplicate/Delete bar on a selected paragraph). **Stage 3 (per-block control descriptors) also SHIPPED this session (3 batches, app-only).** Corrected the stage-0 model: **Duplicate/Delete are universal** (host-rendered after each block's own primaries; accent-primary vs ghost-universal + divider). Shared **`enterEdit`** primary = the Select→Edit transition. All command-bar blocks registered: paragraph/heading (generic), mathBlock (Edit→field), learningObjectives/workedExample/fadedWorkedExample (Edit), selfExplanation/shortAnswer/essay (Prompt), multipleChoice (Choices)/matching (Pairs)/ordering (Items)/interactiveGraph/numberLine/dataPlot (Edit), fillInBlank (Edit), image (**Replace · Caption**). **Image coexistence resolved** (author-ruled): `ImagePopoverHost` no longer auto-opens on selection — the bar's Replace/Caption dispatch an `OPEN_IMAGE_POPOVER` tx meta (focused via a new `initialFocus` prop on ImageEditPopover); insert still opens it (empty source). Rich `advanced` (image sizing/crop, free-text rubric, tolerances, confidence, skills, …) deferred to **stage 4's drawer** (owns the field-type system). Small non-blocking gaps: bulletList/orderedList + structural `row` unregistered. **Stage 4 (Advanced drawer) CORE also SHIPPED this session (app-only).** New **field-type system** — `AdvancedField = toggle|number|text|select`, each pure data (`get(node)`/`set(editor,pos,value)` via editor command). New `AdvancedDrawer` renders the descriptor's grouped `advanced` most-common-first, **docked below the command bar** (an anchor wraps bar+drawer so they stack; still one root host). The bar's `Advanced` stub is now a real disclosure (closed by default, resets per selection). First fields: fadedWorkedExample (step-labels toggle), essay (placeholder + min/max words), shortAnswer/selfExplanation (placeholder) — attr-backed, synced with the inline footers (stage 7 removes those). **DEFERRED to a 4b pass:** complex sub-editors (rubric, per-choice figures, axis config, mistake feedback, skills) + image sizing (width/align/height/crop — needs the image-popover decompose); the field union may gain a `custom` kind. **Stage 4b also SHIPPED this session:** added the **`custom` field kind** (`render(ctx)→ReactNode`) for complex sub-editors; migrated the **rubric builder** — extracted it out of FreeResponseView into a reusable **`RubricEditor`** (deduped: same component backs the inline footer AND the drawer's custom field, synced via the `rubric` attr), a `Grading` group on shortAnswer/essay. Drawer **widens** (`--wide` 28rem, 60vh scroll) for custom fields so rubric rows don't overflow. `/playground`-verified (rubric "+ Add" writes the attr, footer stays in sync). **Still deferred:** other complex sub-editors (per-choice figures, axis config, mistake feedback, skills — same `custom` pattern) + image sizing (needs the image-popover decompose). **Next: stage 5** picker previews + first-run empty state (visible, self-contained), then stage 6 snap motion, stage 7 toolbar diet + focus mode (+ remove the now-redundant inline footers); plus deferred input-parity/a11y pass + slice 6.5 smart-defaults + the remaining 4b sub-editors. Harness now **31 e2e + 579 unit**, all green.
- **Phase 1 polish:** custom domain for R2.
- **Free activity catalog (design captured 2026-06-16, target Phase 2):** public catalog of free first-party math activities a newcomer can run as-is — a cold-start lever pairing with self-signup ([docs/design/free-activity-catalog.md](docs/design/free-activity-catalog.md)). The Phase 5 marketplace's free/discovery slice pulled forward; "use" = run-in-place read-only (not clone-to-edit). Phase 2 slice = browse + run/assign/print; consumer submission dashboards depend on Phase 3 assignment scoping.
- **Vocabulary glossary — Phase 4:** the account/tenant-scoped `glossary_entry` store + `glossaryKey` resolution at publish, leaning to the light **implicit-reuse** model + the suggest-don't-apply **auto-suggest** editor aid. Both additive to the shipped mark. Design in [docs/design/vocabulary-definitions.md](docs/design/vocabulary-definitions.md).
- **Other Phase 2 "decide at phase start" items:** image-hosting quota, block-type priority order, `skills` editing UI.
- **Long-term OCR/AI features (design captured 2026-06-16, not scheduled):** PDF → activity import ([docs/design/pdf-import.md](docs/design/pdf-import.md)) and photo-upload answer checking ([docs/design/photo-grading.md](docs/design/photo-grading.md)). Both reuse existing pipelines; decisions locked: generic-master QR, teacher-grading-aid v1, transcription-only AI rule. Photo-grading flags one real refactor — make answer evaluation server-shareable.

## Status by area

| Area | Status |
|---|---|
| Stages 9–16 (schema, renderer, runtime, editor, publish flow, submissions dashboard) | ✅ Complete; live-verified |
| Database migrations 0001–0006 | ✅ Applied; RLS verified |
| Migration 0007 (submission version pinning) | ✅ Applied (verified 2026-06-12) |
| Migration 0008 (`soft_delete_activity` RPC — delete bug fix) | ✅ Applied 2026-06-17; delete verified working by author |
| Print feature Drops A–D (worksheet config + journal foldable) | ✅ Complete; entirely client-side, no deploy needed |
| Structural columns + grid lines + width presets + drag-between-cells | ✅ Complete; deployed |
| Variable block sizing (Drops 1/3/4: width+align, image height/crop, cell work-space) | ✅ Complete + tests; deployed (publish-activity v34); Drop 2 column-drag cancelled by author (presets remain) |
| Editor UX: sticky toolbar + popover scroll-jump fix | ✅ Browser-verified |
| Phase 2 slice — rich text + inline math in hint/feedback/solution | ✅ Deployed; live UX unverified |
| Image authoring (block + popover + upload + live preview) | ✅ Deployed; R2 cross-origin `<img>` confirmed 2026-06-16 |
| `publish-activity` / `ingest-submission` Edge Functions | ✅ Deployed; ingest runs with `verify_jwt: false` |
| Cloudflare R2 hosting (published HTML) | ✅ Live |
| Auth (Google OAuth, allowlist) / React app / editor stack | ✅ In place |
| CI (GitHub Actions: typecheck/lint/test/build + bundle-staleness guard) | ✅ Added 2026-06-13; verified green locally |
| Markdown paste import | ✅ Complete + verified end-to-end by author; app-only. Format spec + Copy AI prompt + drift guard shipped |
| End-to-end manual test | ✅ Free-mode (2026-06-16) + locked-mode/dashboard (2026-06-17) both author-verified end to end |
| Order-independent blank groups | ✅ Complete + deployed; grouped scoring + `{{~}}` import verified live on a published page |
| Inline vocabulary definitions (rich text + math + optional image) | ✅ Code complete + browser-verified; `publish-activity` redeployed 2026-07-06 — reaches a page on its next re-publish |
| Calculator tool (Phase 2.7) | ✅ Stages 1–4 + full graphing UX overhaul, all browser-verified ([HISTORY 2026-07-06](docs/HISTORY.md)). Live since the 2026-07-08 full deploy pass |
| Activity-wide typography (`meta.typography`; R2-hosted WOFF2; drawer control; WYSIWYG) | ✅ Deployed 2026-07-08; live-page font check cleared 2026-07-10 |
| Number-line block (`number_line`, 1-D graded) | ✅ All 6 slices + shape-pills fix, DEPLOYED 2026-07-12 (kit `CWAMAF2L` era; ingest v34 wire 3–7; publish v85+). Browser-verified: dev harness, bundle-rendered page, `/playground`. Live-page eyeball optional |
| Data-plot block (`data_plot`, statistics charts) | ✅ Slice 1 (`display` + `build_dotplot`, wire v8 + storage v9) AND graded builds (`build_histogram` + `build_boxplot`, additive) DEPLOYED 2026-07-12 (kit `V6T6HAUS`; ingest v36 wire 3–8; publish v88). Browser-verified: `/dev/data-plot`, bundle-rendered pages ("1/1", "2/2 correct"), `/playground`. Live-page eyeball optional |
| Interactive-graph block — Stage 5 | ✅ Four interactions (plot-a-point N-handle, plot-a-function incl. bounded curves/rays/segments, graph-an-inequality, shade-region) + static display. DEPLOYED + live-verified (2026-07-10 author pass). The "answer surface as a graph-block field" seam RETIRED (author call 2026-07-10) — composes as display graph + sibling question block instead |
| Numeric blanks (`answerType: 'numeric'` + tolerance; `{{=…}}` import) | ✅ Live (cleared 2026-07-10); no wire change |
| Multiple-choice block (wire v5 + ```mc import + dashboard) | ✅ Live (cleared 2026-07-10); browser-verified |
| Static graph-paper SVG fallback (author's print fix) | ✅ Live (cleared 2026-07-10); committed in `c334b96` |
| Graded function families — quad/exp/log + quadratic inequalities | ✅ Live (cleared 2026-07-10); browser-verified; `38b9fbe` |
| Display-graph continuation arrows + readDrawables/print-arrowhead fixes | ✅ Live (cleared 2026-07-10); `131f62d`; arrows checkbox FULLY verified 2026-07-12; sin-curve `expression` tangent fix deployed 2026-07-12 (`9a68c0e`) |
| MC choice figures (per-choice image/graph, 2-up grid, shared DrawableListEditor) | ✅ Live (author-confirmed 2026-07-10) |
| Matching + ordering question types (wire v6 + storage v7 + ```match/```order import) | ✅ Deployed (author-confirmed 2026-07-10); pointer-DRAG on a real device confirmed 2026-07-12 |
| Calculator parity Tier A (calc inequalities, per-row domains, widened function table, log regression) | ✅ Deployed + author-confirmed 2026-07-11. Tier B (Compute Engine) DEFERRED on pedagogical grounds. Movable points + Σ/Π dropped by author call |
| Migration 0009 (security/performance housekeeping) | ✅ Applied + fully verified 2026-07-11 (8-step MCP verification passed; advisor residue is intentional — see DECISIONS) |
| ```numberline``` + ```dataplot``` markdown import fences | ✅ Committed 2026-07-12, app-only; reach teachers via the SPA deploy (main pushed) |
| Pedagogical blocks — `learning_objectives`, `worked_example`, `faded_worked_example`, `self_explanation` | ✅ DEPLOYED 2026-07-12 (train author-run: ingest v9 then publish). Browser-verified on `/playground`. First three are content/scaffold (no wire bump); self-explanation is ungraded free text (wire v8→v9 `freeResponses`, storage v9→v10, new runtime module + RUNTIME.md contract + dashboard). **Markdown import shipped**: ` ```objectives / ```worked / ```faded / ```explain ` fences (app-only, rides SPA deploy). Nice-to-have author eyeballs: `{{}}`-in-faded-step keystroke, live self-explanation submit→dashboard round-trip |
| Phase 2.6 manual grading (short_answer + essay + rubrics + grading) | ✅ Code complete (slices 1–5) + **DEPLOYED + live-verified** 2026-07-13. Migrations 0010+0011 applied; `get-feedback` (`verify_jwt:false`) + `publish-activity` deployed; anonymous get-feedback gate confirmed open, advisor clean. In-document rubrics; `grades` table + RLS + direct upsert; teacher grading UI (side-by-side + Needs-grading filter); student feedback via `get-feedback` + a conditional sidecar (base runtime 39.2 KiB). **Markdown import: ` ```shortanswer ` + ` ```essay ` fences added 2026-07-13** (rubric pipe grammar + essay `words:` range; app-only, `/playground`-verified). Only a human app-level round-trip smoke test remains |

Suite now: schema 290 / graph-kit 331 / renderer 588 / app 633 unit + 97 e2e (**1739** total); typecheck + lint green (0 errors; pre-existing warnings only). The ```shortanswer/```essay import fences added 25 app tests. The four pedagogical blocks + import fences + Phase 2.6 short_answer/essay/rubrics added ~82 tests across the packages (incl. a runtime `init.test.ts` case proving a `fill_in_blank` NESTED in a `faded_worked_example` is discovered by `buildRefs`, and a jsdom `free-text.test.ts` for free-text capture/gather/persist + the essay word counter). `runtime-doc-contract.test.ts` classifies the pedagogical trio as CONTAINER and the three free-text types (`self_explanation` / `short_answer` / `essay`) as INTERACTIVE (their shared RUNTIME.md contract section is guard-enforced). Runtime bundle **base 39.2 KiB / graphs 46.0 KiB** minified (+ a 1.7 KiB feedback sidecar inlined only on gradable pages) (re-check with `pnpm bundle:renderer`; treat printed numbers as truth). Graphs is over the 40 KiB soft target — under the 60 ceiling, accepted per the 2026-07-10 budget amendment (DECISIONS.md → "Runtime size budget amendment"). **Base sits at 98% of its soft target** — the next question-type generation should schedule the budget-ladder lever (per-question-type inlining variants; `document.ts` already picks by body scan) BEFORE it lands, per the amendment's "scheduled, not discovered" rule. The graph-runtime-into-kit lever was pulled 2026-07-10 — graph work grows the cached kit, not the inline bundles. (Environment note: this machine is the 2026-07-04 recovery clone — old computer died; toolchain re-set-up verified by a full green run. Kit-upload creds live in a gitignored `.env.r2`, auto-loaded by `pnpm upload:graph-kit` — the upload is an explicit command; `pnpm build:graph-kit` is build-only and never touches R2; see `.env.r2.example`.)

## Repo layout

```
activity-platform/
├── docs/
│   ├── design/        — feature designs captured ahead of implementation
│   ├── DECISIONS.md   — architecture decisions + reasoning
│   ├── HISTORY.md     — archived completed-work logs
│   └── COLLABORATION.md — working-with-the-author notes
├── packages/
│   ├── schema/        — Zod types, document model, factories
│   ├── renderer/      — pure JSON → HTML string; KaTeX inlined; no DOM
│   │   ├── RUNTIME.md — runtime architecture + data-attribute contract
│   │   └── src/runtime/ — published-page runtime (DOM TS, own tsconfig; per-file map in RUNTIME.md)
│   ├── graph-kit/     — @activity/graph-kit: shared graphing kit (Phase 2.7). evaluate.ts (math.js seam) + calculator.ts (mountCalculator) + runtime.ts (lazy kit runtime) + runtime-contract.ts (bridge↔kit types). mathlive + mathjs. Lazy-loaded on published pages; imported directly by the editor preview
│   └── app/           — Vite + React 19 + TS + Tailwind v4 + React Router v7
│       └── src/
│           ├── editor/     — Tiptap extensions, NodeViews, popover hosts, toolbar, slash menu
│           ├── components/ — PublishControl, ImportMarkdownDialog, RequireAuth
│           ├── routes/     — Activities, ActivityEditor, ActivityPrint, Submissions, Playground (dev-only)
│           ├── lib/        — serialize.ts, submissions.ts, uploadImage.ts, markdownToTiptap.ts, markdownImportPrompt.ts, foldable/ (print engine)
│           └── __tests__/
├── supabase/
│   ├── migrations/    — 0001–0009 (all applied)
│   └── functions/     — publish-activity, ingest-submission, upload-image, _shared/ (cors.ts, renderer.bundle.js + graph-kit-manifest.ts — auto-generated)
├── scripts/           — bundle-renderer.mjs; seed-test-data.sql; seed-e2e-activity.sql; seed-e2e-locked.sql
└── ...root configs
```

## Key constants

- **GitHub repo:** `ZanReed/activity-platform`
- **Supabase project ref:** `dtqutpdplefmufrrakxs`
- **Storage bucket (legacy):** `activities` — deleted 2026-06-18 after R2 verified end-to-end (note: the app's `.from('activities')` calls are the DB table, not this bucket)
- **Auth:** Google OAuth via Supabase. Site URL `http://localhost:5173` for dev. Allowlist-only signup (Phase 1).
- **Client env:** `VITE_PUBLISHED_URL_BASE` in `.env.local` (gitignored) mirrors the write-only `R2_PUBLIC_URL_BASE` secret; unset → published-page links hide.

## Open questions / deferred decisions

- **Custom domain for R2** (Phase 2 polish): `pub-<hash>.r2.dev` → `activities.<brand>.com` once a domain is owned; ~5 min of Cloudflare DNS after that.
- **Empty fill_in_blank drag handle attachment** — whether `definingForContent: true` changed the handles-only-on-non-empty behavior is unverified; re-test during a drag-reorder pass. Minor.
- **Blank popover: one-click switch between chips** — deferred design decision, no data loss; full reasoning in [DECISIONS.md](docs/DECISIONS.md) → "Fill-in-blank authoring (Stage 13.5)". Needs a dedicated design pass (FocusTrap/selection entanglement).
- **Section metadata panel** — SectionBreakView's inline title/checkpoint UI is adequate for now; an editor-level panel remains optional.
- **Responsive `--blank-width` sizing** — deferred from Stage 11.
- **Phase 2 block-type priority order** (worked example, faded worked example, learning objectives, self-explanation) — decide when Phase 2 starts.
- **`skills` editing UI** — deferred to Phase 2 per schema scheduling; the field round-trips everywhere, only the editing control is missing. Don't add piecemeal without the per-skill-analytics scope.
- **UX validation with 2–3 other teachers** on the editor patterns before classroom adoption — cost rises sharply once students use activities.
- **Post-success edit edge case** — locked/single paths briefly write-then-remove the persistence blob (wasteful but correct). Low priority.
- **CDN-hosted shared runtime** (Phase 3+): trigger is when republishing for runtime bug fixes gets painful (~50+ active activities).
- **Multi-tenancy / governance when a teacher leaves a district** — Phase 4; helpers are designed for it.
- **Manual grading workflow shape** (Phase 2.6), **media storage/privacy posture** (Phase 2.8), **annotation coordinate space** (Phase 2.9) — each decided at its phase start.

---

**Last updated:** 2026-07-21 (**Editor arc: stage 6 (snap motion) + stage 7 (toolbar-diet cleanup) SHIPPED; focus mode deferred.** Stage 6 = settle-on-place + accent dropcursor + reduced-motion sweep (13 unit + 7 e2e; eng-reviewed, outside voice reversed the detection mechanism to explicit meta-tagging). Stage 7 = removed the last inline control duplicating a settings field (faded-example "Label steps" toggle); block-style picker + column cluster kept in the toolbar (author call); focus mode deferred to its own mini-arc (needs a caret-tracking plugin). Also **greened the whole e2e suite** — fixed 5 stale gutter/quickbar/select-state tests from the right-gutter rework (127 e2e passed). Noted a minor real papercut: the gutter "+" can overlap the drag grip on short blocks. Chip open: slash menu dies under synthetic keyboard input. Full records in Deferred-backlog #3 (stage 6) + #4 (stage 7). Prior 2026-07-20: **Session cleanup.** Removed the now-shipped TODOS.md (`3cd0d4d`; math_block equation-gap reviewability Slices 1+2 are implemented + deployed — see Pending #000). Marked Model A (build + full deploy train + owner eyeball) and Model B (J1b eyeball) DONE. The Model A deploy train's regenerated graph-kit manifest was committed here (`c99eef3`, kit **`XB5CUURV`** supersedes RWCPNEWM — carries MA-T6 self-hosted MathLive fonts + a calculator-bug fix); it had been run author-side but not yet committed. Prior 2026-07-19: **Cleanup + PromptField rollout session.** Author cleared the queued deploys/pushes: graph-kit board dark-theming DEPLOYED (`pnpm deploy:publish`), the board-dark + color-pass commits PUSHED, and `0012_restore_activity` migration applied (`supabase db push`) — delete-with-undo now fully live. Author also verified the 3 owed eyeballs (graph settings drawer D6, number-line open/closed-circle rendering, full Notion-hybrid visual review) and addressed the 2 pre-existing bug chips (light `text-success` sub-AA + the 4 red `number-line-formula.e2e.ts` tests). **This session's code:** `PromptField` empty-prompt placeholder rolled out to the remaining prompt blocks (MC / matching / ordering / fill-in-blank / self-explanation / short-answer / essay — `3eae8a5`, app-only, `/playground`-verified, 656 unit green). **Next: Group 4 eng review** (blank discoverability signifier + math-blank-in-equation, [math-blanks.md](docs/design/math-blanks.md)). — Prior 2026-07-19: **Design-token arc: dark mode + graph-kit color pass** — the deferred item after UX-lens fix 6, all via `/plan-eng-review` → decisions → design doc → sliced build. **(1) Dark mode (teacher chrome + editor) SHIPPED + PUSHED (app-only):** `light-dark()` on every `@theme`/`--ed-*` role driven by `color-scheme` (system pref + a bottom-left System/Light/Dark toggle + a pre-paint FOUC guard), `@media print` forces light; editor `.tsx` got the fix-6 sweep it was missed by (134 literals) + the 5 Dev benches; new `dark-contrast.e2e.ts` (6 tests, WCAG-AA across the dark elevation ladder) caught + fixed a real dark accent-strong 3.7:1 bug. 656 unit + 6 e2e green. [dark-mode.md](docs/design/dark-mode.md). **(2) Graph-kit color pass DEPLOYED + LIVE** (kit **`graph-kit-NXZNIEUK.js`**, manifest `850ceca`): 145 color literals → 60, all in TWO source-of-truth files (new [graph-colors.ts](packages/graph-kit/src/graph-colors.ts) + drawable-palette.ts); calculator chrome is now `--gk-*` vars (dark-READY). Value-identical, no wire change. [graph-kit-color.md](docs/design/graph-kit-color.md). **The 2 pre-existing issues** (light `text-success` sub-AA; 4 red `number-line-formula.e2e.ts`) **were ADDRESSED by the author 2026-07-19.** **Deferred:** published-page dark mode (renderer runtime + styles.ts). The board-dark/color-pass commits have been pushed (author-run 2026-07-19). Prior 2026-07-18: **Image crop mode — AUTHORING UI shipped + DEPLOYED** (`67d2af1`/`20e3dff`): 2-D crop-frame gesture, Crop command-bar primary, `.block-image-window` caption fix; owner CR-J1b passed. Prior 2026-07-15: **Columns-as-universal-container arc: slices 1–5 DONE; slice 6 (Notion-hybrid editor) STAGES 0–4 SHIPPED** — stage 0 descriptor-registry spine + command bar + Playwright harness; stage 1 hover gutter cluster + rest dot (focus/tap parity deferred); stage 2 select-state (outline, grip-click + `Esc`, click=edit); stage 3 per-block descriptors (3 batches, all command-bar blocks, universal Duplicate/Delete, image popover-coexistence); stage 4 core Advanced drawer (field-type system + first fields) + 4b (custom rubric field). **Then a dogfooding-driven discoverability fix (author-directed via /plan-design-review):** a real teacher couldn't find how to delete/manipulate a block (Select was grip/`Esc`-only). Added an always-visible mini **quick-bar** — `[🗑 Delete][⋮ More]` top-right of a block on hover + while the caret is in it (`BlockQuickBarHost`); `⋮` selects → full command bar, `🗑` deletes. Grip-click-select removed (2-click bug); grip is drag-only. This also delivers the deferred stage-1 focus/tap parity. 36 e2e + 579 unit green. Remaining: stage 4b (complex sub-editors + image sizing), 5 (picker previews + first-run), 6 (snap motion), 7 (toolbar diet + focus mode); + deferred input-parity/a11y pass + slice 6.5 smart-defaults. Slice 5 = `publish-activity` redeploy author-run (bundle confirmed zero-drift). Slice-6 stage 0 (app-only, no wire/deploy, on `main`): the control-descriptor registry spine — `blockControls.ts` (keyed by node type, `slashMenuItems` discipline) + a single root `BlockCommandBarHost` (4th sibling of the popover hosts; anchored `--ed-ink` docked bar, springs in via new `--ed-spring`/motion tokens) proven on 3 blocks (mathBlock **Edit**, paragraph/heading generic); image held out of the trio (its `ImagePopoverHost` double-fires — stage-3 concern). New **Playwright interaction harness** (`test:e2e`, `e2e/*.e2e.ts` disjoint from vitest by filename) — the eng-required infra the repo lacked: 6 e2e (incl. an on-screen geometry guard that pins a rAF-measure fix) + 5 unit (registry + parity guard). Typecheck + 1741 unit + 6 e2e green; `/playground` screenshot-verified. Next: stage 1 gutter → stage 2 select/click=edit → stage 3 (~20-NodeView extraction). Prior this session — **Columns-as-universal-container: slices 1–3 SHIPPED + whole monorepo GREEN on branch `rows-refactor`** (schema `ed21768` + renderer `3407887` + app `45b72fd`; 1736 tests). Editor uses the **pragmatic bridge (Option A)** — stored schema is fully rows-of-columns; the editor keeps its block stream and serialize wraps/unwraps 1-col rows losslessly. Option A collapsed slice 4; strict grid editor is slice 6. Browser-verified on `/playground` (2-col insert works, zero console errors; caught + fixed a DevFoldableColumns module-level crash). Branch is ready to merge to `main` (author's call) + slice 5 = author-run publish redeploy. Reshape doc root to sections → rows → columns → blocks; `columns` block type removed. Ratified: full reshape; migration CUT (test data deleted); `schemaVersion 1→2` reject-v1; Column holds `block+`; `row` own group; 1-col renders flat; port `Columns.ts` helpers; `sectionBreak` markers stay. 8 outside-voice editor/serialize hazards folded in — editor keymap/node-model is the critical path, not migration. 6 slices, plan + review report in [docs/design/columns-universal-container.md](docs/design/columns-universal-container.md). Prior 2026-07-14: **Add-block UX rebuild** — app-only, no wire/deploy. Replaced the activity toolbar's "+ Insert" dropdown with a centered two-pane **"Add a block" window** (`BlockInsertModal`; alphabetical rail, Layout preselected, search) opened by a hover **insert line** (spans the containing column/cell, inserts above) + a persistent **end square**; the `/` slash menu stays the fast keyboard path, now signposted by a grey `PlaceholderHint` on empty lines. Column-aware: the line reaches inside cells and `topLevelOnly` structural blocks are disabled/hidden when nested (they were splitting the columns block). Blank popover got a "+ More options" disclosure. New reusable [docs/design/ux-lens.md](docs/design/ux-lens.md). Typecheck + **570 app tests** green; `/playground`-verified end to end. **Raised, not scoped:** columns-as-universal-container idea (see Also-queued) — its own future design session. Prior 2026-07-14: **Math fields auto-focus + caret handoff** (`fcc43fc`, app-only). Prior 2026-07-13: Editor UX pass — "+ Insert" dropdown reorg + faded-example numbering rebuild (`publish-activity` redeployed, author-run); ```shortanswer/```essay import fences; Phase 2.6 manual grading full arc deployed + live-verified.)
