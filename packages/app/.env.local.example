# Copy this file to .env.local and fill in the actual values.
# .env.local is gitignored (matches *.local in packages/app/.gitignore).
#
# Get these from Supabase dashboard → Project Settings → API.
# The anon key is safe to expose in client code; access is gated by RLS.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# The district's Google Workspace domain (identity slice E-6) — used as the
# OAuth `hd` hint on STUDENT-facing sign-ins so Google's account picker
# prefers school accounts. UX only; the admission trigger is the gate.
# Unset = no hint (graceful). The e2e lanes deliberately leave it unset.
VITE_DISTRICT_HINT=school-district.org
