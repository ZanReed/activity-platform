# Declarative per-function deploy flags (eng-review ruling A2/P6, 2026-08-06).
#
# WHY THIS FILE EXISTS: whether a student can reach their first screen depends
# on `verify_jwt` being FALSE for the three anonymous functions below. Until
# 2026-08-06 that decision lived ONLY in package.json's deploy scripts — any
# deploy by another route (raw `supabase functions deploy`, dashboard, CI)
# silently re-enabled the platform's JWT gate and 401'd anonymous requests in a
# way that reads as normal sign-in behavior. It bit once (get-feedback's first
# deploy). Policy P6: a decision recorded only in an npm script is not recorded.
#
# AUTHORITY: this file is the authoritative record of each function's flag.
# The `pnpm deploy:*` scripts keep `--no-verify-jwt` baked in as belt and
# braces; if the two ever disagree, THIS file is the intent and the script is
# the bug. After any deploy, verify live flags with the platform advisor
# (`list_edge_functions` → verify_jwt per function).
#
# get-activity is the ONE anonymous function left (its anonymous branch is the
# viewer's 3.2A pre-auth META endpoint — no Authorization header, so the
# platform's verify_jwt gate must stay OFF). The old "anonymous trio" language
# died at S9 Drop 3: ingest-submission + get-feedback (the published-page wire)
# were deleted from the platform — their entries are deliberately GONE, same
# rule as publish-activity's tombstone below: source dirs survive until the
# Drop 4 sweep, so deploy functions BY NAME, never bare deploy-all.

project_id = "activity-platform"

[functions.get-activity]
verify_jwt = false

# Deliberately verify_jwt = true — a check is always made by a signed-in
# student, so the platform gate is a free first line of defense (CLAUDE.md):

[functions.check-activity]
verify_jwt = true

# TOMBSTONE — publish-activity was DELETED from the platform at S9 Drop 1
# (2026-08-14). Publish is a direct `publish_activity` RPC from the app; there
# is no publish function and no publish deploy step. Its `[functions.
# publish-activity] verify_jwt = true` entry is deliberately GONE rather than
# commented-in-place: the source still sits in supabase/functions/ until the
# Drop 4 sweep deletes it, so a bare `supabase functions deploy` (no function
# name = deploy ALL) would redeploy it — and a live flag entry would make that
# resurrection look configured and intentional. Do not re-add this block; if
# the directory is still here, deploy functions BY NAME.
