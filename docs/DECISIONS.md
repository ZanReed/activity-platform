# DECISIONS.md

Architecture decisions and the reasoning behind them, in case future sessions are tempted to re-decide. Moved here from STATE.md (which stays a short "where am I" snapshot). Organized by the stage/feature that produced them; the decisions themselves are durable.

For the hard rules distilled from these (standing constraints, things NOT to do), see CLAUDE.md. For current status, see STATE.md.

## Architecture decisions made (and the reasoning, in case I forget)

### From earlier stages
- **Vertical-stack-of-typed-blocks editor** (Notion-style), not grid canvas.
- **Tiptap on ProseMirror, with React bindings**, KaTeX for math. Vue 3 was the close runner-up.
- **Tailwind v4 via the `@tailwindcss/vite` plugin**. Single `@import "tailwindcss"` in `src/index.css`. No PostCSS, no `tailwind.config.js`. Theme customization in CSS via `@theme`.
- **Design tokens: ONE source, `@theme` in `index.css` (UX-lens fix 6, 2026-07-18, eng-reviewed).** The chrome (routes/ + components/) no longer hand-codes raw palette literals (`bg-slate-50`, `text-slate-600`, `text-green-700`, …) — it uses semantic utilities generated by `@theme` `--color-<role>` (`bg-surface`, `text-muted`, `text-success`, …). Each role points at a Tailwind built-in shade var (`--color-surface: var(--color-slate-50)`), so the generated utilities are pixel-identical to the literals they replaced (a value-identity sweep of ~274 sites; verified by computed-color diff on the Home canary = 0 diffs). The editor's `editor.css` re-points its shared `--ed-*` tokens at the same `@theme` vars (`--ed-canvas: var(--color-canvas)`), so editor + chrome share ONE definition; editor-only tokens (`--ed-text-muted` at slate-500, indigo/teal accents, motion/shadow/z, primitives) stay in `editor.css`. **Two ratified, deliberate shifts (not pure no-ops):** `--color-success` = emerald-600 (unifies the chrome's old green-700 "correct" onto the editor's emerald — one success color app-wide), and `--color-muted` = slate-600 (AA-safe on white AND the slate-50 tinted panels, so muted text can't be picked below 4.5:1 — replaces the hand-picked slate-500/600 split from fix 2). Dark mode is now *possible* (re-point roles under `[data-theme]`) but not built. App-only — no bundle:renderer, no deploy (index.css + editor.css are app CSS; the published runtime bakes its own styles).
- **React Router v7** for SPA routing.
- **`/playground` is dev-only** — route registered conditionally on `import.meta.env.DEV`. Production bundle physically excludes it.
- **Editor lives as shared module at `src/editor/`**, used by `/playground` and `/activity/:id`.
- **Static HTML on publish** (option C). Host: Cloudflare R2 (Stage 13.6+); initially attempted Supabase Storage but Supabase rewrites HTML responses to text/plain.
- **Two URLs per published activity:** live alias (`/index.html`, 5min cache) and immutable versioned permalink (`/v{N}/index.html`, 1yr cache).
- **Drafts in a separate column** (`activities.draft_content`); versions append-only.
- **Permission helpers as SQL functions** so Phase 3+ collaboration changes one function, not eight policies.
- **Submission identity always present** — Pattern B: name field on the page, gate submit until non-empty. CHECK constraint enforces at storage layer.
- **Responses jsonb keyed by stable `blank.id`**, not array index.
- **`schemaVersion: 1`** on `ActivityDocument` stays at 1 — Stage 9a additions are all optional-with-defaults, so existing stored documents parse cleanly. `SubmissionResponses.schemaVersion` was at 2 with migrate-on-read for v1 at the time of this decision; **it is now at 9** (ingest accepts 3–9; source of truth is `runtime/submission.ts` + `ingest-submission/index.ts`). The additive-with-migrate-forward pattern below still holds.
- **`attempt_number` lives on the submissions table as a column**, derived server-side via the `ingest_submission` RPC (`max + 1` over the student's identity scope, with `unique_violation` retry against partial unique indexes added in 0005). The Edge Function returns the canonical value in its HTTP response so the runtime can reconcile its optimistic guess. Client value is advisory only.
- **Runtime answer scoring is strategy-dispatched** — `evaluateAnswer(input, typed)` reads `data-blank-strategy`, defaulting to `'list'`.
- **Math nodes are atoms** with `attrs.latex` as the source of truth. Serialize as `<span data-math-inline data-latex="...">` / `<div data-math-block data-latex="...">`.
- **NodeView lifecycle pattern (the 5 commitments):** `useLayoutEffect` (synchronous before paint), stable ref as render target, tight dependency arrays (`[latex]`), `throwOnError: false`, `NodeViewWrapper as="span"` for inline / `as="div"` for block.
- **React state vs ProseMirror state in NodeViews:** UI concerns → React `useState`. Document concerns → ProseMirror (`updateAttributes`, `editor.isActive`). Conflating them was the cause of the "input deselects after one keystroke" bug.
- **Math NodeView edit-mode focus.** Entering edit mode must explicitly `.focus()` the `<math-field>`, deferred one animation frame (MathLive custom elements don't autofocus, and a focus issued the same tick the element mounts can be dropped). Exiting edit mode is wired to the field's `onBlur`.
- **MathLive in published HTML is a Phase 2.5 decision**, not committed. Editor-only commitment now.
- **Toolbar UX: static (always-visible) over BubbleMenu/FloatingMenu** — discoverability beats elegance for non-technical teachers.
- **Slash menu architecture:** `@tiptap/suggestion` + `tippy.js` + Tiptap's `ReactRenderer` (the `useImperativeHandle` pattern). Menu items live in `slashMenuItems.ts` as a flat array.
- **Drag handle: Notion-style, mouse-only** via `@tiptap/extension-drag-handle-react`. Hover-revealed in left gutter.
- **Keyboard reorder via `Mod+Shift+ArrowUp/Down`** — custom `BlockReorderShortcuts` extension. Operates on whichever top-level block contains the cursor.
- **`Editor` exposes `onUpdate(json)` callback prop** as the single hook for "react to doc changes."
- **JSON inspector lives in `Playground.tsx`, not `Editor.tsx`.** Standard "lift state up."
- **Serialize layer pattern:** `serialize.ts` in `@activity/app/src/lib/`, exports `tiptapToActivity` / `activityToTiptap`. The renderer never touches this file.
- **Three submission modes:** `single` (no checkpoints), `locked` (checkpoints freeze on check), `free` (checkpoints allow revision). Activity-level field on `ActivityMeta`, default `free`. Per-section checkpoints — tied to section boundaries.
- **Two revision modes:** `free` (revise + resubmit) and `locked` (no revision/resubmit). Activity-level, default `free`. Separate from submissionMode — `single` mode ignores `revisionMode`.
- **Attempt tracking:** each resubmit is a new `submissions` row with incremented server-derived `attempt_number`. Teacher dashboard has toggle: all attempts vs best score + count.

### Runtime architecture (Stages 11–12)
- **Runtime: real modules in source, inlined in output.** Source at `packages/renderer/src/runtime/`; built by `scripts/bundle-renderer.mjs` as a first esbuild step (minified IIFE, `chrome90` target) into a generated TypeScript string module (`runtime/generated/runtime-bundle.ts`), which `document.ts` imports and inlines into every published page via a `<script>` tag. Inlining is the right call for Phase 1: one fewer request on a slow Chromebook, runtime stays co-versioned with the HTML it ships in. Separate-file CDN model is a Phase 3+ migration target.
- **Two tsconfigs in the renderer package.** The renderer is pure no-DOM TypeScript; the runtime is DOM TypeScript. `packages/renderer/tsconfig.json` excludes `src/runtime/**`; `packages/renderer/src/runtime/tsconfig.json` adds the DOM lib with `noEmit` (esbuild does the build). The package's `build` and `typecheck` scripts run both `tsc` invocations in sequence so runtime cannot silently rot.
- **Data-attribute contract is a public API.** Renderer emits; runtime reads. Frozen for already-published activities; additive changes only. Every attribute read uses `?? default` fallback. Init and event handlers wrapped in try/catch — graceful degradation to basic submit if init fails.
- **Renderer data-attribute contract.** Blank tokens are `<input class="blank">` inside `<span class="blank-wrapper">` with sibling `.js-blank-feedback` (void-element-can't-have-children fix); always-available hint `?` button (not post-incorrect reveal); mistake feedback re-evaluated each check; `data-submission-mode` deliberately NOT on the container in single mode (checkpoint markup omitted entirely); split-by-purpose contract (CSS hooks → `data-*` on `.activity-container`, JS-only config → six-field `#activity-config` blob); per-block (not per-blank) confidence fieldset.
- **`init.ts` is the only DOM walker.** Returns `{config, refs, state} | null`; parseConfig returns null on missing/malformed config and the caller falls back to no-op runtime. `buildRefs` walks sections → fill-in-blank blocks → blanks once and produces typed `BlankRef` / `FillInBlankRef` / `SectionRef` maps keyed by uuid. Downstream code consumes refs; nothing else calls `querySelector` against the body. Malformed per-element data attributes are warn-and-skip, not throw.

### Runtime feedback machinery + persistence (Stage 13)

- **State→DOM separation: `render(state, refs)` in `render.ts` is the only function in the runtime that mutates the DOM after init.** Every event handler mutates `RuntimeState` then calls a unified `onUpdate` callback that runs render + persist. Concentrating the DOM-mutation exception in one file makes the rule grep-able and enforceable. One exception: `applyStoredState` in `storage.ts` sets `input.value` during bootstrap restoration, BEFORE the initial render runs and before handlers attach.

- **Change-guard pattern in render.** Every DOM write checks the current DOM state and only writes when the target differs. For class toggles, `classList.toggle(name, condition)` is idempotent. For attribute / `hidden` / `checked` / `textContent` writes, explicit current-vs-target checks. Renders are idempotent — calling twice with the same state produces no observable diff.

- **State shape (per-entity, all populated by `createInitialState`):**
  - `BlankState`: `result` (true/false/null), `matchedMistake` (string or null). (No `hintRevealed` — hint visibility is now global modal state, see below.)
  - `BlockState`: `solutionRevealed` (boolean), `confidence` (`'unsure' | 'think_so' | 'certain' | null`).
  - `SectionState`: `checked`, `locked`, `score`, `total`, `checkedAt`. `total` is section total (denominator for score display), NOT attempted count.
  - Top-level `RuntimeState.hintModalBlankId` (`string | null`): which blank's hint the single global modal is showing, or null when closed. Not persisted.

- **`scoreBlankAndUpdateState` renamed from `checkBlank`.** Pre-Stage-13, `checkBlank` mutated DOM via `applyBlankFeedback`. Post-Stage-13, the function mutates state only — render handles DOM. The rename makes the side effect explicit. Takes `(state, id, ref)`; id passed explicitly so the function doesn't read it from DOM (`ref.input.dataset.blankId` would violate the init-walks-DOM rule). Caller has id from Map iteration.

- **`clearBlankState` returns boolean change indicator.** Used by the `input` event handler to skip `onUpdate` (render + persist) on keystrokes that don't actually change state — avoids cascading renders during fresh typing. Returns true only when result or matchedMistake was non-null and got cleared.

- **Mistake feedback matching rules.** String match against `BlankRef.mistakeFeedback` entries' `match` field. Case-insensitive: trim AND lowercase both sides before compare (a student shouldn't lose targeted help over capitalization). First match wins. Computed at scoring time (in `scoreBlankAndUpdateState`) and stored in `BlankState.matchedMistake`; render reads it.

- **Feedback slot policy.** `.js-blank-feedback` carries mistake-specific text only — populated when a wrong answer matches an authored mistake entry, hidden otherwise. Visual correct/incorrect signal lives on the input border class (`.correct` / `.incorrect`). Screen reader signal is `aria-invalid="true"|"false"` on the input itself (removed entirely when result is null, so the attribute doesn't claim "this is valid" before the student has tried). Avoids "Correct ✓" clutter on a 30-blank worksheet.

- **Hint affordance (global modal).** A single hint modal element lives at the end of `<body>` (emitted by `document.ts`); `refs.hintModal` is its `HintModalRef`. Clicking a blank's `?` button sets `RuntimeState.hintModalBlankId` to that blank's id; `render` copies the blank's `data-hint` into the shared modal body, unhides it, and flips that blank's button `aria-expanded`. Setting `hintModalBlankId` back to null closes it. Replaced the old per-blank inline-reveal (`hintRevealed` + `.js-blank-hint-text` span) — one modal gives more room and future rich formatting. Modal open state is NOT persisted across reload (transient UI, unlike scored results).

- **Edit-to-clear behavior.** `wireBlanks` attaches both `blur` (score + render) and `input` (clear stale state + conditional render) handlers. When the student types into a blank that already has a scored result, the input handler clears it so a stale green "correct" border doesn't linger on an edited but un-re-blurred answer.

### Fill-in-blank authoring (Stage 13.5)

- **Fill-in-blank block is a `fillInBlank` Tiptap node** with content spec `(text | mathInline | blank)*`. Inserted via slash menu, the "Problem" toolbar button, or markdown sentinel `{{answer|alt|alt}}` parsing.
- **Blanks are inline atom nodes living ONLY inside fillInBlank blocks** (the schema's `FillInBlankInline` union: text | mathInline | blank). The block's content spec enforces placement at the ProseMirror level.
- **Three insertion paths**: slash menu, sentinel `{{...}}` typed inline (pipe-delimited acceptable answers — matches runtime's `data-blank-answers` format), or "Blank" toolbar button (enabled only when cursor inside a fillInBlank).
- **Chip rendering**: a small inline pill with the canonical answer + underline beneath. Width derived from answer length: `Math.max(answer.length + 1, 4)` ch. Formula must stay in sync between `BlankView.tsx` (editor) and `inline.ts` (renderer).
- **Per-blank editing via a single popover at editor root**, not per-chip. `BlankPopoverHost` watches editor selection (via `instanceof NodeSelection` narrowing); renders `BlankEditPopover` when a `blank` node is the active selection. One floating-ui instance, one portal mount/unmount lifecycle tied to selection. Per-chip popover was tried in Drop 1 and broke widespread editor behavior (slash menu inline-render, fill-in-blanks uneditable, drag handles broken) — diagnosed as React reconciliation issues with N permanently-mounted popovers.
- **Popover state lifecycle**: local React state for editing, refs mirror state for synchronous flush reads, initial-value refs hold the baseline for change diffs. Per-field save-on-blur is the normal commit path; `flushAll()` runs on every close path (Escape, outside click, Enter) so typed-but-not-blurred values survive close.
- **`updateBlankAttrs(pos, attrs, options?)`** — chain command for updating blank attrs by position. `options.preserveSelection` (default `true`) re-applies NodeSelection at the chip's position after setNodeMarkup so the popover stays open across edits. Close-time flushes pass `false` so the subsequent setTextSelection in onClose can move selection cleanly off the chip in one click. Without the flag, the re-asserted selection fights with onClose's selection move and the popover bounces back open, requiring a second click.
- **`onChange` thread carries options through**: BlankEditPopover → BlankPopoverHost.handleChange → editor.commands.updateBlankAttrs. Lets the popover request `preserveSelection: false` when flushing on close.
- **Popover positioning**: floating-ui with `offset`, `flip`, `shift`, and `size` middleware. `size` dynamically computes `max-height` based on available viewport space at the chosen placement, floor `MIN_POPOVER_HEIGHT = 200`. `flip` lets the popover anchor above the chip when there's more space there (tall popovers near page bottom flip up). CSS no longer sets `max-height` — it's controlled by JS via inline style.
- **Focus trap via `focus-trap-react`**. Tab cycles within the popover; Escape and outside-click still fire our handlers (`escapeDeactivates: false`, `allowOutsideClick: true`). Initial focus deferred to our rAF-based answer-field focus rather than FocusTrap's synchronous default. Focus returns to the chip on close (`returnFocusOnDeactivate: true`).
- **Document-level outside-click handler** in BlankEditPopover closes the popover when mousedown lands outside both popover and chip. Covers page-chrome clicks where ProseMirror's selection-change wouldn't fire.
- **Toolbar buttons for insertion**: Three new buttons across two groups.
  - "Problem" (always enabled, inserts empty `fill_in_blank` block)
  - "Blank" (enabled only when `editor.isActive('fillInBlank')`, inserts blank with `?` placeholder, then `setNodeSelection` via rAF so the popover auto-opens for immediate editing)
  - "Section" (in a separate Structure group at toolbar end, inserts section break via `insertSectionBreak()` — SectionBreakView's existing inline UI handles title + checkpoint editing)
- **Serialize layer extended for Stage 13.5**: `tiptapBlankToActivity` and `activityBlankToTiptap` round-trip all four blank fields (answer, acceptableAnswers, hint, mistakeFeedback). Optional fields (hint, mistakeFeedback) only serialized when non-empty for round-trip exactness.
- **`FillInBlank` carries `definingForContent: true`.** Prevents ProseMirror's content-fit algorithm from auto-lifting an inline atom out of a parent block it just emptied — root cause of the empty-block disappearing on the sentinel input rule. `defining: true` would also fix that symptom but breaks drag-reorder asymmetrically (it doubles as `definingAsContext`, which preserves drag-source context too aggressively and makes later blocks un-droppable above earlier ones). `definingForContent` is the targeted granular flag that isolates the destination-side preservation we want without the source-side regression. The Blank input rule handler passes `range` directly to `insertContentAt` (cleaner than the prior recomputed `from`/`to`, though that recomputation wasn't itself the bug).
- **Clicking another blank chip while a popover is open: SWITCH-vs-CLOSE is a known, deferred design decision (2026-06-16).** The user-friendly target is a one-click switch — clicking chip B while editing chip A should retarget the popover to B (flushing A first). Today it instead *closes* A's popover (committing A); a second click opens B. **There is no data loss** — A's edit always commits via the outside-click `flushAll` plus the field's blur (verified by reproduction: type in A → click B, including rapid/synchronous variants; A's value persists). The wart is purely the extra click. **Why it's still deferred:** a one-click-switch was attempted twice and reverted — (1) early-returning from the outside-click handler on a `.blank-chip` target left focus trapped in the popover so the selection never moved to B (stayed on A); (2) adding an explicit host `setNodeSelection(B)` after `flushAll` also failed, because `flushAll`'s `updateBlankAttrs` transaction (`preserveSelection: false`) disrupts the in-flight selection change and the `FocusTrap` holds focus. This is the same selection/FocusTrap entanglement that broke widespread editor behavior in Drop 1, so it needs a dedicated design pass (e.g. a deliberate close-then-reopen that frees the trap, or restructuring the flush so it doesn't fight the retarget) rather than a quick patch. Low priority precisely because there's no data loss.

### Publish flow infrastructure (Stage 13.6)

- **Publish placement: page header, NOT editor toolbar.** Publish is an activity-level action; the toolbar is for editor-formatting controls. Lives in `ActivityEditor.tsx`. Mirrors common patterns (Google Docs, Notion both put "Share" top-right). **Refined 2026-07-18 (two passes):** the header is now a single row of three groups (`flex-wrap justify-between`) — back-link left, the lucide config + nav chip toolbar in the middle, and Publish (a filled slate-900 button, its own shape/fill/position, NOT a chip) right. **Save state rides ON the button** (not a separate text element beside it — that widened the group and shoved Publish): the button reads "Saving…" while autosave is in flight (it's disabled then anyway, since publish waits for the draft to land), "Publishing…" during publish, else Publish/Republish, at a fixed `min-w-[7.5rem]` so the label swaps don't jitter. Save errors surface as a red line below the header (the one save state that must not be silent). Publish's invoke logic moved to the `usePublish` hook so the button and the `PublishStatus` line (below, right-aligned) share one state. Folding Publish into the chip row first (as a chip) overflowed the 768px width and wrapped it to a lonely line; making it a distinct button + **moving Print layout out of the toolbar** (next bullet) freed the width for one clean row.
- **Print layout config lives in the Print view route, not the editor.** Moved 2026-07-18 (was a `ConfigDrawer` section + a `Printer` chip in the editor header). Rationale: you configure the print against its live preview, and it removed the 7th header chip so the row fits one line. The same `PrintSettingsBody` component is now rendered in `ActivityPrint.tsx`'s sidebar; its edits are the **saved** print config (paper/margin/spacing/work space + header fields) and autosave via `useAutosave` (a read-through `activities.update` of `draft_content`, mirroring the editor — an activity with no draft gets one). This replaced the print route's old **session-only overrides** (the "tweak this printout without saving" model): one source of truth now, at the cost of that one-off-tweak feature (author-ratified). Two-tab caveat (editor + print open at once, both writing `draft_content`) is the same pre-existing class the app doesn't guard.
- **No confirmation modal for first publish or republish.** Each publish creates a new immutable version row; nothing destructive happens. Friction cost of a modal isn't justified yet. Easy to add later if version sprawl becomes a real problem.
- **Disabled while `saveStatus === 'saving'`.** Prevents publishing a stale draft mid-autosave. The 1s useAutosave debounce window can still race a fast click; relying on the "Saved" indicator for now. Proper `flush()` on useAutosave is Stage 14 polish.
- **"Publish" → "Publishing..." → "Republish" label progression** in-session. The "Republish" wording surfaces only after the first successful publish in the current session — reduces "wait, did I already do that?" moments without requiring a persistent state read on mount.
- **Success state shows the live URL inline.** The Open link is the critical affordance — it's how the teacher previews as a student. Public URL (live alias) shown; versioned URL recorded internally but not surfaced for routine sharing. **Updated 2026-07-18:** the old green success pill AND the separate persistent `PublishedLink` were two copy-link affordances for the same URL — consolidated into ONE `PublishStatus` line below the header. It renders whenever the activity is live (`isPublished` OR a session publish): an emerald "Live" dot + Open (with an external-link glyph) + Copy link, upgrading to "Published v{N}" for the session that just published (the version is only known then). The live-alias URL is stable, so Open/Copy work on any load, not just the publishing session.
- **Editor load priority: draft → current version → fresh empty.** The publish RPC clears `draft_content` atomically on success. Without the version-content fallback, reloading the editor after publish shows an empty doc (real bug, first reported during Stage 13.6 sketching, fixed same session). Both queries are issued from the same `useEffect` with cancellation guards. `Activities.tsx` always inserts a draft on create, so the fresh-empty branch is defensive (theoretically unreachable via the normal flow).

### Hosting platform (Stage 13.6)

- **Published HTML hosts on Cloudflare R2, NOT Supabase Storage.** Supabase free-tier prohibits HTML content from being served on `*.supabase.co` domains:
  - **Storage**: public buckets serve HTML as `Content-Type: text/plain` with a sandbox CSP, as an anti-abuse measure to prevent the platform from being used for arbitrary web hosting. Documented at https://supabase.com/docs/guides/storage/quickstart.
  - **Edge Functions**: same rewrite is applied at the gateway level — `GET` requests returning `text/html` are downgraded to `text/plain` with the same sandbox CSP, regardless of what the function's `Response` constructor sets. Documented at https://supabase.com/docs/guides/functions/http-methods and https://supabase.com/docs/guides/functions/limits.
  - Only exception is Pro-plan with custom domain ($25/mo); ROADMAP's sustainability model defers paid tiers to Phase 4.
- **Discovered during Stage 13.6 e2e testing.** First attempt was direct Storage public URL → browser saw text/plain, won't render. Second attempt was a `serve-activity` Edge Function proxy that reads from Storage and re-emits with the right Content-Type → same gateway rewrite hit at the function-response level. Both architectures dead-ended for the same root cause.
- **Cloudflare R2 is the chosen replacement.** Reasoning:
  - **Zero egress cost** (the unique R2 feature; every other major object store charges per-GB downloaded). For a K-12 platform with student-driven download volume, this scales sustainably to Phase 5 marketplace.
  - **Generous free tier:** 10 GB storage, 1M Class A operations (writes), 10M Class B operations (reads) per month. Fits Phase 1-3 by a wide margin; even Phase 4 district scale comfortably.
  - **S3-compatible API.** Standard AWS SDK works in Deno via esm.sh. No proprietary library lock-in; migration to another S3-compatible bucket (Backblaze B2, Wasabi, AWS S3) later is a config-and-DNS change, not a code rewrite.
  - **Free custom domain support.** Phase 4 district procurement conversations get easier when activity URLs live at `activities.yourbrand.com` rather than `pub-7a3f.r2.dev`.
  - **ROADMAP-aligned.** Already named as the destination in Phase 2/3; pulling it forward to Phase 1 is acceleration, not deviation.
- **Hybrid architecture: keep Supabase for everything else.** Database, auth, Edge Functions (publish-activity, ingest-submission) all stay on Supabase. R2 hosts ONLY the published HTML output. This is the minimum-viable migration; full Cloudflare migration (Workers + R2 + Pages) is a future option but not warranted now.
- **`serve-activity` Edge Function: deleted.** Was created in this session as the proxy attempt; superseded by R2 (which serves HTML directly with the right Content-Type). Local directory removed; Supabase deployment removed via `supabase functions delete serve-activity`.

### Editor UI for feature fields (Stage 15)

- **Activity metadata was already persisted before Stage 15.** `ActivityEditor` held the full `ActivityMeta` in state and round-tripped it through `draft_content`; only `title` had a control. The other fields loaded and saved back unchanged. So adding pickers was pure wiring to `setMeta` — `changeKey` already fingerprinted meta, so autosave needed no change.
- **Activity settings live in a collapsible disclosure under the title, NOT a modal/gear-drawer.** Discoverability-beats-elegance (same reasoning as the static toolbar over BubbleMenu). Always-visible label, expand for the three pickers.
- **`revisionMode` control is disabled (not hidden) in single mode**, with "Not used in single-submit mode." helper text. Hiding it would make the submissionMode↔revisionMode coupling invisible; disabling makes it legible.
- **`gradingMode` gets no picker in Phase 1.** It's inert (manual/mixed treated as auto until Phase 2.6 per-block grading lands). Surfacing a control would imply functionality that doesn't exist. Add it when manual-graded block types arrive.
- **`activityType` picker is included even though renderer presentation-branching is Phase 2.** It has a real near-term consumer: Stage 16's dashboard filters on it.
- **Locked-mode checkpoint validation WARNS, does not block save.** Blocking save mid-edit is hostile. An amber inline banner (continuous, not just at publish) fires whenever locked mode coexists with any non-checkpoint section. The implicit leading section (content before the first `sectionBreak`) counts as non-checkpoint. Resolves open-question #258.
- **FillInBlank block-field UI is an inline NodeView footer disclosure, NOT a popover.** Mirrors SectionBreakView's inline-controls pattern and sidesteps the per-chip-popover hazards documented in Stage 13.5. The blank-token popover stays for per-blank fields; block-level fields (solution, confidence) get the footer. The footer stays hidden for a plain/unselected/unconfigured problem to keep long worksheets uncluttered (shown when `selected || settingsOpen || isConfigured`). It's `contentEditable={false}` so ProseMirror doesn't treat it as block content; the solution textarea `stopPropagation`s keydown so editor shortcuts don't fire while typing.
- **`solution` was a plain multiline string in Phase 1; Phase 2 upgraded it to inline rich text + math** (now `InlineNode[]`, authored via `InlineRichTextEditor` in the footer — see Current focus).
- **`skills` editing UI deferred to Phase 2** (the schema scheduled it there). The attr round-trips through the node and serialize so data survives, but no control surfaces it. Don't add a skills tag-input without revisiting Phase 2 scope.

### Print feature (Drops A–D)

- **One renderer, not two.** Print is the same `renderBody` path as the screen; print config produces classes / CSS vars / data-attrs consumed under `@media print`. `renderActivity` (published page) and `renderActivityForPrint` (print document, no interactive chrome) share `renderBody`.
- **`@page` can't read CSS custom properties.** So paperSize + margin go out as a literal per-document `<style>@page{size;margin}</style>` (`printPageStyle`); columns/workSpace/fontSize/problemSpacing ride as `--print-*` vars on `.activity-container` where `@media print` selectors read them.
- **Additive optional-with-defaults schema → no `schemaVersion` bump, no migration.** `meta.print` round-trips through `draft_content` for free because `tiptapToActivity` embeds `meta` whole; only per-block `workSpace` needed a Tiptap attr + serialize handling.
- **Print confidence is a paper twin.** The interactive `.js-confidence-rating` fieldset is print-hidden; a separate `.print-confidence` hand-markable checkbox row (screen-hidden, print-shown) renders only for confidence-rating problems. Don't try to print the interactive control.
- **The print route is client-side (Route 2-friendly).** The app imports `@activity/renderer` directly via Vite, so `/activity/:id/print` and the foldable need no Edge Function and no redeploy. `showAnswers` is print-route-only and never reaches `publish-activity`, so its bundle is server-irrelevant.
- **Journal foldable = client-side, DOM-measured (Route 2); never on the published static page.** It needs a live DOM to measure block heights, so it lives only in the app print route. Engine split mirrors the physical process: **pagination** (chunk blocks into fixed-height panels, never splitting a problem) + **composition** (arrange panels onto duplex sheets). Both are pure + unit-tested (`packages/app/src/lib/foldable/{paginate,compose}.ts`); `measure.ts` (offscreen iframe) and `render.ts` (assemble) are the DOM/output glue; `index.ts` orchestrates.
- **Measure === print is the load-bearing invariant.** The offscreen measuring iframe and the printed document share ONE stylesheet (`foldable/styles.ts`), included after the renderer's `blockStyles`, which **promotes the renderer's `@media print` rules to unconditional** (paper-twin confidence shown, interactive controls hidden, blanks→underlines) and zeroes block margins in favor of one controlled flow gap — so a measured border-box height + one gap fully describes the stack, and what's measured is what prints. Geometry is authored in inches; measurement comes back in px at the CSS-fixed 96px/in, and the root font-size is pinned to 16px so `rem` resolves identically in both contexts.
- **Booklet geometry (confirmed from a real artifact).** Single vertical fold, landscape sheet, double-sided → 4 half-sheet panels (2 per duplex face). Reading order `[content #1, #2, #3, blank glue tab]`; the **blank glue tab is always the fixed back cover** (the face glued into the journal). Imposition: outside face `[glue | content #1]`, inside face `[content #2 | content #3]`. **Overflow spills into multiple self-contained foldables** (3 content panels each; trailing slots padded blank) rather than scaling down — author's choice. Teacher instruction surfaced in the sidebar: print two-sided, flip on long edge, fold down the middle.
- **`Columns` is a worksheet-only setting and is hidden in foldable mode.** Worksheet columns = CSS `column-count` (browser flow/balance) on the flat page. The foldable packs blocks vertically into fixed fold regions, which CSS columns can't express, so the control is hidden there rather than shown-but-inert. The *right* long-term answer to "columns in a foldable" is structural authored columns (below), not a per-mode print toggle.

### Structural columns + variable block sizing

- **Columns are *content*, not a print mode.** A `ColumnsBlock` (2–6 `Column`s, columns-in-columns forbidden) lays blocks side by side in the document, so it renders identically on screen, in worksheet print, and inside a foldable panel — replacing the per-mode CSS `column-count` toggle. Per-column `width` is an optional unitless weight (absent ⇒ equal split). Renderer emits a CSS grid driven by a single `--columns-template` custom property so a media query can override track sizing (`@media screen ≤640px` collapses to one column; print/foldable stay multi-column). Full narrative in HISTORY.
- **Reflow-safe sizing only — no free canvas.** The whole variable-block-sizing arc is bounded to sizing that survives multi-output reflow (screen + worksheet print + journal foldable + future Chromebook/mobile): content-determined height, fractional widths, `min-height` floors that still grow, fixed image boxes with cropping. **Out of scope by deliberate decision:** arbitrary fixed pixel heights decoupled from content, and absolute/free-canvas positioning — both break the foldable's height measurement and print reflow (same reason the editor stays a vertical block stack, not a 2-D canvas).
- **One additive sizing fragment, custom-property-driven.** Optional `width` (fraction `(0,1]`) + `align` (`left|center|right`, default center = attribute-free) live in a shared `sizing.ts` fragment spread onto sizable blocks (image, math_block today; callout/problem have no editor mapping yet, so no fields there — YAGNI). Renderer adds a `block-sized` class + `--block-width`/`--block-height` custom properties + `data-block-align`; narrow screens relax widths to 100% (CSS), print keeps them. All additive/optional ⇒ **no `schemaVersion` bump, no `STORAGE_SCHEMA_VERSION` bump, no migration**; the runtime never reads any of it (purely presentational, additive data-attribute-contract entries in RUNTIME.md).
- **Image width: `Auto` (null) ≠ `100%` (1).** `null`/unset = natural size, never upscaled (the default). `width: 1` = a real "fill the container" width that upscales like any other percentage. They are distinct chips because conflating them (the original "Full"-means-null labeling) made a small image sit in an empty row looking broken.
- **Mismatched image dimensions CROP, never stretch.** When `width` × `height` disagree with the natural aspect ratio the rendered box uses `object-fit: cover` (center crop) — a photo-frame, not a funhouse mirror (author's choice over stretch). Height alone scales proportionally. `height` is in **rem** (scales with the print font-size config, like `Column.minHeight`).
- **Lead with reliable controls; drag gestures only where they prove out.** Column width drag-resize was built (a widget-decoration divider) and **cancelled** after it competed for the same gutter strip as the inner-block drag handle and didn't feel reliable under a real mouse — the **width presets remain the column-width system** (schema/serialize untouched, so a re-attempt stays possible; see git history for `columnResize.ts`). The lesson set the pattern for the rest of the arc: image resize ships popover chips/fields as the baseline *plus* drag-handles as an enhancement, and cell reserved-work-space (`Column.minHeight`) ships as a **toolbar dropdown only** (Auto / rem presets / numeric input via `setColumnMinHeight`), no drag.
- **A top-level columns container flows whole through the foldable — verified.** `measure.ts` extracts `.activity-section > *`, and a `columns` block is a single such child, so it becomes ONE `FlowItem` (its cells are never flattened into separately-paginated items) and `paginate` never splits it across a fold (an oversized container gets its own over-full panel, intact). Inner width resolves against the panel: the grid's `fr` tracks size against `.foldable-panel-content`'s fixed `panelWidthIn`, and the narrow-screen collapse (`@media screen ≤640px`) can't fire during measurement because it keys off the 1200px iframe viewport, not the element. Locked in by `foldable-measure.test.ts` (one item per top-level block; cells stay inside; `--columns-template` carries `fr` tracks) + a named `paginate` case, and proven in a real browser via the dev-only `/dev/foldable-columns` bench: a 2:1 container measured `gridTemplateColumns: 286.7px 143.3px` (ratio exactly 2), filling the 432px panel, in a single panel.

### Markdown import (Phase 1 polish)

- **One shared converter, deliberately standalone.** `lib/markdownToTiptap.ts` is `markdown → Tiptap JSONContent[]`, no editor/DOM dependency, exhaustively unit-tested. It's the same converter the queued **PDF import** reuses (a vision model emits the same markdown surface; the client converts it) — so it lives apart from the paste UI on purpose. Design context in `docs/design/pdf-import.md`.
- **App-only and additive — no schema/renderer/runtime/bundle/deploy.** The feature emits the exact JSONContent shapes `activityToTiptap` produces, so imported blocks round-trip through `tiptapToActivity` and render identically to authored ones. Nothing in the pure packages changes; no `bundle:renderer`, no `publish-activity` redeploy.
- **markdown-it, lazy-loaded, kept vanilla.** Chosen over a hand-rolled parser because the strategic consumer is *model-generated* markdown (PDF import, teachers drafting in an LLM): markdown-it IS CommonMark, so "the model emitted valid markdown my parser choked on" never happens. `await import('markdown-it')` keeps it off the initial editor bundle (it only runs behind the Import dialog). Config is stock (`html:false`, `linkify:false`); the custom constructs are resolved *around* it, **never** by patching its rule chain.
- **Two custom constructs resolved in the mapper; math resolved before markdown-it.** Blanks (`{{…}}`) and the `{checkpoint}` marker are read out of the post-parse text tokens — `{`, `}`, `|` aren't markdown-special (tables off), so they pass through verbatim and can't collide with markdown's grammar. **Math is the exception** and can't be done that way: LaTeX is full of backslashes (`\,`, `\frac`), underscores and carets that CommonMark's backslash-escape and emphasis rules corrupt *in the text tokens* (verified: `$\int_0^1 x\,dx$` came back as `…x,dx`). So `$…$` / `$$…$$` is lifted out of the **raw source first** (a `MATH_SCAN` pass that also matches and skips code spans, so `` `$x$` `` stays literal), swapped for a Private-Use-Area placeholder markdown-it forwards untouched, and re-expanded into `mathInline`/`mathBlock` in the mapper. Inline `$…$` uses a Pandoc-style guard (open `$` not before a space, close `$` not after a space/before a digit) so "$5 and $10" isn't math. A whole-paragraph `$$…$$` becomes a block `mathBlock`; inline elsewhere is `mathInline`. We only tokenize — no katex at import time, no rendering plugin.
- **Blank syntax reuses the editor sentinel `{{answer|alt1|alt2}}`** (same grammar as `Blank.ts`'s input rule and the runtime's `data-blank-answers` format). This **supersedes the illustrative `[[answer]]` sketch in `pdf-import.md`** — one mental model, one parser, no second blank syntax. An empty-answer `{{}}` stays literal text (a blank needs ≥1 char).
- **`{checkpoint}` on a heading → a checkpoint `sectionBreak`** (title = the heading text, marker stripped). Plain headings stay heading blocks (clamped to levels 1–3). A non-checkpoint section-break marker was YAGNI'd for v1.
- **Trigger is a header dialog, not paste-anywhere interception** — resolving the open question in ROADMAP.md ("inline vs. dialog"). Predictable, discoverable, never hijacks ordinary paste, and good for batch-pasting; smart-paste can layer on later. The button sits in the page header next to PublishControl (activity-level action, not an editor-toolbar control). Insert replaces a fresh/empty doc outright (no leading blank) and appends to a non-empty one.
- **A blank-bearing list flattens to one `fillInBlank` per item.** "Paste a numbered list of problems and get problem blocks" (ROADMAP.md:48) is the core use case; blanks can only live inside `fillInBlank` (per the PM schema), so the list marker is dropped and the editor re-numbers the resulting problem blocks. A list with no blanks stays an ordinary nested list. Inside a `fillInBlank` (content `(text|mathInline|blank)*`, no hardBreak) any break becomes a space.
- **Coverage is bounded by what the editor round-trips today; everything else degrades, never corrupts.** Covers headings, paragraphs, bold/italic/code, nested lists, blanks, checkpoint sections, `$…$`/`$$…$$` math, and `![alt](url)` images (an inline image is lifted out of its paragraph into a block-level image, splitting the paragraph around it in document order). Unsupported input (code fences, blockquotes, tables, raw HTML, links) flattens to text / drops the unsupported attribute with a deduplicated, human-readable warning surfaced in the dialog. Verified against the live ProseMirror schema (math KaTeX-renders, backslashes intact; image block carries src+alt).
- **The dialog's close is decoupled from FocusTrap — never `onDeactivate: onClose`.** First attempt tied close to the trap's `onDeactivate`; under React StrictMode the dev double-mount unmounts the trap, fires `onDeactivate`, and the dialog vanished the instant it opened. Close is now driven only by our own Escape-keydown + backdrop-mousedown handlers (`escapeDeactivates: false`, no `onDeactivate`) — same shape as `ImageEditPopover`. A StrictMode RTL regression test (`ImportMarkdownDialog.test.tsx`) renders under `<StrictMode>` and asserts the dialog stays open; reintroducing `onDeactivate: onClose` fails it. (`tabbableOptions: { displayCheck: 'none' }` lets the trap activate under jsdom — prod-safe, all elements visible.) RTL is the repo's first component-test infra.
- **The lifted editor instance is reported up but never nulled on cleanup.** The header Import button gates on the editor instance, lifted from `Editor` via `onEditorReady`. The effect reports only a non-null editor and never nulls on cleanup — nulling on the intermediate StrictMode/keyed-remount cleanup could leave the button stuck disabled. (A reported disabled-button was a stale dev server, not this; the hardening is defense-in-depth so the symptom can't recur.)
- **The accepted format is a single, drift-guarded source of truth.** `docs/markdown-import-format.md` is the authoritative spec and doubles as an LLM prompt; the canonical prompt is `MARKDOWN_IMPORT_AI_PROMPT`; the converter is the behavior. `markdownImportPrompt.test.ts` binds all three: the doc's prompt block must equal the constant byte-for-byte, every documented example must run through the live converter as promised, and the documented "unsupported" list must actually degrade. The Import dialog's **Copy AI prompt** button hands a teacher the prompt to paste into ChatGPT/Claude.
- **The AI prompt tells the model to fence its whole reply; the importer unwraps an outer fence.** LLMs render markdown in chat, so asking for "markdown" yields an uncopyable formatted preview. Wrapping the reply in a fenced code block makes the chat show a Copy button that yields raw markdown (the button strips the fence, so the importer never sees it). As a safety net the converter unwraps a whole-paste ```` ```markdown ```` / ```` ```md ```` fence before parsing — scoped to a markdown-tagged OUTER fence, so a plain ```` ``` ```` block mid-content is still flattened as code.

### Activity deletion — soft-delete via SECURITY DEFINER RPC (0008)

- **A client-side soft-delete (`update activities set deleted_at`) is fundamentally incompatible with the `deleted_at is null` SELECT policy, and no `WITH CHECK` tweak fixes it.** PostgreSQL enforces the table's SELECT policy against the **post-update row**, not just the UPDATE policy's own `USING`/`WITH CHECK`. `activities_select_own` requires `deleted_at is null`; the moment the soft-delete writes a timestamp, the new row is invisible to SELECT and the entire UPDATE is rejected with *"new row violates row-level security policy for table activities"*. Verified by isolating each clause against the live Postgres engine (synthetic temp tables, rolled back): the error persists even with the UPDATE policy at `WITH CHECK (true)`, and disappears only when the SELECT policy is dropped or its `deleted_at` clause removed. The original "the update policy permits an owner to set deleted_at" assumption (in `0002`'s comment and the first delete implementation) was never exercised until a real delete was attempted. `publish_activity` was unaffected purely because it never touches `deleted_at` — the one column the SELECT policy keys on.
- **Fix: a `SECURITY DEFINER` RPC `soft_delete_activity(p_activity_id)` (migration `0008`), not a relaxed SELECT policy.** The RPC runs as `postgres` (which has `BYPASSRLS`, and `BYPASSRLS` overrides `FORCE ROW LEVEL SECURITY`), authorizes via `can_edit_activity` (owner of a live activity — the same helper `publish_activity` uses, so Phase 3+ collaborator roles flow through for free), tombstones the row, and writes an `activity.delete` audit entry. The strict SELECT policy stays intact, so deleted rows remain hidden from clients exactly as before — chosen over dropping `deleted_at is null` from `activities_select_own` (the simpler migration) specifically to keep RLS, not just the app query, as the guard that hides soft-deleted rows. The client calls `supabase.rpc('soft_delete_activity', { p_activity_id })` instead of `.update()`.

### Order-independent blank groups (consume-once)

- **The problem the naive fix can't solve.** Factoring `(x + ☐)(x + ☐)` should accept `(2,3)` and `(3,2)` but reject `(2,2)`. Widening *both* blanks' acceptable sets to `{2,3}` accepts the swap but *also* wrongly accepts `(2,2)`/`(3,3)`. Rejecting the duplicate requires the blanks to coordinate so each correct answer is consumed once — which independent per-blank scoring can't express. So grouping is a real cross-blank concept, not an acceptable-answers tweak.
- **Be general where change is expensive, minimal where it's cheap.** Three layers with very different cost-to-change drove the design: the **runtime data-attribute contract** (every published page bakes it in; additive-only) is made *general now* — blanks carry a `data-blank-group="<id>"` and the runtime buckets by id, so richer grouping later needs no contract change; the **document/schema** layer (additive Zod fields are cheap here) stays *minimal* — one boolean `BlankToken.interchangeableWithPrevious`; the **editor + dashboard** (no external consumers) are *minimal*. Making the runtime contract general cost nothing extra (the runtime buckets by id either way), so generality landed exactly where retrofitting would be most painful.
- **Authoring is "grouped with previous", a boolean sugar that compiles to group ids.** A group = a maximal run of adjacent blanks each flagged `interchangeableWithPrevious` (the run's first blank anchors it; its own flag is ignored). The renderer ([`inline.ts` `computeBlankGroups`](../packages/renderer/src/inline.ts)) assigns the run's anchor blank id as the shared `data-blank-group`. Chosen over a block-level "any order" flag (grades wrong on a block mixing a factor pair with an unrelated blank) and over explicit cross-block group ids (needs a multi-blank-select UI that collides with the single-host popover's FocusTrap/selection model). The boolean is a *strict subset* of group ids, so upgrading to non-adjacent/cross-block grouping later is an additive `group?: string` field, never a breaking migration.
- **Consume-once = maximum bipartite matching, deterministic tiebreak.** [`runtime/blanks.ts` `scoreGroup`](../packages/renderer/src/runtime/blanks.ts) matches typed values to answer *slots* (each slot keeps its own answer list + strategy — we match against per-slot lists, never a flattened pool, which is what preserves consume-once) via Kuhn's augmenting path. Groups are 2–4 blanks, so cost is trivial. Values are processed in document order, so among equal duplicates the *last* one is the one left unmatched — a deterministic tiebreak (removes a latent flaky-test source). Empty members stay unscored (null), mirroring solo-blank behavior. `scoreBlanksInScope` buckets a scope's blanks by group id and is called by **both** `checkSection` and the submit `gatherResponses`, so check-time and submit-time verdicts can't diverge.
- **Grouped blanks resolve at check/submit, never on immediate-mode blur.** A grouped blank can't be judged in isolation (its group-mates may be empty/mid-typing), so scoring it on blur would flash a misleading ✗ on an answer that's correct once matched. `wireBlanks` skips the immediate-mode blur scorer for blanks with a group id; they resolve when a group-aware scope runs. An inherent, accepted constraint of the feature.
- **Client stays authoritative — no Edge Function change.** `ingest-submission` Zod-validates and stores the client's per-blank `correct` + `score` verbatim; the dashboard *displays* the stored verdict (it doesn't rescore). So grouping is contained to schema/renderer/runtime/editor, and the "make answer evaluation server-shareable" refactor stays parked for photo-grading (where a server/human actually regrades). Verified before building, since a server rescore would have silently failed `(3,2)≡(2,3)`.
- **Markdown marker is a leading tilde, `{{~3}}`.** Picked because `~` is captured by the existing `blankCanon` group (`[^{}|]+`) and rarely begins a real answer (a `|` prefix would collide with the alternate-answer separator). The importer strips it and sets the flag; on a problem's first blank it's a harmless no-op (the renderer ignores a leading flag). Kept in three-way sync (converter / `MARKDOWN_IMPORT_AI_PROMPT` / format doc) under the existing `markdownImportPrompt.test.ts` drift guard.
- **Dashboard shows the group's set, not the slot's own key.** A grouped blank's "answer key" column would otherwise show *that slot's* canonical answer, which can differ from the student's (correct) entry and read like a bug. `buildActivityIndex` attaches the group's full answer set to each member; the drill-down renders "x or y (any order)". The ✓/✗ was always right; this fixes the *explanation*.

### Reference panel (Phase 2)

Optional teacher-authored scaffold content (formula charts, vocab lists, conversion tables…). Shipped in three drops — A: persistence (serialize round-trip), B: rendering (screen + print), C: authoring. The schema field `ActivityDocument.referencePanel` pre-existed (Stage 9e forward-compat); no `schemaVersion` bump was needed anywhere.

- **Runtime is untouched — the panel renders OUTSIDE `.activity-section`.** The runtime's `init` walker scopes every query to `.activity-section`, so anything outside it is invisible to scoring / persistence / checkpoints. The panel renders as an `<aside>` / `<details>` outside the sections, so it needs zero runtime code and no `STORAGE_SCHEMA_VERSION` bump. `data-block-category="scaffold"` is therefore an analytics/CSS hook, NOT the scoping mechanism (category is a coarse discriminator, never the gate).
- **Screen = summonable floating window (calculator pattern); print = static box at the top.** (Author-requested 2026-07-08; supersedes the earlier collapsible bottom-bar toolbar.) The bottom bar permanently occupied viewport width and needed scroll-clearance machinery; the calculator had meanwhile established a better pattern — a summon button + a draggable, resizable floating panel that slides off the work. One `referencePanel` still drives both presentations: `renderReferenceTool` emits a `.reference-summon` button (in the shared `.tool-corner` bottom-right cluster, beside the calculator's) + a hidden `.reference-float` fixed window; `.reference-print` (hidden on screen, shown `@media print`) is unchanged at the worksheet top.
- **Panel content stays SERVER-RENDERED; the kit is not involved.** The panel is static scaffold HTML (KaTeX baked in at publish) already shipped in the page — the sidecar only toggles/drags a window around it. Pulling the graph-kit for window chrome would cost hundreds of KiB and break panels on pages with no kit URL. The sidecar (`runtime/reference-panel.ts`, ~1.3 KiB IIFE, inlined ONLY when an activity has a panel — kept OUT of the scoring runtime so its strict init/render/state invariants stay untouched) reimplements the calculator kit's header-drag (~40 lines, same clamps); resize is the native CSS `resize:both` handle (the body owns scroll; the panel keeps `overflow:hidden`).
- **Floating-panel conventions (match the calculator):** summon button hides while the panel is open (they'd overlap otherwise); × or Escape closes (Escape respects `defaultPrevented` so an inner definition popover can consume its own); focus moves panel→button with the state change (the panel carries `tabindex="-1"`); `role=dialog` NON-modal. Panel anchors bottom-LEFT so an open calculator (bottom-right) never collides; z-order: cluster 110 < reference-float 115 < calculator panel 120 < popovers 1000. Geometry memory is free — drag/resize write inline styles and the element is never destroyed, so size/position survive close/open for the page session (deliberately not localStorage, same call as the calculator). Without JS the panel stays `hidden`; the print box is the JS-free surface (accepted: published pages already require JS for everything interactive). The old scroll-clearance `ResizeObserver` + `has-reference-panel` container padding are deleted — a floating window hides nothing permanently.
- **Print inclusion is a single on/off toggle** (`PrintConfig.printReferencePanel`, default true). A teacher with a class set of printed charts turns it off; the on-screen toolbar is unaffected. Additive + defaulted → no schemaVersion bump. Granular per-component print selection deferred until asked for.
- **Authoring uses an ISOLATED `ReferencePanelEditor`, not a parameterized `<Editor>`.** The main editor is the repo's riskiest surface (single-host popover, `defining`/drag invariants, NodeViews); a separate component keeps that blast radius at zero. It registers a constrained extension set and runs the toolbar in `variant="reference"` (hides Problem / Blank / Section). The shared `Toolbar` gained only an additive `variant` prop — the main editor (default `'activity'`) renders exactly as before.
- **Panel fingerprint uses the editor's Tiptap JSON, not the serialized panel.** `tiptapToReferencePanel` mints fresh block UUIDs per call, so feeding the serialized `ReferencePanel` to change-detection would churn the autosave key (the standing "fingerprint Tiptap JSON, never serialized docs" rule). `ActivityEditor` holds `panelJson` + `panelTitle`, gates `changeKey` on *both* editors having reported (so the autosave baseline settles in a single transition — no spurious load-time save), and `panelFromEditor` reconstitutes the panel at save time (returns undefined for an empty panel, so an empty scaffold is never persisted).
- **`FillInBlank` / `Blank` are registered in the panel editor only to satisfy the schema.** The `column` cell's content expression references `fillInBlank` (which references `blank`), so a panel editor that offers columns can't build its ProseMirror schema without them. The reference toolbar still hides the Problem / Blank buttons, so a teacher can't *author* a blank in a panel; a pasted one renders inert (outside `.activity-section`). Cleaner alternatives (decoupling the shared `column` content spec, or dropping panel columns) were heavier, and the author had approved columns for formula-chart layouts.
- **StarterKit v3 bundles `Underline`** — discovered while debugging the panel editor's schema crash. The explicit `@tiptap/extension-underline` registration (added in the earlier underline commit) is redundant and warns "Duplicate extension names: underline". The panel editor relies on StarterKit's; the main editor's redundant copy is queued for cleanup.

### Inline vocabulary definitions (Phase 2)

Select text → "Define" → author a definition shown in a popover on the published page. Targets the math-vocabulary barrier (e.g. "factor" as noun vs. verb). Full design + rationale in [docs/design/vocabulary-definitions.md](design/vocabulary-definitions.md); the load-bearing decisions:

- **A mark, not a node.** The defined text is still text — it wraps, layers other marks, and is authored by selecting a run, not inserting a block. The definition *content* (rich, possibly with an image) is separate from the inline *trigger* (the mark).
- **Marks moved from a string enum to an object discriminated union** (`{ type, …attrs }`) so a mark can carry attributes — `definition` is the first attribute-carrying mark (`link`/`color` are latent future ones). Chosen over a minimal "string marks + one parallel field" approach: object marks are uniform (no heterogeneous `string | object` array), match Tiptap's own model, and future-proof. **No data migration** — the DB stores Tiptap JSON (already object marks); `ActivityDocument` is a transient publish artifact. A `z.preprocess` upgrades legacy bare-string marks (and, later, the legacy plain-`definition`-string mark) on read, so old documents parse with **no `STORAGE`/`schemaVersion` bump**.
- **Definition content is rich (text + inline math) + one optional image**, not a plain string — "a picture worth a thousand words." Content reuses the blank-hint stack: authored via the shared `InlineRichTextEditor`, stored as canonical `InlineNode[]` in the mark attr, pre-rendered by the renderer. The content's text run uses a **restricted inline type (simple marks only)** — this forbids nested definitions AND breaks the schema recursion that reusing `InlineNode` would create (`DefinitionMark → content → text → marks → DefinitionMark`). One image (not a full block document) was the deliberate scope: covers ~all vocab cases without a cramped block-editor-in-a-popover. Image reuses the existing `upload-image` / R2 path.
- **Runtime is a separate sidecar, not the scoring runtime.** Definitions appear anywhere inline text does (including the reference panel, outside `.activity-section`), are non-scored and non-persisted. So `runtime/definitions.ts` (~1.9 KiB, inlined only when a page has a definition) mirrors the reference-panel sidecar pattern: it manages its own popover and is kept out of the scoring runtime's strict init/render/state invariants. The renderer pre-renders content into a hidden `<template class="js-definition-content">` next to the span (KaTeX baked in); the sidecar **clones** it into the popover (never re-renders), falling back to a plain-text `data-definition` attribute. Additive to the frozen data-attribute contract.
- **Editing is a single root-level host** (`DefinitionPopoverHost`), mirroring `BlankPopoverHost` — never per-mark mounting (the standing reconciliation constraint). Mark-based (vs. the blank/image node hosts) via `getMarkRange`; the range start keys the popover so its draft reloads only on retarget, and commits land on close (a per-keystroke `extendMarkRange` would churn the undo stack).
- **Phase 4 glossary is account/tenant-scoped, not per-activity** (decided 2026-06-19). The eventual glossary lives in a tenant-level `glossary_entry` table (NOT `ActivityMeta`), resolved at publish; the mark references it by the already-reserved `glossaryKey`. The leaning is the **light "implicit-reuse"** model (the platform remembers a teacher's definitions and offers them back) over a heavyweight managed glossary; an official managed glossary, if built, is opt-in. **Auto-suggest** (find glossary terms in text) must be **suggest-don't-apply** — never silent/automatic at render — because of word-sense ambiguity, over-marking, teacher intent, and assessment integrity. None of this affects the shipped mark.


### Calculator parity — Tier A shipped, Tier B deferred on pedagogical grounds (2026-07-11)

The graphing calculator was pulled to parity in one deliberate batch (the strategy: batch-to-parity then gate by `CalculatorRestrictions` flags, never dribble features in one at a time). **Tier A** — everything the current math.js number-only + solve-for-y (linear-in-y) engine can express — shipped: inequality graphing in the calculator (gated by `allowInequalities`) and on display curves, per-expression domain restrictions, the widened function table (reciprocal + hyperbolic trig, floor/ceil/round/sign, nPr/nCr, min/max/mean), and logarithmic regression end to end. Commits `c8f2a38` + `eb92fb3`; deployed + author-confirmed same day.

- **Tier B is DEFERRED for a pedagogical reason, not a cost one — do not re-pitch it as "cheap now that the engine seam exists."** Tier B is the set that needs the Compute Engine jump (the documented engine-upgrade escape hatch): implicit/conic graphing (x² + y² = 25), symbolic derivatives, polar/parametric, real statistics/distributions. The author's call (2026-07-11): **that power has no educational value for the basic problem-solving this platform teaches** — a tool strong enough to do the work removes the work. So the deferral is about learning value, not implementation effort; a future session that notices the engine seam is in place and proposes Tier B "while it's easy" is re-deciding a settled question. Revisit only if the *curriculum* target changes (e.g. a calculus or AP-stats track), not because the plumbing got convenient.
- **The engine boundary is still the batchability line.** Anything that fits math.js number-only + solve-for-y is a bounded, in-scope batch; anything requiring symbolic/implicit evaluation is Tier B behind the (still-unmade) Compute Engine decision. Design ref: [docs/design/calculator-tool.md](design/calculator-tool.md).
- **Two build-time findings worth not rediscovering:** (1) MathLive serializes unknown multi-letter identifiers as SPACED letters (`floor(2.7)` → `f l o o r(2.7)`, `10 mod 3` → `10m o d 3`), so `normalizeAsciiMath` reassembles the provided names via letter-boundary lookarounds (not `\b` — digits abut letters) — caught only by driving the real widget, not unit tests. (2) The planned `<`/`≤`/`≥` keypad graft was unnecessary: MathLive's built-in numeric layout already ships those keycaps, and a gated-off inequality key simply yields the friendly row message (physical-keyboard parity), so no suppression is needed.

### Runtime size budget amendment (2026-07-10)

The inlined-runtime budget moved from 20 KiB target / 40 KiB ceiling to **40 KiB target / 60 KiB ceiling** (minified; warned/enforced by `scripts/bundle-renderer.mjs`). Context: matching + ordering pushed the base variant to 35.8 KiB and the graphs variant to 38.5 — 1.5 KiB under the old ceiling — and the old 20 KiB target had already been breached (and ignored) since numeric + MC landed. The reasoning, so the numbers aren't re-litigated from scratch:

- **The original 20/40 was picked, not derived.** No education platform ships anything near it (measured comparables: Desmos calculator ~1 MB transferred, DeltaMath landing ~360 KB; Carnegie and Amplify players heavier still). The budget is self-discipline, not market parity — worth keeping, but not worth defending a specific arbitrary number.
- **What changed in the requirements:** the paper-first long-term workflow (student works the printed sheet while the page loads; the digital activity serves as answer key + AI-feedback companion) takes first-paint latency off the critical path. What stays binding on floor hardware (school Chromebooks on 6–8-year district replacement cycles — the one domain where "hardware will improve" reliably doesn't) is JS parse/execute cost, which scales with minified size regardless of network. Hence a ceiling still exists and still fails the build.
- **A target below the shipped size is worse than a bigger target.** Base sat at 21.2 KiB against the 20 KiB target for sessions — a permanent warning everyone learned to scroll past. 40 restores the warning's meaning; 60 leaves room for one more question-type generation before the structural levers must be pulled.
- **The load-bearing rule is the kit invariant, not the number.** Heavy capability (JSXGraph, MathLive, mathjs today; the photo-submit/AI-feedback client tomorrow) lives in lazy, content-hashed kits on R2, cached across activities; a plain worksheet ships only the small inlined runtime. Raising the ceiling does NOT license runtime dependencies or eager widgets — "No JS dependencies" and "No `@activity/schema` import" stand unchanged.
- **Prescribed response when a variant nears 60** (in order, never quietly bumping the constants): (1) dedupe the four per-question-type solution/confidence chrome loops in `render.ts` (+ their init/submission parallels), ~1–2 KiB; (2) per-question-type inlining variants — `document.ts` already picks base vs graphs by body scan, the same mechanism can give blanks-only worksheets a slimmer bundle; (3) kit-ify the heaviest interaction's DOM plumbing behind a thin bridge (scoring/restore/submit stay inline so answers survive a kit-load failure) — the pattern proven by the 2026-07-10 graph-runtime-into-kit move (27.8 → 23.8).

### Supabase security/performance housekeeping (0009, 2026-07-11)

Migration `0009_security_housekeeping.sql` — driven by a security + performance advisor run (2026-07-11), with every fix verified against the live catalog and the real call sites before writing SQL. What it changes is documented in the migration itself; this section records the decisions and the deliberate non-fixes so they aren't re-litigated at the next advisor run.

- **`activity_aggregate_stats` was a real cross-teacher leak, closed with `security_invoker`.** The 0003 view defaulted to definer semantics (owner `postgres` bypasses RLS) and Supabase's default grants exposed it to every signed-in user — submission counts, average scores, and `owner_id` for ALL activities. Nothing queries the view yet (Phase 5 prep, grep-verified), so the flip is observably free. The Phase 5 cross-teacher aggregate stays what 0003 planned: an explicit SECURITY DEFINER *function* with its own authorization check — deliberate definer semantics belong in a function, never in a view's implicit default.
- **RPC lockdown: revoking `anon`/`authenticated` alone would have done nothing — PUBLIC held EXECUTE.** The live ACLs carried `=X/postgres` (Supabase's default grant to PUBLIC) on every function, so 0009 revokes PUBLIC too. Grants after the revoke follow verified call sites, and one brief-level assumption was corrected in the process: **`publish_activity` is NOT called with the service role** — the publish Edge Function calls it with the *user's JWT* (its header comments even document dropping `SUPABASE_SERVICE_ROLE_KEY`), so `authenticated` keeps EXECUTE on it (and on `soft_delete_activity`, called client-side). `ingest_submission` is service-role-only — before 0009, a student who found `/rest/v1/rpc/ingest_submission` could bypass the Edge Function's Zod validation and ip-hashing entirely. **Follow-the-pattern rule: Supabase's default privileges still grant every NEW function to PUBLIC/anon/authenticated, so each future RPC migration needs its own revoke/grant stanza.** (`ALTER DEFAULT PRIVILEGES` was considered and deferred — it changes behavior invisibly for future migrations; an explicit per-RPC stanza is boring but self-documenting.)
- **`rls_auto_enable` provenance recorded.** It's the function behind the `ensure_rls` event trigger (auto-enables RLS on new public tables) — created via the dashboard, never in a migration. 0009 locks its EXECUTE down (event triggers don't check EXECUTE at fire time) and memorializes it in the migration comments so the next catalog-vs-repo diff doesn't treat it as a mystery.
- **Helper `search_path` pinned to `''` with schema-qualified bodies; known inlining trade accepted.** Any `SET` clause disables SQL-function inlining, so the three helpers became real function calls in the policies that use them. Irrelevant at current row counts, and they were never in the advisor's initplan findings; noted so a future perf pass knows where the inlining went.
- **`ALTER POLICY` does change USING/WITH CHECK clauses — 0006's "drop + recreate is the only way" comment was wrong.** It has worked since Postgres 9.5 and is atomic (no window without a policy), which is how 0009 applies the 9 `(select auth.uid())` initplan rewrites.
- **Kept as-is, on purpose (the advisor will keep flagging these):**
  - `allowlist` + `audit_log` RLS-with-no-policies (INFO) — deny-by-default, service-role-only, 0002's explicit design.
  - The two permissive SELECT policies on `submissions` (WARN) — consolidation into one OR'd policy is provably semantics-preserving (their `assignment_id is null` / `is not null` guards are disjoint and permissive policies OR together anyway), but they model the two access paths (link-share via activity owner; assignment via teacher) that 0002's comments slate to diverge in Phase 3/5. Cost is one extra short-circuiting policy check per row; merge only if a future perf pass measures it.
  - `unused_index` INFOs — no meaningful traffic yet; revisit with real usage data.
  - Leaked-password protection (WARN) — auth is Google OAuth only; there are no passwords to protect. Revisit only if a password provider is ever enabled.
  - **`publish_activity` + `soft_delete_activity` keep an `authenticated_security_definer_function_executable` WARN — that residue is correct, not a miss.** Both are genuinely called by signed-in users (publish via the Edge Function with the user's JWT → the `authenticated` role; soft-delete client-side), and they *must* be SECURITY DEFINER to bypass RLS for their audited writes, with `can_edit_activity` as the real gate. The advisor flags any DEFINER function callable by `authenticated`; the only ways to clear it — SECURITY INVOKER or revoking `authenticated` — each break a real call site. So this WARN stays by design. (0009 did clear the far more dangerous variants: the view ERROR, all `anon`-executable functions, and the 4 DEFINER functions that had no client caller.)
- **Applied + verified 2026-07-11 (author applied via the pushed deploy; Claude ran the read-only verification through the Supabase MCP).** All eight checks passed: view is `security_invoker`; DEFINER-function EXECUTE grants carry no `anon`/`PUBLIC` (`ingest_submission` service-role-only; publish/soft-delete keep `authenticated`); helper `search_path` pinned to `''`; zero policies evaluate `auth.uid()` per row; five FK covering indexes present; a two-identity impersonation test (real owner vs. a non-owner, rolled back) confirmed cross-teacher isolation survives the initplan rewrite (owner saw 14 activities, non-owner saw 0 activities / 0 assignment_students / 0 submissions / 0 update rows); and the advisor re-run left only the intentional residue above. The step-7 app smoke (publish / delete / submit through the running app) is the one check left to the author.

### Faded worked example — box is one problem, steps are lettered (2026-07-13, reverses a 2026-07-12 call)

The original faded_worked_example (2026-07-12) reused fill_in_blank wholesale, so each faded step was a full problem: it pulled `nextProblemNumber` and rendered a 2.5rem number gutter. In practice (author feedback) that was wrong on three axes — **feel** (a 2-step Guided practice stole worksheet numbers 1–2, pushing the first real problem to #3), **formatting/spacing**, and **print/writing width** (the gutter + box padding ate ~4rem before the answer area). Redesign, author-ruled:

- **The whole box is ONE numbered problem**, its number leading the title ("3. ✍ Guided practice"). `renderFadedWorkedExample` pulls `nextProblemNumber` once; the editor's `problemNumberAt` treats the box as **atomic** (+1, don't descend) so its steps never inflate the count. `isNumberedBlock` gains `faded_worked_example` at type level; the "don't count nested steps" exception is context-dependent and lives in the renderer/editor walk, not the predicate.
- **Faded (fill_in_blank) steps are lettered (a)/(b)… LOCALLY** and rendered gutter-less (a run-in inline label, `is-faded-step` → `display:block`), for maximum writing/print width. Only the faded steps are lettered; shown exposition steps stay unlabeled.
- **Opt-out:** new `showStepLabels: boolean` (default true) on the schema block + a per-box toggle. Default ON — labels help students/teachers reference "part b" — but a teacher can drop them for bare blanks. **Editor reactivity gotcha:** a child fill_in_blank NodeView can't react to its parent's attr change, so the step always renders its letter and the *parent* box hides them via an `is-labels-off` CSS modifier (the parent re-renders on its own attr change). The renderer, reading the attr at render time, simply omits the label.
- **Display-only, so no wire/storage bump:** scoring still rides the child blanks by section scan. Reaches published/print pages on the next `publish-activity` redeploy. This supersedes the 2026-07-13 gutter-narrowing tweak (which only sanded one corner of the same problem).

### Student identity S1 — admission, containment, classes, compliance (2026-07-28)

First landed slice of the components-as-data re-architecture's identity lane (rulings 2B/TV1-A/3.1C in `~/.gstack/.../user-main-design-20260728-components-as-data.md`). Migrations 0013/0014 (author-run; walkthrough in `scripts/verify-0013-0014.sql`).

- **Students live in `public.users` with a new `role='student'`** — one auth mirror, not a parallel `students` table. A parallel table would fork every join and the signup trigger; the risk of the shared table (students inheriting teacher grants) is closed by containment guards instead.
- **Admission is domain-gated in the signup trigger, allowlist wins ties.** `handle_new_auth_user` checks exact-email allowlist first (teacher, unchanged behavior), then `student_domain` (lowercase exact-match; subdomains listed explicitly), else rejects. The `hd` OAuth param is UX only — the trigger is the gate. `student_domain` mirrors allowlist's posture: RLS forced, zero policies, service-role only.
- **Containment: `current_user_is_teacher()` appended to the five authoring policies.** Until 0013, "any authenticated user" ≡ "teacher" — the moment students exist, `activities_insert_own` et al. would have let them author. The helper is SECURITY DEFINER (avoids users-RLS entanglement inside other tables' policies), pinned search_path, `authenticated`-only EXECUTE (0009's follow-the-pattern rule: every new function gets its own revoke/grant stanza — Supabase still default-grants PUBLIC).
- **Classes are the 13+ assertion carrier (3.1C).** `classes.age_assertion_at/by/text_version` are NOT NULL — an assertion-less class is unrepresentable; `assertion_text_version` rides `POLICY_VERSION` (lib/policyVersion.ts, drift-guarded against docs/compliance/privacy-policy.md) so a wording change is distinguishable in the record. The UI checkbox is unchecked-by-default and resets per class (no sticky consent), guard-tested.
- **Cross-table class RLS uses SECURITY DEFINER helpers** (`is_class_teacher`/`is_class_member`) — classes policies consult class_members and vice versa; invoker helpers would re-enter each other's policies (the 0002 recursion hazard in cross-table form). `join_class` RPC is the only write path into class_members (no client INSERT policy); rejoin-after-removal reactivates, lockout = remove + regenerate code. `list_class_members` RPC exists because users RLS is self-only — a teacher cannot join to users client-side for roster names.
- **`submissions.student_id` is the third identity branch** in `submissions_identity_present`, added in S1 (not S4) because lanes A and B both need the identity model settled — S4 would otherwise fork the migration mid-lane. ON DELETE RESTRICT deliberately: the retention job must delete submissions before the user row; wrong order fails loudly. Account rows carry `student_id` ONLY (invariant enforced by the future S4 RPC), keeping the 0005 partial attempt indexes inert; `submissions_account_attempt_idx` guards the account-scope max+1 race.
- **Scope amendment (author, 2026-07-28): there are no old published pages to maintain.** The live R2 pages are the author's own tests. Consequences: TV2's redirect-afterlife is dead scope; T1A's "anonymous wire regression-pinned forever" drops (the parity gates that remain are correctness gates — golden grading corpus, print snapshots); T9 shrinks to publish-goes-viewer-only + delete/abandon the old R2 objects; at S9 cutover the anonymous identity branches and ingest's multi-wire ladder are REMOVED, not preserved. Until T6 ships, the anonymous path stays — it is the author's own testing loop, not a legacy guarantee.
- **Retention windows are contractual before mechanical** (docs/compliance/retention-policy.md): responses/grades 400d after class deletion, student account 30d after last membership, ip_hash/user_agent 30d, audit_log 2y. The purge-job extension lands with the server lanes; it is a hard S9-cutover checklist item — no real class before the job ships.

**Advisor follow-up (0015, 2026-07-28).** The post-apply security advisor run caught two instances of the 0009 "every new function needs its own revoke/grant stanza" rule being missed: `restore_activity` (0012) had been granted to `authenticated` without revoking PUBLIC, leaving it anon-callable at `/rest/v1/rpc/restore_activity` (bounded — its inline owner check compares `owner_id = auth.uid()`, NULL for anon, so anonymous calls raise without touching a row — but precisely the class of exposure 0009 cleared), and 0014's `generate_join_code` repeated the miss plus an unpinned `search_path`. 0015 closes both. **The standing lesson: the rule is real and easy to forget — a migration adding any function is not done until its revoke/grant stanza is written, and the advisor should be run after every DDL migration, not just during a dedicated housekeeping pass.** The intentional residue (three deny-by-default tables with no policies; the DEFINER-callable-by-authenticated WARNs whose real gate is an inline ownership/role check) is enumerated in 0015's footer so a future advisor run can be triaged in one read.

**0015 verified clean (2026-07-28, author applied).** Security advisor 15 → 13 lints; both targets closed, nothing new. Post-apply triage of what remains, so a future run is a one-read compare:

- **Intentional security residue (13):** `rls_enabled_no_policy` INFO on `allowlist` / `audit_log` / `student_domain` (deny-by-default, zero policies IS the access control); `authenticated_security_definer_function_executable` WARN on the nine RPCs whose real gate is an inline ownership/role check (`publish_activity`, `soft_delete_activity`, `restore_activity`, `current_user_is_teacher`, `is_class_teacher`, `is_class_member`, `join_class`, `list_class_members`, `soft_delete_class`); `auth_leaked_password_protection` (Google OAuth only).
- **A catalog check the advisor cannot make.** Querying `pg_proc` ACLs directly (not just reading the lint output) found the last four functions still carrying PUBLIC + `anon` EXECUTE: `can_read_activity`, `can_edit_activity`, `can_access_assignment`, `can_grade_submission`. The advisor is silent because 0009 made them `SECURITY INVOKER` — which is also why they are harmless: as `anon`, RLS applies with `auth.uid()` NULL, so they return false for every input, disclosing nothing. **Fold the revoke into the next DDL migration** rather than spending a standalone one; the lesson is that the advisor's function lints only cover DEFINER functions, so the ACL query in `scripts/verify-0013-0014.sql` §B1 is the more complete check.
- **New performance WARN accepted: `multiple_permissive_policies` on `classes` + `class_members`.** Two permissive SELECT policies each (`classes_select_own`/`classes_select_member`; `class_members_select_teacher`/`class_members_select_self`) — identical in shape and reasoning to the `submissions` pair 0009 accepted: they model two genuinely different audiences (teacher vs. student) that diverge further as the viewer arc lands, permissive policies OR together anyway, and the cost is one short-circuiting extra check per row. Merge only if a future perf pass measures it on real row counts.
- **`unused_index` INFO on all six new indexes** (`student_domain_added_by_idx`, `classes_teacher_idx`, `classes_assertion_by_idx`, `class_members_class_idx`, `class_members_student_idx`, `submissions_student_idx`) — expected: zero traffic, no student has signed in yet. Re-evaluate with real usage, same as the pre-existing set.

**0016 closes the stanza set (2026-07-28).** Rather than carrying the four-INVOKER-helper revoke into "the next DDL migration," it shipped immediately — a carry-forward parked across a session boundary is how the 0012 gap survived three years of migrations. `can_read_activity` / `can_edit_activity` / `can_access_assignment` / `can_grade_submission` are now `authenticated` + `service_role` only, and **every function in `public` has an explicit revoke/grant stanza**, so Supabase's default PUBLIC grant is nowhere load-bearing. Deliberate behavior change: an `anon` PostgREST SELECT against `activity_versions` / `assignment_students` / `submissions` / `grades` now raises "permission denied for function" instead of returning empty — no supported path is affected (app queries are authenticated; published-page surfaces go through postgres-owned DEFINER RPCs that keep EXECUTE regardless), it just fails louder. **The durable rule this batch produced: the Supabase advisor's function lints inspect SECURITY DEFINER only — run the `pg_proc` ACL query (`scripts/verify-0013-0014.sql` §B1, restated in 0016's footer) after every DDL migration; a clean advisor report is not evidence that grants are clean.**

**0016 verified (2026-07-28).** ACL completeness query returns 0 rows — no function in `public` is anon/PUBLIC-executable — and all four helpers carry `authenticated, postgres, service_role`, confirming no grant was dropped in the revoke. The security advisor is unchanged at 13 intentional lints before and after, which is the point: **the four functions this migration fixed never appeared in an advisor report at any stage.** That asymmetry (ACL query found a real gap the linter is structurally blind to; the linter's 13 remaining rows are all deliberate) is the reason the ACL query is now part of the post-DDL routine rather than an occasional audit.

### Read API S2 — definer RPC, upgrade-on-read, sanitizer, durable cache (2026-07-28)

First server-lane slice of the components-as-data re-architecture (T2 + T3 + the 3.2A meta endpoint; rulings 1B/4A/TV4-A/Q2B). Eight decisions, author-approved as a batch:

- **One Edge Function (`get-activity`), `--no-verify-jwt`, self-verifying.** The anonymous 3.2A meta branch forces the flag anyway; splitting into two functions would double the deploy-flag hazard surface. Modes: anonymous meta (title + teacher name, rate-limited) / authenticated resolve (current version pointer, `no-cache`) / authenticated content (version-keyed, `private, max-age=31536000, immutable`). `pnpm deploy:get-activity` bakes the flag in.
- **Access rule: any authenticated user reads the published current version** of a non-deleted activity. The R2 link-share model behind sign-in; classes carry the 13+ assertion and are NOT activity ACLs. Draft content and non-current versions are unreachable (a stale `version_id` 404s with `code:'stale_version'` so no stale URL is ever cacheable as content). Enforced in the DB (`get_published_activity`, DEFINER, runs as the caller), not in function code.
- **Durable cache home: a DB table (`activity_version_reads`), not an R2 object.** The R2 bucket is public — a cached sanitized doc there would bypass the auth rule entirely. Keyed `(version_id, sanitizer_rev)`; allowlist posture (RLS forced, zero policies, service-role only); lazy fill on first read (publish flow untouched); `ON DELETE CASCADE` (derived data never blocks deleting its source).
- **`SANITIZER_REV` is COMPUTED, not hand-bumped:** a hash of the registry's sanitize declarations + secret-field lists (+ a manual `SANITIZER_ALGO_REV` for logic-only changes). A spec change automatically orphans stale cache rows — the graph-kit-manifest / 0015-stanza class of forgettable step, designed out.
- **Cache stores the student-independent artifact; `ordering`'s serve shuffle is serve-time.** Seeded Fisher–Yates on `${version_id}:${user_id}` (+ block id + field): reloads never reshuffle, republish legitimately may, and the response stays deterministic per student so browser-side immutable caching holds. Documented residual: a shared browser profile can hand student B student A's cached permutation — cosmetic only (grading references item ids; no key material differs); a per-user URL would leak an id into logs for no security gain.
- **Sanitizer is a generic SanitizeSpec-driven transform in `packages/viewer/src/sanitize/`** (registry declares, transform obeys), with one hardening beyond the S0 contract: in-band secrets (BlankToken / MathPrompt fields) are stripped by an unconditional deep walk over every block, not gated on `inlineBlankSecrets` — the schema admits a prompted `math_inline` in ANY content array, and a declaration miss there must not become a silent leak (the flag is now documentation + type-projection signal). S2's cross-check also caught a real S0 gap: `MathPrompt.acceptableAnswers` was missing from `MATH_PROMPT_SECRET_FIELDS` ("also accept" answers would have shipped to students). Upgrade-on-read lives in `packages/schema/src/upgrade.ts` — a chain with ZERO migrations today (v2 parses, v1 fails loudly per the documented greenfield hard-cut; future v3 is one pure entry). Both reach the function via a NEW committed bundle (`_shared/viewer-server.bundle.js`, `pnpm bundle:viewer-server`, CI drift-guarded) so the read path never loads the renderer + KaTeX.
- **Leak tests are wire-level with sentinels, plus anti-drift guards:** one document carrying every block type and every interaction variant, every secret sentineled; the serialized wire is string-scanned for survivors. `data_plot.data` is asserted PRESENT (the `derivableFromServed` residual is a whitelist, never a silent skip). Guards make the two silent failure modes loud: every declared strip path must RESOLVE on a fully-populated fixture (a typo'd path = red test), and every suspicious schema key (top-level, interaction-variant, choice-level) must be declared stripped (a future block adding `solution` without a registry strip = red test). The type-level projection (`SanitizedBlock` — `block.solution` fails to COMPILE in viewer code) is enforced by `tsc` over `tests/` via the viewer's typecheck script — vitest 1.6's typecheck mode silently passed deliberately-broken assertions (red-green verified), so the plain tsc pass is the enforcement.
- **Anonymous meta is a postgres-owned DEFINER RPC (`get_activity_public_meta`) with deliberate anon EXECUTE** — the one documented exception to 0009's no-anon-functions rule (it exposes exactly the two facts every published page shows publicly today); rate limiting is a best-effort per-isolate window in the function, proportionate to what it guards. Reads write no audit rows (P3A's census aggregates from the write path; per-read audit at student scale is hot-path bloat).

**0017 applied + verified (2026-07-28), and a new failure mode found: recorded-but-never-run.** The author's `supabase db push` failed at `0013` with "relation student_domain already exists" — the SQL-editor history gap S1 already documented — but migration history nonetheless ended up listing **0013 through 0017 as applied while 0017's DDL had never executed** (no `activity_version_reads`, neither function). **This is strictly worse than an unapplied migration:** an unapplied one fails loudly on the next push, whereas a recorded-but-never-run one is skipped forever and surfaces only as a runtime 500 from the first caller. The S1 lesson ("repair, then push") is therefore incomplete on its own — **`migration repair` must be paired with a positive existence check that the objects the migration creates actually exist**, because repair marks history without touching schema. The verification scripts already encode the right check (§A object-shape queries); the rule is to run them even when history claims the migration is applied.

Verification after applying the body verbatim: cache table RLS forced with zero policies and PK `(version_id, sanitizer_rev)`; both functions DEFINER with `search_path=public`; grants exact (`get_published_activity` has no `anon`, `get_activity_public_meta` has no PUBLIC); the ACL completeness query returns exactly one row — `get_activity_public_meta`/`anon`, the deliberate 3.2A exception. Behavior (rolled-back fixture): resolve returns the current version, meta returns title + teacher name, meta on a draft returns 0 rows, **anon calling `get_published_activity` is refused at the grant layer** ("permission denied for function" — the belt-and-suspenders `auth.uid()` check inside is never even reached), an authenticated caller against a draft gets `Not available` (one message, no oracle), and `activity_version_reads` reads 0 rows as `authenticated`. Advisor 13 → 17 lints; **all four new rows were named in 0017's footer before the run** (cache-table `rls_enabled_no_policy`; `get_activity_public_meta` under both the anon and authenticated DEFINER lints; `get_published_activity` under the authenticated one) — the footer-as-pre-registered-triage habit from 0015/0016 paid off again.

**Rate-limit finding (2026-07-28, live measurement) — and the constraint it exposed.** Deploy verification of the 3.2A meta endpoint produced two findings, the second more important than the first.

**(1) The per-isolate limiter is nearly inert.** 95 sequential anonymous requests from one IP produced **zero 429s**. Supabase's Edge Runtime recycles isolates aggressively enough that the module-level Map is empty on most requests; the original "cold starts reset it" caveat understated it, since on this platform nearly every request is effectively a fresh isolate. (Mechanism inferred from the zero-429 result plus cold-boot-shaped latencies — a module-scope `BOOT_ID` logged per request would confirm it definitively.) **No in-memory scheme can work on Edge Functions**; a real limit needs shared state (a small DB counter table). Only caught because deploy verification exercised the limiter against the live platform instead of trusting unit-level reasoning — an in-memory counter in a serverless isolate is decoration until measured.

**(2) The inertness was MASKING a calibration bug — the finding that actually matters.** The limiter was keyed per-IP at 30/min. **A classroom is one IP:** every student in a school sits behind the same NAT, so "open this link now" is 30+ meta requests from a single address within seconds, and hundreds per minute across a campus at a bell change. The threshold was off by ~2 orders of magnitude against the real topology — and this endpoint serves the PRE-AUTH interstitial, so a 429 is the first screen a student ever sees, before they can sign in. **Had the limiter worked, it would have locked out a classroom;** it only appeared harmless because it never fired. Worse, the first version of this entry advised a future session to "move it to a DB counter table," which followed literally would have ported the broken threshold into working shared state and shipped the outage. Fixed 2026-07-28: ceiling raised to a school-safe 600/min, with the topology reasoning written directly above the constant (raising is safe; lowering toward a per-person number is the bug), and the hardening note now carries "port the school-safe ceiling with it."

**The durable rule: per-IP rate limiting is the wrong primitive for this product, because our users arrive thirty-at-a-time from one address.** Check that before reaching for IP-based throttling anywhere in this codebase. The generalizable lesson is that a dormant safeguard hides its own miscalibration — "it never fires" reads as "it's fine" right up until someone makes it work.

**S2 verified end to end on the live deployment (2026-07-29) — and why the first green run didn't count.** The first content-path run returned all 10 wire probes clean and was NOT treated as evidence: the activity under test was an empty default document (1 block, 904 stored bytes, zero answer keys), so the sanitizer was handed nothing and correctly returned nothing. **A leak scan over a fixture with no secrets is a vacuous pass** — the probes cannot distinguish "stripped correctly" from "never present." This is the same trap as the inert rate limiter one step earlier: a check that cannot fail looks identical to a check that passed.

The meaningful run used a purpose-built fixture (`scripts/leak-test-fixture.md`, committed so the test is repeatable and so a future session doesn't re-derive it): 22 blocks across fill_in_blank ×3, multiple_choice, matching, ordering, interactive_graph, number_line, math_block, short_answer, essay, faded_worked_example — 11,166 stored bytes carrying 9 of 10 secret markers, served as 8,436 (~2.7 KB stripped). The browser wire scan was clean, and a **stronger server-side scan of the cached artifact** (16 probes, including `inequalities` / `models` / `regions` / `tolerance` / `partialCredit` / `builtinFeedback`, which the browser list omitted) confirmed every secret absent while `allowNoSolution` survived — exactly the registry's declaration, so the positive control passed too, not just the negatives.

**Recorded gaps rather than glossed:** the fixture's `graph` fence produces a `graph_inequality`, so `correctPoints` (the `plot_point` key) had nothing to catch — that probe was vacuous even in the good run; and the `ordering` serve-shuffle is per-request, so it is absent from the cached artifact by design and was never exercised in production. Both are covered by the unit suite. The durable practice this produced: **when a verification passes, check what the fixture actually contained before believing it** — and prefer scanning the stored artifact server-side over probing the wire from a client, because the server-side scan can enumerate fields the client-side probe list forgot.

**The 13 "leaks" that weren't (2026-07-29) — substring probes conflate JSON keys with values.** Fixture 2's first run reported **13 leaked fields**. All 13 were false positives produced by the fixture itself: it documented its own probe list in backticked prose (`` `models` ``, `` `solution` `` …), the importer turned each backtick span into a text node whose value was exactly that field name, and the probes — plain substring matches like `'"models"'` or bare `'mistakeFeedback'` — matched the quoted **string value** as readily as a key. The correlation was exact: the 13 flagged fields were precisely the 13 words in that paragraph, and `correctInterval`, the one secret field the paragraph omitted, came back clean.

**The sanitizer was never at fault.** Re-probing the cached artifacts with a trailing colon — a JSON key is always followed by `:`, a string value never is — returns **false for all 19 secret fields across all three published fixtures**, with `"allowNoSolution":` present on both fixtures that author it (positive control intact). The earlier "clean" result on fixture 1 was re-proved the same way rather than left standing on the flawed method.

**Fixes:** probes now carry the colon; the fixture no longer names schema fields in prose, and keeps one deliberate sentence ("Show your full solution, and keep every measurement within tolerance") as a **precision control** — the words must appear as text while neither is flagged as a field, pinning the distinction against regression.

**Why this matters beyond the test.** The failure mode is not exotic: "show your solution" and "within tolerance" are ordinary teacher prose, so a colon-less leak scan would false-positive on real activities the moment one was written. A leak test that cries wolf on legitimate content gets muted, and a muted test catches nothing — the false positive was arguably more dangerous to the arc than a missing probe would have been. **Durable rules: probe JSON for `"field":`, never `"field"`; make the fixture's own documentation non-importable; and when a test fires, confirm the finding against the stored artifact server-side before believing the client.** Together with the vacuous-pass lesson above, both directions are now pinned — a test that cannot fail and a test that fails on everything look equally green from a distance.

### Math-gap submissions were never possible — the seam between two correct halves (2026-07-29)

**Found by submitting a real published page during Drop 0 §6c**, not by any test or review. Every submission from an activity containing an in-equation math gap (Model A, shipped 2026-07-22 `9f06198`) was rejected by `ingest-submission` with `responses failed schema validation / Invalid uuid`. **The feature has been unsubmittable since the day it shipped.**

**Both halves were individually correct, and that is the whole story.** `MathPrompt.id` is deliberately NOT a uuid — the id is embedded in a `\placeholder[id]{}` marker and MathLive rejects hyphens, so the importer mints `'g' + hyphen-stripped uuid` (`markdownToTiptap.ts`), documented at both the schema and the minting site. And the runtime deliberately registers gaps into the SAME `refs.blanks` map as ordinary blanks (`runtime/init.ts`), because Model A's ruling was to reuse the existing wire rather than add a shape. Neither decision is wrong. But `SubmissionResponses.blanks` was keyed `z.string().uuid()`, so the two met only in production.

**Why the suite was green.** `math-prompts.test.ts` tested the DOCUMENT schema (where a non-uuid prompt id is valid). `math-prompt-bridge.test.ts` tested the capture bridge. Neither ever constructed a `SubmissionResponses` containing a gap id. Two suites, both passing, both internally consistent, with the defect living precisely in the join they didn't cover — **the same shape as every other bug found this session** (an inert rate limiter, a vacuous leak scan, false-positive probes, a self-recursive RLS policy). The recurring lesson is that unit tests certify components while the failures live at seams, and only exercising the real path end to end finds them.

**Fix: widen the KEY, not the value.** `BLANK_ID_KEY` accepts a uuid OR `g` + 32 hex, applied to the current (v9) shape only — gaps postdate v9 and shipped without a wire bump, so every gap-bearing page sends v9, and the frozen v1–v8 shapes keep the uuid-only constraint that is all they could ever have contained. Deliberately a *widening, not an opening*: a guard test pins that `''`, `not-an-id`, `g123`, `gZZZZ`, path-traversal strings, and an over-long gap id are all still rejected.

**Server-only, and it repairs already-published pages.** The published HTML already sends the right payload; only validation refused it. So there is no republish, no `schemaVersion` bump, and no page change — one `pnpm deploy:ingest` makes every existing gap-bearing page work. This is the rare case where the CLAUDE.md ordering rule (redeploy ingest BEFORE republishing) is satisfied trivially, because nothing needs republishing.

**Regression pin added at the seam itself** (`math-prompts.test.ts` → "the seam"), red-green verified: reverting the key to `.uuid()` fails all three positive cases.

### Numbered canvas blocks had no gutter to sit in (2026-07-29)

**Found on a real published page during Drop 0 §6c, then browser-measured rather than eyeballed.** `number_line` and `data_plot` emitted a `.block-problem-number` div but had **no grid rule**, so the block stayed `display: block`. The number div — block-level with `text-align: right` — stretched the whole content column (measured **760px wide instead of 40px**) and rendered **alone on a line above the canvas with the number flung to the right margin**. Every graph-family question on every published page was affected: the blocks an Algebra I course leans on hardest.

**The defect was structural, not cosmetic-by-nature.** Emitting the gutter div (`blocks/number-gutter.ts`) and providing the grid that positions it (`runtime/styles.ts`) are two separate places, and nothing tied them together — so a block type could draw a number into a gutter that did not exist. Fixed by folding `number-line` and `data-plot` into the shared `.block-interactive-graph` problem grid (they are one visual family), and pinned by `numbered-gutter.test.ts`, which parses the stylesheet for `display:grid` + `grid-template-columns` rules and asserts every number-drawing block class is covered. Red-green verified; the failure message names the symptom, not just the assertion.

**Two adjacent fixes the browser pass surfaced, neither visible from the code:**

- **Gutter metrics were inconsistent.** The gap-bearing math block used `2rem / 0.25rem` while every other problem grid used `2.5rem / 0.5rem`, so its number sat 8px off and the numbers did not form one vertical rule down the page. Unified.
- **KaTeX re-centered numbered equations at TWO levels.** `.block-math__body { text-align: left }` alone was not enough: KaTeX's inlined stylesheet centers `.katex-display` **and** `.katex-display > .katex`. The first fix attempt made the equation *compute* as left-aligned while still *rendering* centered half a column away from its number — it measured correct and looked wrong. Only overriding all three selectors fixed it, and the guard now pins all three. **The lesson: a computed-style assertion is not a rendering assertion when a vendored stylesheet is in play.**

**Process note:** this was fixed against a renderer-rendered harness page served through the app's dev server and inspected with real `getBoundingClientRect` measurements — not from the screenshots that reported it. The screenshots correctly identified *that* something was wrong and misidentified *what* (they suggested the math block's number was misplaced; in fact its number was fine and its equation was centered away). Guessing from the screenshot would have fixed the wrong thing.

**0018 applied + verified (2026-07-29).** The recursion is closed: `users_select_self` is now `((id = (select auth.uid())) OR current_user_is_admin())`, and the direct authenticated `SELECT` that raised 42P17 returns exactly one row — the caller's own. The full isolation sweep that originally exposed the bug (by erroring) now runs clean, with both helpers behaving (`current_user_is_teacher()` true, `current_user_is_admin()` false for a teacher). Advisor 17 → 18; the single new lint is `current_user_is_admin` joining the documented DEFINER-with-inline-gate set. **Two counts in the sweep looked wrong and were checked rather than assumed** — 2 activity_versions and 1 submission visible — and both are correct owner-scoped RLS (the teacher published twice; the submission is on their own activity). Checking beat pattern-matching: "unexpected number in an isolation test" is exactly where a real leak would hide.

**A migration-history wrinkle worth the note:** applying through the MCP tool recorded the version as a timestamp (`20260729131811`) rather than `0018`, so history and the repo filename disagree — the same divergence class as 0017's recorded-but-never-run state, mirrored. It is harmless *here only because 0018 is idempotent by construction* (`create or replace function`, `revoke`/`grant`, `alter policy`), so a future `db push` re-runs it as a no-op and records `0018`. **The general rule this suggests: write DDL migrations idempotently where the cost is zero, because the history/file relationship has now failed in both directions on this project.**

### Direct-to-Storage image upload — the gate moves into policy SQL (0019, 2026-07-31)

**The decision:** author image uploads go browser → `activity-images` Storage bucket directly; the `upload-image` Edge Function is deleted. Authorization = ONE RLS INSERT policy on `storage.objects` that parses the activity id out of the object key (`{activityId}/{uuid}.{ext}`) and calls `can_edit_activity` **as the caller**. Ran through `/plan-eng-review` same day (3 findings + an 11-finding outside-voice pass, 0 unresolved); this entry records what survives re-litigation.

- **This deliberately REVERSES the posture 0019 shipped with hours earlier** (zero write policies; the function's service-role client as the only writer; "the gate is function code, not policy SQL"). What changed: with the R2 → Storage retarget done, the function was pure pass-through — same helper, same caller-scoped decision, plus a Deno hop, a multipart parse, a cold start (3–4s measured on this project), and a service-role write path that existed only to bypass the RLS we could just as well express. The function-based variant never deployed, so nothing live ever ran it.
- **The helper rule held under pressure.** The outside voice pushed inline text-comparison (`a.id::text = segment` — no uuid cast at all). Rejected: CLAUDE.md's "don't inline ownership checks in policies" exists so Phase-3 collaborator support changes `can_edit_activity` once and every gate follows — including this bucket. The cast hazard the inline form avoids is fully handled by the CASE guard instead.
- **The CASE guard is load-bearing, not style.** Postgres does not guarantee AND evaluation order, so `regex AND cast` can still evaluate the cast first and abort with a 22P02 error instead of a clean policy denial. `CASE WHEN <regex> THEN can_edit_activity(<cast>) ELSE false END` is the guaranteed order. Pinned by `scripts/verify-image-storage.sql` case D. Regex is case-insensitive (`~*`) — everything emits lowercase uuids today, but an uppercase-normalized id must not become a silent deny.
- **No UPDATE/DELETE/SELECT policies, on purpose.** Objects are immutable-by-absence (overwrite = UPDATE = no policy = impossible; uuid keys make collision vanishing anyway); reads ride the public-URL route; deletion is the post-S9 GC's job (TODOS.md). **Adding an UPDATE policy is the prerequisite for TUS/resumable upload progress** — that's a policy-design amendment, not a UI task (TODOS.md carries the warning).
- **Bucket limits promoted to load-bearing.** With the function gone, `allowed_mime_types` + `file_size_limit` are the ONLY server-side mime/size enforcement (the client's fail-fast copy is UX, not security). MIME honesty is unchanged from the function era: both trusted the client's claimed type; the allowlist caps the damage to 5 raster types either way.
- **One-way door, named:** the function was the natural choke point where per-teacher upload quotas would have gone for free. Direct upload means any future quota is DB-side machinery (trigger or policy subquery on `storage.objects`). Accepted — quotas are a Phase-2 "decide at phase start" item and were never built in the function either.
- **Apply-time feasibility was probed, not assumed:** `CREATE POLICY on storage.objects` verified working as `postgres` on this project (rolled-back probe, 2026-07-31) before committing the migration. The "must be owner of table objects" hosted-Supabase failure class doesn't bite here; the recovery (dashboard SQL editor + `migration repair`) is documented in 0019's header anyway.
- **Verification is two-layer by design:** `verify-image-storage.sql` proves the predicate (8 impersonated cases, rolled back); the live E2E checklist at its foot proves the Storage API layer the SQL can't reach (JWT forwarding, bucket limits firing, upsert header). The JWT-forwarding check runs FIRST — "the client library forwards the session token" is the exact assumption class that already failed once on this project (functions.invoke, see the pre-rewrite uploadImage.ts header in git history).

**Image cache-control on the free tier (verified 2026-07-31, post-ship).** `uploadImage.ts` sets `cacheControl: '31536000'` and it IS stored correctly (`storage.objects.metadata.cacheControl = "max-age=31536000"` on a real editor upload — which also settles the Blob-vs-byte-array question: supabase-js's multipart branch honors the option, so passing the `File` directly is fine and no code change is warranted). **The public endpoint nonetheless serves `cache-control: no-cache`**, because passing the stored value through to browsers is Supabase's Smart CDN behavior and Smart CDN is **Pro plan and above**; the free tier answers with `sb-gateway-mode: direct`. **This is not fixable in our code** — we already set the only value we control. **Impact was measured rather than assumed:** the response carries a strong `etag`, and a conditional request returns `304` with a zero-byte body, so the cost is one revalidation round-trip per image per page load, never a re-download of the bytes. Accepted. Two things to remember: this is a genuine (minor) regression from the R2 era, where a raw signed PUT set `public, max-age=31536000, immutable` on the object directly and no gateway rewrote it; and it converts into a free improvement the day the project moves to Pro, so it belongs on the list of things a plan upgrade would buy.

## Check-RPC latency — cold-start mitigation RULED (2026-08-01, closes finding R2)

**The ruling R2 deferred is now made on data.** R2 named cold start as T5's open
risk (Edge cold starts measured 3–4 s in S2) and deliberately refused to
pre-build a fix: *"measure the batched RPC first; if p95 misses, options are a
warm-ping cron or accepting with explicit Checking… UI. Don't pre-build
either."* The measurement exists, so the decision is made.

**Measured on the deployed function** (`scripts/verify-check-e2e.js`, 2026-08-01,
activity `6a84c8cb`, a real 11-item section: 8 blanks + 1 MC + 2 free-text).
n=42 sequential awaited checks, derived from `section_checks.created_at` deltas
— each gap is one full round trip including the grade and the durable write:

| min | p50 | p90 | **p95** | max | mean |
|---|---|---|---|---|---|
| 598 ms | 855 ms | 1474 ms | **1503 ms** | 4183 ms | 1084 ms |

Client-side wall clock corroborates: the two first-calls measured 1515 ms and
1124 ms in the browser.

**RULED: accept the latency. No warm-ping cron.** Reasoning, in the order it
actually mattered:

1. **Warm p95 of 1.5 s is fine for this interaction.** Check is explicit,
   user-initiated, and already renders a pending state (2.1A). This is not a
   keystroke path or a page load.
2. **The ~4 s outlier is one cold start, and it lands on one student per
   period.** Everyone who checks after them is warm.
3. **The classroom pattern self-mitigates.** Thirty students checking within a
   few minutes keeps the isolate alive; the shape of the load is the fix.
4. **A cron is a bad trade at this size:** a permanently scheduled job, running
   against the free tier around the clock, to save one student roughly three
   seconds once a day.

**What would reopen this:** a p95 that drifts past ~2.5 s warm, or a report of
checking feeling slow mid-lesson. Re-run the E2E's timing section first — the
number, not the impression, is what should move it. If it does reopen, the cron
is still the cheaper option than restructuring the RPC, because the grade itself
is not the cost: the floor here (598 ms) is dominated by transit and the durable
write, not by grading work.

**Methodology note for whoever re-measures:** the table above is derived from DB
timestamps rather than the script's own client-side numbers, because the row
deltas isolate server round-trip from browser overhead. The script prints its
own `WARM TIMING` line as the client-side view; expect it to read slightly
higher. Both are in the same place, which is why the ruling did not need to pick
between them.

## Print parity — the artifact, and why it is not a pixel diff (2026-08-01/02, S5, closes finding R4)

Finding R4 said the renderer-retirement print gate was **unfalsifiable**: "per-block
print-parity snapshots" compared a string-rendered HTML page against a React tree, and
no comparable artifact was defined. It proposed cross-surface pixel-diff within a ruled
threshold. **That half was rejected and replaced.**

**Why cross-surface pixel-diff cannot work here.** The two surfaces are deliberately
different renderings — different DOM, different container layout, different font
pipelines (@fontsource in the viewer, R2 `@font-face` on published pages), different
KaTeX asset versions. A threshold loose enough to tolerate those legitimate differences
is too loose to catch a missing blank underline; one tight enough to catch a missing
underline is permanently red. Either way the gate stops meaning anything, and a
permanently-red gate is worse than none because people learn to ignore it.

**What replaced it (ruling S5-6), in three parts:**

1. **Rules parity — the falsifiable core.** `printExpectations(type, ctx)` states what
   must be true of a printed block; the gate asserts it on BOTH surfaces via computed
   styles and DOM. Parity means "every ruled rule holds on both", which is a claim that
   can fail.
2. **Viewer-only screenshot baselines.** Pixels catch what the rules do not NAME (a
   collapsed margin, an overlapping figure). Compared viewer-against-viewer, where the
   only variable is our own code — the threshold problem does not arise.
3. **One recorded human sign-off** on a generated side-by-side contact sheet. "Does this
   read as well on paper" is not automatable; making it an explicit, dated, one-time
   read is more honest than a threshold pretending to encode taste.

**Consequences worth knowing.**

- **The gate is SPEC-referenced, not output-referenced.** It asserts the registry's
  `PrintSpec`, not the renderer's current output. That is what makes an improvement
  expressible: S5-OV6 fixed three long-standing break-inside oddities (a numbered
  equation, a chart, and a free-text prompt could each split across a page) on the viewer
  while published pages keep their behaviour until they retire. Improved rules are marked
  viewer-only **by name, with the reason**, so the exemption is auditable rather than a
  silently weakened assertion.
- **Three fixture classes, not one.** A roster keyed to the block registry is structurally
  blind to layout (rows/columns/footprint) and to the document print layer
  (header/reference box/glossary/paper size/spacing) — none of which is a block. Those
  became their own guarded rosters (S5-OV1/OV2).
- **Print emulation never paginates.** `emulateMedia({media:'print'})` applies print CSS
  in one continuous viewport, so every break rule checked there is a computed VALUE, not
  a fact about layout. `page.pdf()` is the only thing that runs Chromium's paged-media
  layout, so a separate suite covers it. Per-block "did not straddle" is deliberately NOT
  automated: Chromium's PDF text is in compressed streams, and the contact sheet shows a
  human the same property directly.
- **The gate must not depend on infrastructure the cutover deletes.** It runs with all
  external requests blocked, so it survives R2's removal at S9. A retirement gate that
  dies with the thing it retires is no gate.

**What the first runs found**, before it guarded anything: seven real defects (a check
reading the wrong element, improved rules asserted on the wrong surface, border STYLE
measured where Tailwind's preflight makes WIDTH the signal, a lazy-component wait
satisfied by other blocks' wrappers, callout having ONE fixture when the rule is that
four variants stay distinguishable, variant checks running against the wrong instance,
and two more viewer-only improvements). Then red-green testing found an eighth: the
"solutions never print" rule passed **vacuously**, because pills, feedback, and solutions
only exist after a check — the rule was asserted about elements never on the page. Fixed
by checking a section first. Both classes of finding are the reason a gate gets red-green
tested before it is trusted.

## Static SVG lives in graph-kit, not in the renderer (2026-08-02, S5-T2)

Both surfaces must print IDENTICAL static graphs, and the viewer cannot import the
renderer (it dies at S5.5) any more than the renderer should import the viewer. So
`graph-svg` / `number-line-svg` / `data-plot-svg` moved wholesale into
**`@activity/graph-kit/static-svg`** — a pure subpath with no DOM, no JSXGraph, no
MathLive, the same discipline as `/scorers` and for the same bundle-leak reason.
graph-kit is the package both surfaces already depend on and the one that outlives
cutover, so it is the only home that adds no dependency edge and leaves nothing dangling.
The renderer re-exports; its 724 tests passing unchanged is the proof the move was
behaviour-preserving.

Both surfaces now emit `data-drawables` from that ONE renderer, which is what makes the
gate's count assertion mean something: they cannot disagree about what they drew.

**The rule the count encodes (S5-1 as amended by OV4):** a QUESTION prints empty axes —
the student's plotted work is an answer, and a printed worksheet is the blank version. A
DISPLAY figure prints its AUTHORED drawables, because that is the content the block exists
to show. The original "empty axes everywhere" ruling would have silently deleted what was
being taught, and no treatment-level rule would have noticed. It is enforced by narrowing
on the interaction type, so only the display variant even HAS a drawables field.

## Sign-out is only correct if it works offline (2026-08-02, S6-6)

`supabase.auth.signOut()` **returns early on a network failure without clearing the
local session** — verified against the installed `@supabase/auth-js`, not assumed. So a
student tapping Sign out on dying school Wi-Fi saw an error, walked away, and left the
next student signed in as them. Every per-user guard downstream — buffer keys, cache
names, queued checks — was correct and irrelevant, because the session itself named the
wrong person, and nothing anywhere reported a problem.

`signOutEverything` now falls back to `signOut({ scope: 'local' })` when the network call
fails, and still throws afterward so the caller can say "couldn't reach the server" — by
which point the device is already clean, which is the part that matters to whoever sits
down next. Purge runs FIRST either way: the on-device work is ours to remove, and a failed
network call must never be what decides whether it survives.

**The standing rule this produces: every sign-out control calls `signOutEverything`.** It
had ZERO callers when the hardening landed — `Home.tsx`, the app's only sign-out button,
called the raw client method — so the purge never ran and the hole stayed open for the one
user type who can actually sign out today. A control that ends a session without calling
this one is a shared-device leak by construction.

The idle prompt (2.4A) escalates to a local sign-out after an unanswered grace period.
A prompt alone assumes somebody is still there to read it, which is exactly the assumption
that fails on a shared cart at the end of a period.

## One idempotency key, and it persists (2026-08-02, S6-3, overturns an S3 rule)

The store used to keep check idempotency keys in a module-local map, documented as
"deliberately NOT persisted" on the reasoning that a key only matters while a request is
in flight in this tab. That reasoning missed the dominant Chromebook failure: request
sent → Wi-Fi drops → response lost → lid closes. On reopen the in-memory key is gone, the
retry mints a new one, and the server records a SECOND attempt for one piece of student
work — at exactly the moment the network is worst.

The key now lives in store state and therefore in the buffer, so that retry is a replay
against S4-B2's idempotency index. It is cleared on success, so a deliberate re-check
still creates a new attempt; only the failed-and-retried case collapses. The old comment
is replaced in place with the reasoning that superseded it, so the next reader sees why it
changed rather than re-deriving the original argument.

**Corollary:** the queue mints nothing. Two authorities over one key is two attempts for
one piece of work, and the second one is invisible until a teacher wonders why a student
checked twice.

## What the service worker may cache — and what it must not (2026-08-02, S6-5/S6-11; supersedes the user-scoped-runtime-cache design)

**The API is never cached by the worker.** The Cache API keys by URL and ignores the
Authorization header, and this platform serves a per-student SHUFFLED document from one
URL — so a cached response is another student's paper, graded against the wrong shuffle.
That is silent grading corruption, not staleness. The earlier plan was to scope runtime
cache names per user; retiring the mechanism is stronger than scoping it, and the document
a student needs offline is already kept per-user by the store's `documentCache` (a
localStorage record under the `activity-viewer:` prefix, where the sign-out purge and the
boot sweep already reach it). `caches.ts` remains as the naming contract and guard rail
for any future per-user cache; today it has no producer.

**Precache is the navigation document alone** — one entry. "Shell-only globPatterns" reads
well until you measure the shell: the entry chunk is ~3 MB because the editor and viewer
still share one bundle, and a glob wide enough to catch it also catches sibling chunks no
route needs. Every asset filename is content-hashed and immutable, so runtime CacheFirst
reaches the same offline result while downloading only what the student actually opened —
which is what the budget ruling was protecting; the glob was only ever the means.

**The worker installs during the first visit, so that visit's own requests never pass
through it.** Nothing is cached, and offline would not work until a student's THIRD visit.
`warmAssetCache` seeds the runtime cache from `performance.getEntriesByType('resource')` —
exactly what the page used, no manifest to keep in sync, and nearly free because the
assets are immutable and already in the HTTP cache.

**The shell is NOT purged at sign-out** (refining R5, which asked for the caches holding
student WORK). The shell is application code, identical for every user, holding no student
data; wiping it would make the next student re-download the app over school Wi-Fi and
would defeat the offline capability precisely on the shared devices that need it most.

## One editable tab: Web Locks, the availability probe, and no BroadcastChannel (2026-08-02, S6-4)

Two tabs on one activity both flush the same buffer key, so the last writer wins and the
student loses half their work with nothing on screen to explain it. The guard is
`navigator.locks`, not the hand-rolled heartbeat the design sketched: a heartbeat forces a
staleness guess, and Chromebooks make that guess wrong in both directions — background
tabs are throttled hard (alive looks dead) and an OS-killed tab keeps its timestamp (dead
looks alive). Web Locks release when the holder's context goes away, which deletes the
question instead of tuning it.

**No BroadcastChannel.** When a tab steals the lock, the displaced tab's own request
promise rejects — the steal IS the notification. A second channel carrying the same fact
is a second thing that can disagree with the first.

**The `ifAvailable` probe is load-bearing, and its absence was a real bug.** A plain queued
request is SILENT when the lock is taken: it simply never resolves, so "will never be
granted" is indistinguishable from "not granted yet". With the route starting optimistic
(a read-only flash on every single-tab load is a worse trade than a brief editable flash
on a rare second tab), the losing tab stayed editable and could clobber the winner. jsdom
has no `navigator.locks`, so every unit test had been taking the single-tab branch; only
a real browser could find it.

**Read-only means the tab stops WRITING, not just stops typing** — including the
flush-on-hide. Otherwise closing a stale tab hours later overwrites the live tab's newer
work. It is implemented as one `disabled` fieldset around the worksheet rather than a prop
threaded through 22 block components, so a block type added years from now inherits it
without knowing the rule exists; canvas surfaces are not form controls and get a companion
`pointer-events` rule.

**Testing principle this produced:** assert the GUARANTEE (exactly one tab editable, and
takeover decides which), never which tab wins. React StrictMode remounts the effect in dev,
releasing and re-requesting the lock, so the initial winner is genuinely either tab. Two
tests were written asserting an ordering the lock never promised; both were flaky, and the
second one passed at lane level while failing four runs in five in isolation.

## The orphan GC must never eat unsent work (2026-08-02, S6-9)

Version-keyed buffers accumulate across republishes, so a GC collects superseded ones on
load. Shipped unconditionally, that GC **deleted the buffer of every student who had left
work or a queued check unsent** whenever a teacher republished overnight — no error, no
trace, and the student's only symptom is that their work is gone. The slice that built the
buffer introduced the thing that destroyed it.

The GC now has exactly one exception: a superseded buffer survives if it still holds
responses or a pending check. Tested from both sides, because a GC that stops collecting
spent buffers brings back the quota ceiling it was written for.

What happens to that work is the boot path's decision, not the GC's: render the student's
own version PINNED when the device still holds its document (the grader accepts
non-current versions by design, so the queued check can still fire), and otherwise
preserve the blob behind an honest notice. Never auto-discard in-flight work to show
someone a newer document.

## Three e2e lanes, and why they are separate (2026-08-02, S6-12)

The editor lane keeps reusing whatever dev server is up, so local iteration on 200+ specs
is unchanged. The **student** lane runs on its own server with PINNED Supabase env, because
the harness fakes a sign-in by writing the storage key supabase-js derives from the
configured URL — a reused server carrying a real `.env.local` derives a different key and
lands every student spec on the sign-in screen, a failure that looks like anything but its
cause. The **sw** lane runs against a production build, non-parallel with one worker,
because the worker only exists in a build and outlives a page: one spec's registration
would otherwise control the next spec's first navigation.

The harness changes no shipped code — no dev-only session backdoor to keep out of
production builds. The one supabase-js internal it leans on is pinned by a unit test that
writes a session under the derived key and asserts a real client reads it back.

**Offline in a spec means two switches, not one.** Playwright's `route` handlers bypass
`context.setOffline`, so a stubbed endpoint keeps answering happily while the browser
believes it is offline — a test written with only one of them silently verifies a normal
online load.

## The teacher answer key travels beside the document, never inside it (2026-08-03, S5.5 D3A)

**Decision.** Components stay typed against `SanitizedActivityDocument` — the shape whose defining property is that answers were removed — and the teacher print route supplies answers through a second channel: a React context carrying a flat block-id-keyed map extracted from the AUTHORED document.

**Why not a `showAnswers` prop.** Widening the components to accept raw documents would have made the sanitized-type projection and the "a component cannot read an answer" property into conventions rather than facts. With the context, the guarantee is structural: a subtree with no provider has nothing to read, and the student route never imports the module. "Can this surface reveal answers?" becomes a question about the import graph, which a test can answer.

**The key is position-free, and that is the load-bearing part.** It names answers by choice id, target id, item id — never "B" or "third". Printed versions shuffle presentation, so a stored letter would be correct for Version 1 and silently wrong for every version after it, which is a teacher marking thirty sheets against a key that disagrees with them. Components derive the letter or number from the order they are rendering, so one canonical key stays correct for every variant.

**Two channels, one walk.** In-band answers (blank tokens, math gaps) are found by a deep structural walk at any depth — the same posture the S2 sanitizer and S3's `blockIndex` already take, because the schema admits a prompted `math_inline` in any content array. Out-of-band answers (which choice is correct, which target pairs with which item) need per-type extractors, and `ANSWER_KEY_COVERAGE` forces every `auto_gradable` type into a justified bucket with a non-vacuity test proving each claim.

**A viewer-only improvement, recorded rather than hidden.** The renderer never filled math gaps at all (`renderMathBlock` is called without `showAnswers`), so a gap-bearing equation — a graded question — printed a key with nothing in it. The viewer fills them, and the parity gate names this as viewer-only instead of requiring the viewer to reproduce a hole.

**What would reopen it.** A component that genuinely needs an answer to lay itself out (as the graph widgets needed the key for handle count, solved by the sanitizer's derive step). That is a sanitize-spec question, not a reason to widen the component types.

## Print shuffles are declared apart from serve shuffles (2026-08-03, S5.5 D15A/D17A)

**Decision.** Arrays that must not print in authored order are declared on the registry's `PrintSpec` (`shuffled`), never in `sanitize.serveShuffled`, and a per-block `shuffleLockedBy` field lets an instance opt out.

**Why the separation is load-bearing in both directions.** Adding a field to `serveShuffled` changes what the server sends every student (a wire change) and moves `SANITIZER_REV`, orphaning the read cache and forcing a `get-activity` redeploy. Print needs neither. A test pins `SANITIZER_REV` at its deployed value so the placement cannot drift back.

**The defect this closed.** The teacher print path sanitizes without the per-student serve shuffle, so an ordering question — whose authored sequence IS the answer — would have printed the answer as the worksheet, for a whole class. The renderer had always shuffled on print; the viewer would have stopped, and the parity gate could not have noticed because no rule named the property. Found by the eng review's outside voice AFTER the answer-channel design had been ruled.

**The generalised guard is the better half:** every serve-shuffled field must ALSO be print-shuffled. A field is serve-shuffled for exactly one reason — its authored order is secret — so any such field the print path does not shuffle puts that secret on paper. The next block type cannot repeat it.

**A shuffle must never deal the identity.** The viewer's `seededShuffle` could return the authored order; the renderer's has always guaranteed it cannot. A fair shuffle lands there 1/n! of the time, which is negligible at six items and one student in two for a two-item ordering block. Now matched. S4's grading guard for the served-equals-authored case stays, because it also covers documents served unshuffled.

**The opt-out is declared, not special-cased.** "May this block be rearranged?" is per-type knowledge (`multiple_choice.print.shuffleLockedBy = 'lockChoiceOrder'`), so the transform stays a generic walk. No heuristic decides it: "all of the above" is guessable from text, "both A and B" is not, so the teacher who wrote the question says so once in the editor.

## The foldable clones the app's stylesheets rather than inlining them (2026-08-03, S5.5 D7A as spiked)

**Decision.** The foldable's measure iframe and printed document reproduce the app's CSS by cloning its live `<style>`/`<link>` elements, not by importing CSS as strings.

**Why the ruled approach was wrong, and how we found out.** D7A planned to inline CSS (`?inline`) and hand-build an `@fontsource` tag. Spiking it first showed KaTeX's stylesheet references its fonts RELATIVELY (`url(fonts/KaTeX_Main-Regular.woff2)`), which in a `srcdoc` frame resolve against the app's page URL — so every glyph would fall back and every equation would measure at the wrong height, mis-paginating panels. The renderer had sidestepped this by rewriting those URLs to a CDN, a network dependency the viewer should not inherit.

**Why cloning works.** Stylesheets the app has already loaded carry absolute URLs, because the bundler resolved them when it injected them. Cloning gets correct fonts in dev and production, plus any `@fontsource` family lazily loaded for the activity's typography, with no build-time rewriting and nothing to keep in sync.

**Two things a panel is not.** A panel is not a PHONE: the viewer collapses multi-column rows below 480px, and a panel is a quarter of a landscape sheet, so the breakpoint fired and flattened authored column ratios on paper. A panel is not a SCREEN: panels render in screen media, so `@media print` never fires in them — sheets came out dark on a dark-mode machine with the section card's border drawn around every panel. Both are countered in the panel stylesheet, and both were found by driving a browser, not by any test.

## Teacher display_name is a name or nothing — never the email (2026-08-04, migration 0021)

**Decision.** Three layers, all shipped in 0021: `handle_new_auth_user` stores `nullif(trim(full_name), '')` instead of `coalesce(full_name, email)`; `get_activity_public_meta` refuses to return an email-shaped value (`\S+@\S+\.\S+`, the same line the client's `looksLikeEmail` guard draws — that client guard stays as a third layer); and rows where the old fallback had stored the email are backfilled to NULL. The author's own row went to NULL, not a hand-picked name (ruled 2026-08-04): neutral attribution ("your teacher") until a settings control exists. The teacher-facing "how your name appears to students" control is **backlogged, not designed** — no settings surface exists yet and one real teacher account doesn't justify building it mid-rewrite.

**Why the leak existed.** Ruling 3.2A put "teacher display name" on the ANONYMOUS pre-auth screen, meaning "Mrs. Jafari". But display_name was populated by the 0003/0013 signup trigger's `coalesce(full_name, email)`, and Google supplies no `full_name` for every account — so the column silently held an email, and the anon RPC published it to anyone with the link. Found live 2026-07-31; client-side suppression was the interim mitigation.

**Why NULL beats deriving a display form.** The RPC could have synthesized "first name" or a mangled email, but any derivation invents a name the teacher never chose — the column should hold the truth (a chosen name, or nothing) and NULL is already a designed-for state in the 0017 meta contract, with fallback copy shipped in the client. When the settings control lands, it writes a real value and all three layers pass it through untouched.

**Why the backfill is scoped to `display_name = email` exactly.** That equality is precisely the trigger artifact; anything broader (e.g. "any email-shaped value") could rewrite a name a user deliberately set. The RPC guard makes rendering such a value impossible anyway, which is what lets the backfill stay surgical.

**Follow-on the same day: the real name came off too, by hand.** Verifying 0021 surfaced a third teacher row (created 2026-07-29) where Google HAD supplied a `full_name`, so it held `"Zan Reed"` and published it to the anonymous pre-auth screen. That was ruling 3.2A working correctly — a name is what the screen is for — and the backfill left it alone on purpose. The author nonetheless ruled it NULL, cleared with a direct one-row UPDATE rather than a migration (a personal-preference data edit, not schema; recorded in STATE because a migrations-only rebuild won't reproduce it).

**The signal that ruling sends to the deferred control.** Asked twice in one day what should appear as their student-facing attribution, the author chose "nothing" both times — including over a perfectly valid real name. So the backlogged name-appearance control should default to publishing NOTHING and make a name opt-in, rather than adopting the SSO profile's `full_name` automatically. The trigger still stores what Google supplies (correct: other teachers may well want their name shown), but "stored" and "published by default" should not be the same decision.

## The account clock waits for the work (2026-08-04, ruling + migration 0023)

**Decision.** When a student leaves all their classes, the **work's retention clock wins**. An account becomes purge-eligible only once no retained work remains — it is not deleted at 30 days while its submissions are still inside their own window. `submissions.student_id` stays ON DELETE RESTRICT; it is now the *enforcement* of the ruling rather than an obstacle to it.

**The question was forced by a conflict between two promises.** The retention policy said the student account goes at 30 days, and the work at 400 — but the account is what makes the work attributable, and RESTRICT meant the two could not both happen. The clocks also start from different events (last class membership ending vs. the class being deleted), so the conflict is routine, not an edge case.

**Why not cascade the work away with the account.** Simplest, and it would have made the 30-day promise literally true — but the 400-day window exists to keep *attributable* school records ("can I see my grade from last semester"). A student removed from a class mid-year would lose their graded work 30 days later while the class was still running. That destroys the thing the window was for.

**Why not sever and pseudonymize.** On paper it satisfies both: identity gone at 30 days, work retained for 400. In practice it needs the `users → auth.users` FK dropped (public.users cannot outlive auth.users today), a `submissions` FK change, and a pseudonym scheme — and once the work is unattributable it no longer answers the question it was being kept for. Maximum cost, self-defeating outcome.

**What settled it** was a sentence already written for students in privacy-policy.md: *"submitted classwork may be kept for the school's records window, then purged."* The student-facing promise already contemplates work outliving an account-deletion request. The retention policy's flat "30 days" account line was the one that had to change; it now states that the account follows the work.

**The honest cost, which must stay disclosed:** a departed student's email and display name persist for as long as their work does — up to ~400 days. That is the price of attributable school records, and it is the minimum needed to serve the documented purpose.

### Three defects surfaced underneath the ruling

**The purge job deleted the wrong table.** 0003 ran `delete from users` under a comment claiming it "cascades to auth.users via the FK from public.users". The FK runs the other way — `public.users.id REFERENCES auth.users(id) ON DELETE CASCADE` — so deleting `public.users` left the Google identity (email, name, provider metadata, OAuth consents, sessions) in `auth.users` permanently, while the policy promised both were gone in 30 days. Deleting `auth.users` is what actually removes an account.

**One ineligible account aborted the whole nightly run.** Same failure class 0022 fixed for `section_checks`: several children of `users` are RESTRICT or NO ACTION, so a single blocked account raised 23503 and rolled back everything the job had already done. Eligibility is now a per-account precondition — blocked accounts are skipped and counted, never fatal. Both behaviors were reproduced against the live function inside rolled-back transactions.

**No account can be purged at all, and 0023 deliberately does not fix it.** `audit_log.actor_id` is NO ACTION and the signup trigger writes a `user.create` row for every account, so every account is permanently blocked by its own audit trail. Resolving it means ruling on audit_log's 2-year security window against account deletion — SET NULL the actor and keep the event, delete the rows with the account, or make the account wait out the two years. That is another compliance decision, so 0023 makes the blockage *visible and reported* rather than guessing. Queued in STATE.
