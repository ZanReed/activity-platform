{
  "name": "@activity/renderer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
  "test": "vitest run",
  "test:watch": "vitest",
  "typecheck": "tsc --noEmit && tsc --noEmit -p src/runtime/tsconfig.json",
  "build": "tsc && tsc --noEmit -p src/runtime/tsconfig.json",
  "clean": "rm -rf dist"
  },
  "dependencies": {
    "@activity/schema": "workspace:*",
    "katex": "^0.16.10"
  },
  "devDependencies": {
    "@types/katex": "^0.16.7"
  }
}
